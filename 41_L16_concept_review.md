# CMU 10-708 Lecture 16 概念体系梳理 — 结构学习

> 整理自 Eric Xing 教授 CMU 10-708 Lecture 16: Structure Learning — GGM, Neighbor Selection, Time-varying Networks
>
> 核心教材: Koller & Friedman Ch.18, Friedman et al. (2008), Meinshausen & Bühlmann (2006), Kolar et al. (2010)

---

## 📐 全局定位：从参数学习到结构学习

```
L1-L10: 推断 (已知图结构, 做查询)        L5: 参数估计 (已知图结构, 估参数)
    │                                        │
    └────────────────┬────────────────────────┘
                     │
                     ▼
           L16: 结构学习 (Structure Learning)
           
           问题: 图结构本身是未知的 — 如何从数据中学习?
           
           三大主题:
           ① Graphical Gaussian Model — 精度矩阵 = 图
           ② Neighbor Selection — 逐节点 Lasso 回归
           ③ Time-varying Networks — 时变图结构
```

**一句话概括 L16**: 从 i.i.d. 样本学习概率图的结构 — 对于高斯数据, 图 = 精度矩阵的非零模式, 可以用 Graphical Lasso 或逐节点 Lasso 估计; 当结构随时间变化时, 引入时间平滑约束。

---

## 概念 1：高斯图模型 (Gaussian Graphical Model, GGM) — 精度矩阵即图

---

### 1.1 多元高斯的条件独立性 (🔑🔑🔑)

```
X = (X₁, ..., X_p) ~ N(0, Σ)

协方差 Σ:  描述变量的"共变性" — Σ_ij 大 ⇏ X_i ⟂ X_j
精度矩阵 Ω = Σ⁻¹: 描述变量的"净依赖" — Ω_ij = 0 ⇔ X_i ⟂ X_j | rest!

定理 (核心!):
  对于多元高斯分布:
    X_i ⟂ X_j | X_{others}  ⇔  Ω_ij = 0  (精度矩阵的元素)

  等价: 偏相关系数 ρ_{ij|rest} = -Ω_ij / √(Ω_ii · Ω_jj)
        ρ_{ij|rest} = 0 ⇔ 无连接边

直觉:
  Σ_ij 包含直接效应 + 间接效应 (通过其他变量的路径)
  例: X₁→X₂→X₃: Σ_{13} ≠ 0 (因为 X₂ 传递了关联)
                 Ω_{13} = 0 (给定 X₂ 后, X₁ 和 X₃ 独立!)

  Σ 是"全部关联", Ω 是"扣除中介后的净关联"
```

### 1.2 为什么精度矩阵 = 图? (🔑🔑)

```
多元高斯的联合分布 (均值=0):
  P(x) = (2π)^{-p/2}·|Ω|^{1/2}·exp(-½ x^T Ω x)

对对数密度求导:
  ∂²log P(x) / ∂x_i ∂x_j = -Ω_ij

如果 Ω_ij = 0: x_i 和 x_j 的交叉项消失
             → 密度函数可分解 → X_i ⟂ X_j | rest

用 Hammersley-Clifford:
  基团 (clique) 由 Ω 的非零模式定义:
    (i,j) 有边 ⇔ Ω_ij ≠ 0
    → 图结构 = Ω 的支撑集 (support)!

例:
      Ω = [[ 2, -1,  0],
           [-1,  2, -1],
           [ 0, -1,  2]]

  图: 1 — 2 — 3  (链式结构, 没有 1—3 边!)
```

### 1.3 精度矩阵 vs 协方差矩阵

```
     Σ (协方差)                      Ω = Σ⁻¹ (精度矩阵)
  ┌──────────────┐               ┌──────────────┐
  │ Σ_ij = 0     │ → 边缘独立     │ Ω_ij = 0    │ → 条件独立
  │ 即 X_i ⟂ X_j │  (弱!)        │ 即 X_i ⟂ X_j│ | rest (强!)
  └──────────────┘               └──────────────┘

  Σ 通常稠密 (几乎所有元素 ≠ 0)     Ω 可能是稀疏的 (许多 0)
  因为间接关联填充了 Σ              因为条件独立消除了间接效应

  核心: 图结构学习 = 找 Ω 的零模式 = 找精度矩阵!
```

---

## 概念 2：Graphical Lasso — 估计稀疏精度矩阵

---

### 2.1 问题设定 (🔑🔑)

```
给定 n 个 i.i.d. 样本 X^(1), ..., X^(n) ~ N(0, Σ), X^(i) ∈ R^p

目标: 估计 Ω = Σ⁻¹, 并希望 Ω 稀疏 (图结构简单!)

挑战: 当 p > n 时 (变量数超过样本数):
  经验协方差 S = (1/n) X^T X 是奇异的 → 不能直接求逆!

解决方案: ℓ1 正则化 → Graphical Lasso (Friedman et al., 2008)
```

### 2.2 Graphical Lasso 的目标函数 (🔑🔑🔑)

```
负对数似然 (Gaussian) + ℓ1 惩罚:

  min_{Ω≻0}  -log det(Ω) + tr(SΩ) + λ·||Ω||_{1,off}

  Ω≻0: 正定约束 (保证是有效的精度矩阵)
  -log det(Ω): 来自高斯密度的 log 行列式项
  tr(SΩ): 数据拟合项 (S = 经验协方差)
  ||Ω||_{1,off} = Σ_{i≠j} |Ω_ij|: 惩罚非对角线元素 (鼓励稀疏)
  λ: 正则化参数 (越大越稀疏)

直觉:
  -log det(Ω) + tr(SΩ) = - Gaussian log-likelihood (up to const)
  加上 ℓ1: 让大部分 Ω_ij → 0 → 学习简单的图结构
```

### 2.3 坐标下降求解算法 (🔑🔑)

```
核心洞见: 分块坐标下降, 每次更新精度矩阵的一个行/列

将 Ω 和 S 分块:
  Ω = [[W,  w],     S = [[S_11, s_12],
       [w^T, ω]]          [s_21,  s_22]]

  其中 W ∈ R^{(p-1)×(p-1)}, w ∈ R^{p-1}, ω ∈ R (标量)

梯度 = 0 给出:
  min_w  w^T W^{-1} w - 2 s_{12}^T w + λ ||w||₁

这是 Lasso 问题! (响应 = ..., 设计矩阵 = W^{-1} 的 Cholesky 因子)

算法 (Friedman et al., 2008):
  1. 初始化 Ω = (S + λI)^{-1}
  2. 循环 p 个变量 (每次更新一行/列):
     a. 将当前 Ω 分块, 提取 W 和 s_12
     b. 解 Lasso: min_w w^T W^{-1} w - 2 s_{12}^T w + λ ||w||₁
     c. 更新 w 和 ω = 1/(s_22 + λ - w^T W^{-1} w)
  3. 收敛: Ω 变化 < tol
```

### 2.4 λ 的选择

```
λ 控制图的稀疏程度:

  λ → 0:  无惩罚 → Ω ≈ S⁻¹ (如果可逆) → 完全图 (稠密)
  λ → ∞:  最大惩罚 → Ω 对角阵 → 空图 (无任何边!)
  
  λ 的典型选择:
    - Cross-validation: 在验证集上评估log-likelihood
    - BIC: -2·loglik + k·log(n), k = 非零边数
    - StARS (Stability Approach): 找使图"稳定"的 λ

实践中:
  - λ 越大 → 边越少 → 图越稀疏
  - 可以"扫描"λ 值, 观察图的演化路径
```

---

## 概念 3：Neighbor Selection (Meinshausen-Bühlmann) — 逐节点 Lasso

---

### 3.1 核心思想 (🔑🔑🔑)

```
Graphical Lasso: 估计完整的 Ω (p×p 矩阵, 正定约束)
  → 计算量 O(p³), 当 p 很大时太慢!

Neighbor Selection (Meinshausen & Bühlmann, 2006):
  分解为 p 个独立的 Lasso 回归!
  
  对每个节点 j:
    响应 = X_j  (第 j 个变量)
    预测 = X_{-j} = {X_1, ..., X_{j-1}, X_{j+1}, ..., X_p}  (其他所有变量)
    
    解 Lasso: min_β ||X_j - X_{-j}·β||² + λ·||β||₁
    
    邻居估计: N̂(j) = {k : β̂_k ≠ 0}  (非零系数 → 邻居!)

  组合策略:
    AND rule: (i,j) 有边 ⇔ i ∈ N̂(j) AND j ∈ N̂(i)  (更保守)
    OR rule:  (i,j) 有边 ⇔ i ∈ N̂(j) OR  j ∈ N̂(i)  (更灵敏)
```

### 3.2 为什么 Lasso 能选邻居? (🔑🔑)

```
回顾: 对于高斯, X_j 对 X_{-j} 的最优线性回归系数 = -Ω_{j,-j} / Ω_{jj}

即: X_j = Σ_{k≠j} (-Ω_{jk}/Ω_{jj})·X_k + ε_j
           └──── 回归系数 β_k ────┘

所以: β_k ≠ 0 ⇔ Ω_{jk} ≠ 0 ⇔ (j,k) 有边!

Lasso 的 ℓ1 惩罚使得大多数 β_k 被压缩为 0 → 自动选出非零邻居。

理论保证 (Meinshausen & Bühlmann, 2006):
  在适当条件下 (incoherence / irrepresentable condition):
  P(N̂(j) = N(j) for all j) → 1  当 n → ∞

  即: 逐步一致性 (asymptotic consistency)!
```

### 3.3 Graphical Lasso vs Neighbor Selection

```
                         Graphical Lasso        Neighbor Selection
  ┌─────────────────┬─────────────────────┬─────────────────────┐
  │ 目标            │ 精度矩阵 Ω          │ 邻居集 N(j)         │
  │ 方法            │ 全矩阵优化 (正定)   │ p 个独立 Lasso      │
  │ 计算复杂度      │ O(p³)               │ O(p·n²) (可并行!)   │
  │ 输出            │ Ω̂ (精确精度估计)    │ 图结构 (边/无边)    │
  │ 对称性          │ 自动对称            │ 需 AND/OR 规则保证  │
  │ 高维适用性      │ p≈1000              │ p≈10000+ (可并行)   │
  │ 统计效率        │ 高 (同时估计所有)   │ 中 (逐节点, 丢失全局)│
  └─────────────────┴─────────────────────┴─────────────────────┘

经验法则:
  p < 1000: Graphical Lasso (更精确)
  p > 5000: Neighbor Selection (更可行)
  两者在理论上渐近等价, 但有限样本表现不同
```

---

## 概念 4：时变马尔可夫网络 (Time-varying Markov Networks)

---

### 4.1 问题动机 (🔑🔑)

```
静态结构学习假设: 图结构在时间上不变

现实中的反例:
  - 脑连接网络: 静息态 vs 任务态 → 不同!
  - 金融网络: 牛市 vs 熊市 → 关联结构变化!
  - 基因调控网络: 不同发育阶段 → 不同调控关系!

→ 需要估计随时间变化的图序列 G¹, G², ..., G^T
```

### 4.2 时变 Graphical Lasso (🔑🔑🔑)

```
Kolar et al. (2010): Kernel Graphical Lasso

基本思想:
  每个时间点 t 有自己的精度矩阵 Ω^(t)
  但相邻时间点的图结构应该相似!

目标函数:
  min_{Ω^(t)≻0} Σ_t [ -log det(Ω^(t)) + tr(S^(t)Ω^(t)) + λ₁||Ω^(t)||₁ ]
                + λ₂ Σ_t ||Ω^(t) - Ω^(t-1)||₁
                └────── 时间平滑惩罚 ──────┘

S^(t): 时间 t 附近的加权经验协方差 (核平滑):
  S^(t) = Σ_τ w(τ,t)·X^(τ)(X^(τ))^T
  其中 w(τ,t) ∝ K_h(|τ-t|)  (带宽 h 的核函数)

λ₁: 空间稀疏度 (每个图的复杂程度)
λ₂: 时间平滑度 (相邻图可以多"跳跃")
```

### 4.3 几种时变网络模型

```
1. Sliding Window (滑动窗口):
   S^(t) = 在 [t-h, t+h] 窗口内的经验协方差
   简单但边界处理粗糙

2. Kernel Smoothing (核平滑):
   S^(t) = Σ_τ K_h(|τ-t|)·S_τ / Σ_τ K_h(|τ-t|)
   给近时间点更高权重, 远时间点更低权重

3. Fused Lasso (融合Lasso):
   额外添加 Σ_t ||Ω^(t) - Ω^(t-1)||₁
   → 鼓励相邻图的边"一起变化" (结构性断点)

4. State-Space Models:
   图结构本身遵循某种动态过程:
   G^t ~ P(G^t | G^{t-1})  (状态转移)
   X^t ~ P(X^t | G^t)     (观测模型)
```

### 4.4 时变结构的可视化

```
                    t=1    t=2    t=3    t=4    t=5
                    ───    ───    ───    ───    ───
                   1─2    1─2    1 2    1 2    1─2
                    \      |      |      |\     | \
                     3    3 4    3─4    3─4    3─4

变化类型:
  - 边出现 (birth):  (2,3) 在 t=3 出现
  - 边消失 (death):  (1,3) 在 t=3 消失
  - 边持久 (persist): (1,2) 一直存在
  - 断点 (breakpoint): t=3 和 t=4 之间有明显结构变化

关键 λ₂:
  λ₂ 小 → 允许图结构自由变化 → 可能过拟合噪声
  λ₂ 大 → 图结构几乎不变 → 退化为静态 Graphical Lasso
```

---

## 概念 5：其他结构学习方法一览

---

### 5.1 三大范式

```
                    结构学习的三种路径
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   基于约束           基于分数           基于正则化
   (Constraint)       (Score-based)      (Regularization)
        │                  │                  │
  CI 测试            BIC / BDe          ℓ1 惩罚
  PC 算法            Hill-climbing      Graphical Lasso
  Grow-Shrink        Greedy search      Neighbor Selection
        │                  │                  │
        ▼                  ▼                  ▼
  适用于小规模        适用于中等规模      适用于高维
  有向无环图          (DAG搜索 NP-hard)  无向图为主
```

### 5.2 三种方法的对比

| | 基于约束 | 基于分数 | 基于正则化 |
|---|---|---|---|
| **核心思想** | 条件独立性测试 | 最大化 BIC 等分数 | 凸优化 + ℓ1 惩罚 |
| **代表算法** | PC, Grow-Shrink | Hill-climbing, GES | Graphical Lasso |
| **输出** | 等价类 (CPDAG) | 最优 DAG | 精度矩阵 / 无向图 |
| **计算** | O(p^k·n) 指数级 | NP-hard (启发式) | O(p³) 多项式! |
| **统计保证** | 渐近一致 | BIC 一致 | Oracle 性质 |
| **高维适用性** | 差 (CI 测试不可靠) | 中 | 好 (ℓ1 天然高维) |

---

## 📋 演化路径一张表

| 模型 | 数据假设 | 图类型 | 方法 | 复杂度 | 适用 p |
|------|---------|--------|------|--------|--------|
| **GGM + MLE** | 高斯 | 无向, 稠密 | Σ 求逆 | O(p³) | < n |
| **Graphical Lasso** | 高斯 | 无向, 稀疏 | ℓ1 正则化 | O(p³) | >> n |
| **Neighbor Selection** | 高斯 | 无向, 稀疏 | p 个并行 Lasso | O(p·n²) | >> n |
| **Time-varying GL** | 高斯, 时序 | 无向, 稀疏+平滑 | 核平滑 + ℓ1 | O(T·p³) | >> n |
| **PC Algorithm** | 任意 | DAG, 稀疏 | CI 测试 | O(p^k) | < n |
| **Hill-climbing** | 任意 (离散) | DAG | 启发式搜索 | NP-hard | < n |

---

## 🔗 概念关系图

```
                    从数据中学习图结构 (L16)
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   X ~ N(0, Σ)        非高斯/离散          时序数据
        │              (L5, L12-13)            │
        ▼                                     ▼
  精度矩阵 Ω = Σ⁻¹                     时变 Graphical Lasso
   Ω_ij = 0 ⇔ 无边                      Ω^(t) 随时间变化
        │                                     │
   ┌────┴────┐                          λ₂ 时间平滑
   ▼         ▼
Graphical    Neighbor
Lasso       Selection
(全矩阵)    (逐节点)

核心洞见: 图结构 = 统计依赖 = 精度矩阵的非零模式
         结构学习 = 在高维空间中估计稀疏精度矩阵
```

---

## 🎯 核心洞见一句话

| # | 洞见 |
|---|------|
| 1 | **精度矩阵即图** — 对于高斯分布, Ω_ij = 0 ⇔ X_i ⟂ X_j \| rest, 图 = Ω 的非零模式 |
| 2 | **Σ vs Ω** — Σ 是"全部关联"(含间接), Ω 是"净关联"(扣除中介), 后者定义图 |
| 3 | **ℓ1 惩罚是结构学习的引擎** — log-likelihood + λ\|\|Ω\|\|₁ → 自动发现稀疏图 |
| 4 | **Graphical Lasso = 大 Lasso** — 目标函数是 -log det(Ω) + tr(SΩ) + λ\|\|Ω\|\|₁, 用分块坐标下降 |
| 5 | **Neighbor Selection = p 个小 Lasso** — 逐节点回归, 系数非零 = 邻居, 可并行化 |
| 6 | **λ 是"复杂度旋钮"** — λ=0 → 完全图, λ=∞ → 空图, 中间: 数据驱动的稀疏结构 |
| 7 | **时变 = 空间稀疏 + 时间平滑** — 两个惩罚项 λ₁\|\|Ω^(t)\|\|₁ + λ₂\|\|Ω^(t)-Ω^(t-1)\|\|₁ |
| 8 | **高维可行性的代价** — ℓ1 正则化让 p>>n 的结构学习成为可能, 但需要 incoherence 条件 |

---

## 🧪 自测清单

- [ ] 对于高斯分布: Ω_ij = 0 等价于什么条件独立性? 为什么?
- [ ] Σ_ij = 0 和 Ω_ij = 0 的区别? 哪个定义了图结构?
- [ ] Graphical Lasso 的目标函数三项各自含义? λ 过大/过小会怎样?
- [ ] 为什么 p >> n 时不能直接用 S⁻¹ 估计 Ω?
- [ ] Neighbor Selection 如何通过 p 个 Lasso 回归找到图结构? AND rule vs OR rule?
- [ ] Graphical Lasso 和 Neighbor Selection 在什么场景下选哪个?
- [ ] 时变 Graphical Lasso 如何引入时间平滑? λ₂ 的作用?
- [ ] Kernel Smoothing 和 Fused Lasso 在时间维度上的区别?
- [ ] 结构学习三大范式 (约束/分数/正则化) 各适合什么场景?
- [ ] ℓ1 正则化为什么能产生稀疏解? (从优化的几何直觉解释)

---

> L16 完成了 PGM 体系的核心闭环:
> 表示 (L1-L3) → 推断 (L4, L7-L10) → 参数学习 (L5) → 结构学习 (L16)
>
> 结构学习回答了"图从哪来"这个根本问题 — 从数据中自动发现变量间的依赖结构。
> 对于高斯数据, 答案优雅简洁: 图 = 精度矩阵的非零模式, 用 ℓ1 正则化在高维中估计。
> Graphical Lasso 及其变体是现代高维统计和网络科学的基石工具。
