# CMU 10-708 Lecture 5 课后练习 & 答案

> 配套教材: Koller & Friedman Ch.10-11 课后习题精选
>
> 题目分为三级: 🟢 基础 | 🟡 进阶 | 🔴 挑战

---

## 🟢 基础题 (必须掌握)

### Q1. 消息手算 (链式图)

对以下链式图 X₁ → X₂ → X₃，给定参数：

```
P(X₁):
  X₁=0: 0.4
  X₁=1: 0.6

P(X₂|X₁):
          X₁=0  X₁=1
  X₂=0    0.8   0.3
  X₂=1    0.2   0.7

P(X₃|X₂):
          X₂=0  X₂=1
  X₃=0    0.9   0.4
  X₃=1    0.1   0.6
```

**(A)** 写出从 X₁ 到 X₂ 的消息 m₁→₂(X₂) 的公式并计算结果。

**(B)** 写出从 X₃ 到 X₂ 的消息 m₃→₂(X₂) 的公式并计算结果。

**(C)** 用消息结果计算 P(X₂) 的边际概率。

<details>
<summary>点击查看答案</summary>

**(A) m₁→₂(X₂)**

```
m₁→₂(X₂) = Σ_{X₁} P(X₁) × P(X₂|X₁)

m₁→₂(X₂=0) = 0.4 × 0.8 + 0.6 × 0.3 = 0.32 + 0.18 = 0.50
m₁→₂(X₂=1) = 0.4 × 0.2 + 0.6 × 0.7 = 0.08 + 0.42 = 0.50

∴ m₁→₂ = [0.50, 0.50]
```

**(B) m₃→₂(X₂)**

```
m₃→₂(X₂) = Σ_{X₃} P(X₃|X₂) × m₄→₃(X₃)

但 X₃ 是叶子节点，没有其他邻居，所以:
  "来自其他邻居的消息乘积" = 1 (空乘积)

m₃→₂(X₂=0) = Σ_{X₃} P(X₃|X₂=0) × 1 = 0.9 + 0.1 = 1.0
m₃→₂(X₂=1) = Σ_{X₃} P(X₃|X₂=1) × 1 = 0.4 + 0.6 = 1.0

∴ m₃→₂ = [1.0, 1.0]
```

> 叶子消息恒为 1 向量！因为条件分布对子节点求和正好是 1。

**(C) P(X₂)**

```
P(X₂) ∝ m₁→₂(X₂) × m₃→₂(X₂)

P(X₂=0) ∝ 0.50 × 1.0 = 0.50
P(X₂=1) ∝ 0.50 × 1.0 = 0.50

归一化 (恰好已归一): P(X₂) = [0.50, 0.50]
```

验证: 暴力枚举 —
```
P(X₂=0) = Σ_{X₁,X₃} P(X₁)P(X₂=0|X₁)P(X₃|X₂=0)
        = 0.4×0.8×(0.9+0.1) + 0.6×0.3×(0.9+0.1)
        = 0.4×0.8 + 0.6×0.3 = 0.32 + 0.18 = 0.50 ✓
```

</details>

---

### Q2. 收集与分发阶段

对下面的树状图：

```
        A
       / \
      B   C
     /     \
    D       E
```

以 A 为根节点。

**(A)** 写出收集阶段 (Collect) 的消息传递顺序（后序遍历）。

**(B)** 写出分发阶段 (Distribute) 的消息传递顺序（前序遍历）。

**(C)** 收集阶段完成后，哪些节点的边际概率已经可以计算了？为什么？

<details>
<summary>点击查看答案</summary>

**(A) 收集阶段顺序:**

从叶子到根的后序遍历: D → B → E → C → A

具体消息:
1. m_D→B(X_B) — 叶子 D 发给 B
2. m_B→A(X_A) — B 收集了 D 的消息后发给 A
3. m_E→C(X_C) — 叶子 E 发给 C
4. m_C→A(X_A) — C 收集了 E 的消息后发给 A

**(B) 分发阶段顺序:**

从根到叶子的前序遍历: A → B, A → C → D, E (B,C 可并行)

具体消息:
1. m_A→B(X_B) — A 发给 B
2. m_A→C(X_C) — A 发给 C
3. m_B→D(X_D) — B 转发给 D
4. m_C→E(X_E) — C 转发给 E

**(C) 收集完成后:**

只有根节点 A 的边际可以计算!

```
P(A) ∝ m_B→A(X_A) × m_C→A(X_A)
```

其他节点（如 B）还缺来自 A 方向的消息 (m_A→B)，所以边际不完整。

> 这就是为什么需要分发阶段 — 把根节点汇总的全局信息再"分发"回各个子树。

</details>

---

### Q3. 消息公式中 N(i)\\{j} 的含义

BP 的核心消息公式为：

```
m_{i→j}(Xⱼ) = Σ_{Xᵢ} [ ψᵢⱼ(Xᵢ, Xⱼ) × ∏_{k ∈ N(i)\{j}} m_{k→i}(Xᵢ) ]
```

**(A)** 解释 N(i)\\{j} 的含义。为什么要排除接收方 j？

**(B)** 如果不排除 j（即包含 m_{j→i}），会发生什么？

<details>
<summary>点击查看答案</summary>

**(A) N(i)\\{j} 的含义:**

N(i) 是节点 i 在图中的所有邻居。
N(i)\\{j} 是"除 j 之外的所有邻居"。

排除 j 的原因: **防止消息循环**。

节点 i 发给 j 的消息 = "汇总除了 j 那边以外的所有信息，告诉 j 关于 Xⱼ 的事"。

如果包含 m_{j→i}（j 之前发给 i 的消息），那么:
- i 发给 j 的消息里包含了 j 发给 i 的信息
- j 收到后再发给 i，又包含了 i 发给 j 的信息
- 形成正反馈循环 → 信息被重复计算

**(B) 不排除 j 的后果:**

在树上可能导致边际翻倍（每条消息被算两次）。
在有环图上会更严重 — 消息绕环越传越大，发散而非收敛。

> **直觉**: "我告诉你的事"不应该包含"你刚才告诉我的事"。排除 j 确保每条边上的两个方向的消息是**互补**的 — 分别汇总了树的两个不同"半边"的信息。

</details>

---

### Q4. BP vs VE — 多查询场景

一个树状网络有 50 个节点，每个变量取 3 个值。

**(A)** 用 VE 计算所有 50 个节点的边际概率，大约需要多少次变量消除操作？

**(B)** 用 BP 计算所有 50 个节点的边际概率，大约需要多少次消息传递操作？

**(C)** 在这个例子中 BP 比 VE 节省了多少计算？（用 O 记号估算比例）

<details>
<summary>点击查看答案</summary>

**(A) VE:**

每次对单个节点查询，需要消除 49 个变量（其余所有节点）。
总消除操作: 50 次查询 × 49 次消除 = **2450 次消除**。

每次消除 O(k²) 到 O(k^w)（w 为树宽度=1，所以 O(k²) = O(9)）。

总复杂度: **O(50 × 49 × 9) = O(22050)**

**(B) BP:**

- 树有 49 条边（50 个节点的树），每条边两个方向 → 98 条消息。
- 收集阶段: 49 条消息 (从叶子到根)
- 分发阶段: 49 条消息 (从根到叶子)
- 总共: **98 次消息计算**

每条消息: O(k²) = O(9)

总复杂度: **O(98 × 9) = O(882)**

**(C) 节省比例:**

```
VE/BP ≈ 2450 / 98 ≈ 25 倍
```

更一般地，对 n 个节点的树:
- VE: O(n² · k²)  — 因为对每个节点的查询都需要重新消除
- BP: O(n · k²)   — 每条边两个方向各一条消息

**比例 ≈ n 倍**。在 50 节点时 BP 快 25 倍，在 500 节点时快 250 倍！

</details>

---

## 🟡 进阶题

### Q5. 手动构建团树 (Junction Tree)

考虑以下 DAG（v-structure / collider）：

```
    A   B
     \ /
      C
      |
      D
```

即 A → C ← B → D（注意 B 同时指向 C 和 D）。

**(A)** 画出这个 DAG 的**端正图** (moral graph)。

**(B)** 对该端正图做三角剖分 (triangulation)，添加必要的填充边。

**(C)** 找出所有极大团 (maximal cliques) 并构建团树。

<details>
<summary>点击查看答案</summary>

**(A) 端正图:**

Moralization 步骤:
1. 连接每个节点的所有父节点 (v-structure 处理): A 和 B 都是 C 的父节点 → 加边 A—B
2. 把所有有向边变成无向边: A—C, B—C, B—D

端正图:
```
    A —— B
     \   / \
      C     D
```
或重新排列:
```
  A —— B —— D
    \  /
     C
```

**(B) 三角剖分:**

检查环: 图中有环 A—B—C—A（三角形，已经三角剖分）。

没有其他长度 ≥ 4 的无弦环。不需要添加填充边。

所以端正图本身已经三角剖分了。

**(C) 极大团和团树:**

极大团: {A, B, C} 和 {B, D}

交集: {B} ← 这就是分离集 (sepset)

团树:
```
  ┌─────────┐       ┌───────┐
  │ {A,B,C} │──{B}──│ {B,D} │
  └─────────┘       └───────┘
```

验证运行交集性质: B 同时出现在两个团中，且出现在连接它们的路径上 ✓

**注意**: 原始 DAG 不是树，但团树是一棵树。这就是 junction tree 的力量 — 把非树原图编译成树状团图。

</details>

---

### Q6. Loopy BP 的收敛性

对下面这个简单的 4-循环 MRF：

```
  A —— B
  |    |
  D —— C
```

所有边上的势函数都是:
```
ψ(Xᵢ, Xⱼ) = [[2, 1],
              [1, 2]]
```
（即"同值"配对的权重大于"异值"配对）

**(A)** 在这个图上跑 Loopy BP 会收敛吗？如果收敛，消息会是什么？

**(B)** 如果把势函数改成:
```
ψ(Xᵢ, Xⱼ) = [[1, 10],
              [10, 1]]
```
（即"异值"配对权重更大 — 类似反铁磁 Ising 模型），Loopy BP 会收敛吗？

<details>
<summary>点击查看答案</summary>

**(A) "同值"势函数 — 会收敛**

初始消息全为 [1, 1]。

第一轮迭代:
- 每条边上的消息都会因为"同值"权重而偏向同方向。
- 但由于对称性，所有消息最终收敛到 [1, 1]（均匀消息）。

实际上因为模型完全对称且"同值"势函数是吸引的 (attractive)，所有节点边际都是 [0.5, 0.5]。

**结论**: 收敛 ✓，收敛到均匀分布。

**(B) "异值"势函数 — 可能不收敛（震荡）**

这是一个"受挫"(frustrated) 系统:

如果 A=0，那么边 A—B 的 ψ 倾向于 B=1。
但边 B—C 的 ψ 又倾向于 C=0。
边 C—D 倾向于 D=1。
边 D—A 倾向于 A=1 — 但 A=0!

消息在每一轮迭代中交替变化:
- 第 1 轮: m₁→ⱼ 偏向某个方向
- 第 2 轮: 收到的消息变了，新消息偏反方向
- 第 3 轮: 又变回来...

在许多这类反铁磁 (antiferromagnetic) 模型上，Loopy BP **不收敛**，表现为:
- 消息值在两套值之间反复震荡
- 边际估计可能也是震荡的

> **实践中的教训**: Loopy BP 的收敛性取决于势函数的"温度"和图的拓扑。吸引性 (attractive / ferromagnetic) 模型通常收敛好，排斥性 (repulsive / antiferromagnetic) 模型可能不收敛。

</details>

---

### Q7. Max-Product 的回溯 (Traceback)

对链 X₁ → X₂ → X₃，参数同 Q1。

**(A)** 用 Max-Product 求 MAP 赋值 argmax_{X₁,X₂,X₃} P(X₁, X₂, X₃)。

**(B)** 对比 MAP 赋值和各个节点的边际最大值。它们一样吗？

<details>
<summary>点击查看答案</summary>

**(A) Max-Product 计算:**

**Step 1: 消息 m₁→₂ (max 版本)**

```
m₁→₂(X₂) = max_{X₁} P(X₁) × P(X₂|X₁)

X₂=0: max(0.4×0.8, 0.6×0.3) = max(0.32, 0.18) = 0.32  argmax X₁=0
X₂=1: max(0.4×0.2, 0.6×0.7) = max(0.08, 0.42) = 0.42  argmax X₁=1

m₁→₂ = [0.32, 0.42]
```

**Step 2: 消息 m₃→₂ (max 版本)**

```
m₃→₂(X₂) = max_{X₃} P(X₃|X₂)

X₂=0: max(0.9, 0.1) = 0.9  argmax X₃=0
X₂=1: max(0.4, 0.6) = 0.6  argmax X₃=1

m₃→₂ = [0.9, 0.6]
```

**Step 3: 在 X₂ 处求 max**

```
score(X₂) = m₁→₂(X₂) × m₃→₂(X₂)

X₂=0: 0.32 × 0.9 = 0.288
X₂=1: 0.42 × 0.6 = 0.252

max 在 X₂=0, score = 0.288
```

**Step 4: 回溯 argmax**

```
X₂* = 0
  → 从 m₁→₂(0): argmax X₁=0,  ∴ X₁* = 0
  → 从 m₃→₂(0): argmax X₃=0,  ∴ X₃* = 0
```

**MAP 赋值: (X₁=0, X₂=0, X₃=0)**

验证联合概率: P(X₁=0, X₂=0, X₃=0) = 0.4 × 0.8 × 0.9 = 0.288 ✓ 确实最大!

**(B) 对比边际最大值 vs MAP:**

从 Q1 计算边际:
- P(X₁) = [0.4, 0.6] → 边际最大: X₁=1
- P(X₂) = [0.5, 0.5] → 边际最大: 任意（都是 0.5）
- P(X₃) = (需要算 P(X₃)=Σ P(X₁,X₂,X₃) = [0.65, 0.35]) → 边际最大: X₃=0

MAP: (X₁=0, X₂=0, X₃=0)

**X₁ 的边际最大是 1，但 MAP 中是 0！**

> 这就是"边际最优 ≠ 全局最优"的经典例子。X₁=1 的边际概率更高是"平均来看"，但当结合所有变量一起看时，(0,0,0) 这个组合的整体概率最高。

</details>

---

## 🔴 挑战题

### Q8. 推导 BP 消息与 VE 中间结果的关系

考虑一个树状 DAG G = (V, E)。对某个节点 r 选定为根，执行收集+分发两阶段 BP。

**(A)** 证明: 收集阶段中每条边 (i→j) 上的消息 m_{i→j} 等于在"以 i 为根的子树"上执行 VE（消除该子树中除 j 外所有变量）后得到的中间因子 τ。

**(B)** 由此说明: BP 的收集阶段 = 对每个子树独立做 VE，分发阶段 = 把全局归一化信息反向传播。

<details>
<summary>点击查看答案</summary>

**(A) 归纳证明:**

**基例**: 叶子节点 ℓ（在收集阶段，ℓ 没有子节点）。

VE 在子树 {ℓ} 上消除 ℓ:
- 涉及因子: P(ℓ|parent)（或 P(ℓ) 如果 ℓ 是根）
- 消去 ℓ 后: τ(parent) = Σ_ℓ P(ℓ|parent) = [1, 1, ..., 1]

BP 消息: m_{ℓ→parent}(parent) = Σ_ℓ ψ(parent, ℓ) × 1（没有入边消息）
= Σ_ℓ P(ℓ|parent) = [1, ..., 1] ✓

**归纳步**: 假设对所有真子节点 k ∈ children(i)（k ≠ j），消息 m_{k→i} 等于消除 k 的子树后的 τ_k(i)。

那么 BP 消息:
```
m_{i→j}(Xⱼ) = Σ_{Xᵢ} [ψ(Xᵢ, Xⱼ) × ∏_{k ∈ N(i)\{j}} m_{k→i}(Xᵢ)]
            = Σ_{Xᵢ} [P(Xⱼ|Xᵢ) × ∏_{k} τ_k(Xᵢ)]
```

这正是 VE 中消除节点 i（及其所有子节点子树）的操作:
1. 收集所有含 Xᵢ 的因子: P(Xⱼ|Xᵢ) 和所有 τ_k(Xᵢ)
2. 对 Xᵢ 求和
3. 结果: τ_i(Xⱼ) ✓

**(B) 全局视角:**

```
收集 = 从叶子到根的方向做 VE
      每条消息 = 一个子树被消除后的"信息压缩包"

分发 = 把根节点汇总的全局信息（通过 VE 消除所有非根变量后）
      反向传给各子树，让它们也能算出完整边际

完整 BP = 对每条边双向都有"压缩包"
       → 任何节点的边际 = 相邻压缩包的乘积
```

这就是为什么 BP 是"动态规划" — 子树结果被缓存 (memoized) 为消息，后续直接复用。

</details>

---

### Q9. 为什么运行交集性质至关重要？

考虑以下 BAD 团树：

```
  ┌───────────┐       ┌───────────┐
  │ {A, B, C} │──{C}──│ {C, D, E} │
  └───────────┘       └───────────┘
        │
      {A, B}
        │
  ┌───────────┐
  │ {A, E, F} │
  └───────────┘
```

**(A)** 指出这个"团树"违反了什么性质（提示: 检查"运行交集"）。

**(B)** 解释违反该性质为什么会导致 BP 结果不一致（给出具体的数值例子）。

<details>
<summary>点击查看答案</summary>

**(A) 违反运行交集性质:**

变量 A 同时出现在团 {A, B, C}、{A, B} 和 {A, E, F} 中。

连接 {A, B, C} 和 {A, E, F} 的路径经过 {A, B}。

运行交集性质要求: A 必须在连接 C₁={A,B,C} 和 C₃={A,E,F} 的路径上的**每一个**团中都出现。

检查路径: {A,B,C} → {A,B} → {A,E,F}
- {A,B} 中有 A ✓
- 但是... 另一个方向呢？

实际上还有另一条路径: {A,E,F} 中的 A 可以通过不同的边到达 {A,B,C}。

问题在于: A 在 {C, D, E} 中**不**出现！但如果我们想让 A 穿过这个团树的不同分支，就会出问题。

更精确的违规: 这个图不是一棵树！它有一个环: {A,B,C} — {A,B} — {A,E,F} — ... 没有形成环。

但实际上它确实形成了非树结构。让我重新分析:

这个图中有:
- 团节点 1: {A, B, C}, 连接 团节点 2: {C, D, E}   (交集: {C})
- 团节点 1: {A, B, C}, 连接 团节点 3: {A, B}       (交集: {A, B})
- 团节点 3: {A, B},   连接 团节点 4: {A, E, F}     (交集: {A})

问题:
1. 这不是树! 团节点 1 有两个子节点 (2 和 3)，团节点 3 又有子节点 4 — 这本身可以是树。
2. 但更严重的是: 变量 A 出现在 1, 3, 4 中但**不在** 2 中。
   如果有一条消息从 4 到达 1 (关于 A 的信息)，然后 1 要把关于 A 的信息传给 2...
   等等，团 2 的 scope 是 {C,D,E}，不包含 A，所以 A 相关的信息确实不需要经过 2。

让我重新考虑 — 这个团树可能实际上**是**有效的（满足运行交集）。

团 1 {A,B,C} 和团 4 {A,E,F}: 它们之间的路径是 1→3→4:
- 团 1 中有 A? ✓
- 团 3 中有 A? ✓
- 团 4 中有 A? ✓

团 2 {C,D,E} 和团 4 {A,E,F}: 路径是 2→1→3→4:
- 团 2 中有 E? ✓
- 团 1 中有 E? ✗ ← **违反!**

E 出现在团 2 和团 4 中，但连接的路径上的团 1 {A,B,C} 和团 3 {A,B} **都不包含 E**！

这就是运行交集的违反: **E 在两个不连续的团中出现**。

**(B) 不一致的后果:**

假设每个变量有 2 个值。

消息从团 2 传到团 1:
- 团 2 ({C,D,E}) 关于 E 的信念被传递给团 1 ({A,B,C})
- 但团 1 的 scope 是 {A,B,C} — **不包含 E**！
- 在边缘化 E 的过程中，关于 E 的信息会丢失/扭曲

消息从团 4 传到团 3:
- 团 4 ({A,E,F}) 关于 E 的信念: 边际 P(E) = [0.3, 0.7]
- 团 3 ({A,B}) 的 scope 也不含 E，所以这个信息也会被边缘化丢失

最终，如果从两个不同方向查询 P(E):
- 从团 2 方向: P(E) = [0.6, 0.4]
- 从团 4 方向: P(E) = [0.3, 0.7]

**不一致！**

运行交集性质保证: 关于变量 X 的信息沿着所有包含 X 的团的**连通路径**传播，没有被"截断"或"丢失"。

> 这就是 junction tree 算法要求"运行交集"的根本原因 — 它保证了校准 (calibration) 后所有团在交集上的边际一致，从而保证了全局一致性。

</details>

---

### Q10. Loopy BP 什么时候效果好？（研究性问题）

Loopy BP 在实践中经常效果很好，虽然理论上没有保证。从信息传播的角度，解释什么时候 Loopy BP 倾向于给出好的近似，什么时候倾向于失败。

<details>
<summary>点击查看答案</summary>

**Loopy BP 效果好的条件:**

1. **环很长 (long loops)**: 消息绕环一圈的"回声"衰减显著（衰减因子 < 1），多次迭代后接近 0。等价于图上几乎"局部树状"。

2. **弱耦合 (weak couplings)**: 边上的势函数接近均匀 (即变量之间关系弱)。极端情况: 如果所有边因子都是均匀分布，BP 在第一轮就精确收敛。

3. **吸引性势函数 (attractive/ferromagnetic potentials)**: 所有边倾向于变量同值。数学上这对应势函数是"关联的"(associative)，此时 BP 有收敛保证且近似质量有理论上界。

4. **单峰后验 (unimodal posterior)**: 后验分布只有一个显著的模式 (mode)。如果分布是多峰的，BP 的边际可能是各模式的"平均"，不代表任何真实配置。

**Loopy BP 失败的情况:**

1. **短环 + 强耦合**: 消息绕环不断自我放大，震荡或发散。

2. **排斥性势函数 (repulsive/antiferromagnetic)**: 类似反铁磁 Ising 模型，BP 经常震荡不收敛。

3. **多峰后验**: BP 计算的边际是各峰之间的"妥协"，可能不代表任何真实的高概率配置。例如:
   - 后验有两个峰: (0,0,0) 和 (1,1,1)，概率各 0.5
   - BP 边际: P(X₁=0) = 0.5, P(X₂=0) = 0.5
   - 但 MAP 赋值要么是 (0,0,0) 要么是 (1,1,1)
   - BP 边际暗示 (0,1) 也是合理的 — 但它的概率其实接近 0！

4. **临界温度附近**: 系统接近相变点时，关联长度发散，环上的消息衰减因子 → 1，BP 需要的迭代次数 → ∞。

**实践启发**:
- 图像去噪 (Ising 模型，吸引性) → BP 效果好 ✓
- Turbo/LDPC 码译码 (长环，弱耦合) → BP 效果极好 ✓✓✓
- 随机 satisfiability 问题 (多峰) → BP 需要 Survey Propagation 等扩展 ✗

</details>

---

## 📊 综合自测评分

每题 10 分，共 100 分。

| 得分 | 评价 |
|------|------|
| 90-100 | L5 完全掌握，消息传递的本质已了然于心 |
| 70-89  | 主干理解扎实，建议再手推一两个消息传递练习 |
| 50-69  | 概念框架有了，回去重看 L5 的消息公式推导部分 |
| < 50   | 先吃透 Q1-Q3，彻底弄懂"消息是什么"再往下走 |

---

> L5 是 PGM 推断篇的核心。掌握 BP 后，L6-L9 进入**学习 (Learning)** 篇章 — 参数和结构如何从数据中习得。
