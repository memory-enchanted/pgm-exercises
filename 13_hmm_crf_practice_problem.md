# CMU 10-708 Lecture 6 课后练习 & 答案

> 配套教材: Koller & Friedman Ch.19-20 + Rabiner (1989) 课后习题精选
>
> 题目分为三级: 🟢 基础 | 🟡 进阶 | 🔴 挑战

---

## 🟢 基础题 (必须掌握)

### Q1. HMM 三要素识别

给定一个 HMM，隐状态 K=2（Rainy=0, Sunny=1），观测 |X|=3（Walk=0, Shop=1, Clean=2）。

参数如下：
```
π = P(Z₁): [0.6, 0.4]

A = P(Z_t | Z_{t-1}), 行=Z_t, 列=Z_{t-1}:
    [[0.7, 0.4],
     [0.3, 0.6]]

B = P(X_t | Z_t), 行=X_t, 列=Z_t:
    [[0.1, 0.6],   # Walk
     [0.4, 0.3],   # Shop
     [0.5, 0.1]]   # Clean
```

**(A)** 写出该 HMM 的图结构（节点和边的拓扑）。

**(B)** 写出联合分布 P(Z₁, Z₂, Z₃, X₁, X₂, X₃) 的因子分解形式。

**(C)** 解释为什么 HMM 的 DAG 中不存在 collider (V-structure)。

<details>
<summary>点击查看答案</summary>

**(A) 图结构:**

```
    Z₁ → Z₂ → Z₃
    ↓     ↓     ↓
    X₁    X₂    X₃
```

- 3 个隐状态节点 Z₁, Z₂, Z₃
- 3 个观测节点 X₁, X₂, X₃
- Z 链: Z₁→Z₂, Z₂→Z₃ (一阶马尔可夫链)
- 发射边: Z_t→X_t (每个观测只依赖当前状态)

**(B) 因子分解:**

```
P(Z₁, Z₂, Z₃, X₁, X₂, X₃)
= P(Z₁) × P(Z₂|Z₁) × P(Z₃|Z₂) × P(X₁|Z₁) × P(X₂|Z₂) × P(X₃|Z₃)
= π_{Z₁} × A_{Z₂,Z₁} × A_{Z₃,Z₂} × B_{X₁,Z₁} × B_{X₂,Z₂} × B_{X₃,Z₃}
```

**(C) 为什么没有 collider:**

HMM 的 DAG 中所有边要么是水平方向 Z_t→Z_{t+1}，要么是垂直方向 Z_t→X_t。任何三个相邻节点的结构只可能是:
- 链式: Z_{t-1} → Z_t → Z_{t+1}（或类似变体）
- 分叉: Z_{t-1} ← Z_t → Z_{t+1}（或 Z_t → X_t 且 Z_t → Z_{t+1}, 即 Z_t 指向两个方向）

不存在两个箭头**同时指向**同一个节点的情况 → 没有 V-structure → 没有 collider。

> 这意味着 HMM 的端正图 (moral graph) 不需要添加任何填充边！HMM 是无弦的 (chordal)，这也是为什么 Forward/Backward 可以在上面精确推断的原因。

</details>

---

### Q2. Forward 算法手算

使用 Q1 的 HMM 参数。观测序列: X₁=Walk, X₂=Shop, X₃=Clean。

**(A)** 写出 Forward 算法中 α₁ 的计算公式和结果。

**(B)** 写出 α₂(Rainy) 的完整计算过程。

**(C)** 计算 P(X₁, X₂, X₃) = Σ_j α₃(j)。

<details>
<summary>点击查看答案</summary>

**(A) α₁ 初始化和计算:**

```
α₁(j) = π_j × B_j(X₁)

α₁(Rainy) = 0.6 × B(Walk|Rainy) = 0.6 × 0.1 = 0.06
α₁(Sunny) = 0.4 × B(Walk|Sunny) = 0.4 × 0.6 = 0.24

α₁ = [0.06, 0.24]
```

**(B) α₂(Rainy) 的完整计算:**

Forward 递推公式:
```
α_t(j) = [Σ_i α_{t-1}(i) × A_{j,i}] × B_j(X_t)
```

对于 α₂(Rainy):
```
α₁(Rainy) × A(Rainy→Rainy) = 0.06 × 0.7 = 0.042
α₁(Sunny) × A(Sunny→Rainy) = 0.24 × 0.4 = 0.096
Σ = 0.042 + 0.096 = 0.138

α₂(Rainy) = 0.138 × B(Shop|Rainy) = 0.138 × 0.4 = 0.0552
```

同理 α₂(Sunny):
```
α₁(Rainy) × 0.3 + α₁(Sunny) × 0.6 = 0.018 + 0.144 = 0.162
α₂(Sunny) = 0.162 × 0.3 = 0.0486
```

α₂ = [0.0552, 0.0486]

**(C) P(X₁, X₂, X₃) — 继续算 α₃:**

```
α₃(Rainy) = [α₂(Rainy)×0.7 + α₂(Sunny)×0.4] × B(Clean|Rainy)
          = [0.03864 + 0.01944] × 0.5 = 0.05808 × 0.5 = 0.02904

α₃(Sunny) = [α₂(Rainy)×0.3 + α₂(Sunny)×0.6] × B(Clean|Sunny)
          = [0.01656 + 0.02916] × 0.1 = 0.04572 × 0.1 = 0.004572

P(X₁, X₂, X₃) = α₃(Rainy) + α₃(Sunny)
              = 0.02904 + 0.004572 = 0.033612
```

验证: P(X₁, X₂, X₃) ≈ 0.0336，即观测序列 Walk→Shop→Clean 的似然约 3.36%。

</details>

---

### Q3. 从 Forward α 到 L4 VE 的对应

**(A)** 在 Forward 算法中，α₂(Z₂) 对应 VE 中的什么中间结果？

**(B)** 写出 Forward 算法的消除顺序，以及每一步被消去的变量。

**(C)** 为什么 Forward 算法的复杂度是 O(T × K²) 而不是 O(K^T)？

<details>
<summary>点击查看答案</summary>

**(A) α₂(Z₂) 对应 VE 中间结果:**

```
α₂(Z₂) = τ(Z₂) ← 消去 Z₁ 后的中间因子

VE 步骤:
1. 初始因子: P(Z₁), P(Z₂|Z₁), P(Z₃|Z₂), P(X₁|Z₁), P(X₂|Z₂), P(X₃|Z₃)
2. 固定证据: 把 P(X₁|Z₁) 替换为 P(X₁=Walk|Z₁)
3. 消去 Z₁: τ(Z₂) = Σ_{Z₁} P(Z₁) × P(Z₂|Z₁) × P(X₁=Walk|Z₁)

这正是 α₂(Z₂)！只是少了 × P(X₂|Z₂) 这一步（因消元顺序不同，可以含也可不含 X₂）。
```

更准确地说: Forward 算法的 α_t(Z_t) = "消去 Z₁,...,Z_{t-1} 且包含 X₁,...,X_t 证据后，scope 为 {Z_t} 的中间因子"。

**(B) 消除顺序:**

```
π = [Z₁, Z₂, Z₃]  (从左到右)

Step 1 (消 Z₁): 涉及因子 {P(Z₁), P(Z₂|Z₁), P(X₁|Z₁)}
                → τ₁(Z₂), scope = {Z₂}

Step 2 (消 Z₂): 涉及因子 {τ₁(Z₂), P(Z₃|Z₂), P(X₂|Z₂)}
                → τ₂(Z₃), scope = {Z₃}

Step 3: 只剩 τ₂(Z₃) 和 P(X₃|Z₃)，乘积后求和得 P(X₁,X₂,X₃)
```

**(C) 为什么是 O(T × K²) 而非 O(K^T):**

因为 HMM 是一阶马尔可夫链——每步消元只需要考虑**相邻两个时刻**之间的转移。消去 Z_t 时:
- 中间因子的 scope 只有 {Z_t, Z_{t+1}} (大小 ≤ 2)
- 计算量是 K×K = K² per step
- T 步 → O(T × K²)

如果没有马尔可夫结构（每个 Z_t 依赖所有前面的 Z），那 scope 会膨胀到包含所有未消变量（最多 T 个），复杂度确实会变成 O(K^T)。

> VE 的复杂度 = O(N × k^{w+1})，其中 w 是 treewidth。HMM 的 treewidth = 1 → 复杂度 O(T × k²)。

</details>

---

### Q4. Backward 算法

使用 Q2 中已算出的 α₂ = [0.0552, 0.0486]，计算 Backward 消息 β₂。

**(A)** 写出 β 消息的递推公式和 β₃ 的初始值。

**(B)** 计算 β₂(Rainy) 和 β₂(Sunny)。

**(C)** 用 α₂ × β₂ 计算平滑边际 P(Z₂ | X₁=Walk, X₂=Shop, X₃=Clean)，并验证是否归一化。

<details>
<summary>点击查看答案</summary>

**(A) β 递推公式和初始值:**

```
β_T(j) = 1  (对所有 j，叶子节点无条件信息)

β_t(i) = Σ_j A_{j,i} × B_j(X_{t+1}) × β_{t+1}(j)
```

T=3，所以 β₃ = [1, 1]。

**(B) β₂ 计算:**

```
β₂(Rainy) = Σ_j A_{j,Rainy} × B_j(X₃) × β₃(j)

= A(Rainy→Rainy) × B(Clean|Rainy) × 1
  + A(Sunny→Rainy) × B(Clean|Sunny) × 1

= 0.7 × 0.5 × 1 + 0.3 × 0.1 × 1
= 0.35 + 0.03 = 0.38


β₂(Sunny) = A(Rainy→Sunny) × B(Clean|Rainy) × 1
            + A(Sunny→Sunny) × B(Clean|Sunny) × 1

= 0.4 × 0.5 × 1 + 0.6 × 0.1 × 1
= 0.20 + 0.06 = 0.26
```

β₂ = [0.38, 0.26]

**(C) 平滑边际:**

```
γ₂(Z₂) ∝ α₂(Z₂) × β₂(Z₂)

γ₂(Rainy) ∝ 0.0552 × 0.38 = 0.020976
γ₂(Sunny) ∝ 0.0486 × 0.26 = 0.012636

总 = 0.020976 + 0.012636 = 0.033612 (= P(X)，和 Q2 一致!)

归一化:
P(Z₂=Rainy | X₁, X₂, X₃) = 0.020976 / 0.033612 = 0.624
P(Z₂=Sunny | X₁, X₂, X₃) = 0.012636 / 0.033612 = 0.376
```

> 关键洞察: γ₂ 归一化常数 = P(X) — 这也是 Forward 最终结果。向前和向后消息在任意时刻 t 的乘积，归一化后就是平滑边际，归一化常数恒为 P(X)。

</details>

---

## 🟡 进阶题

### Q5. Forward-Backward = Sum-Product BP

**(A)** 画出 HMM 链 Z₁—Z₂—Z₃ 对应的**因子图** (factor graph)。提示: 观测 X_t 被嵌入边因子中。

**(B)** 在因子图上，写出 BP 消息 m_{Z₁→Z₂}(Z₂) 的表达式，并证明它等于 Forward 消息 α₂(Z₂)（或归一化版本的对应部分）。

**(C)** 写出 BP 消息 m_{Z₂→Z₁}(Z₁) 的表达式，并证明它等于 Backward 消息 β₁(Z₁)。

**(D)** 用 BP 公式证明: P(Z_t | X) ∝ m_{Z_{t-1}→Z_t}(Z_t) × m_{Z_{t+1}→Z_t}(Z_t) = α_t(Z_t) × β_t(Z_t)。

<details>
<summary>点击查看答案</summary>

**(A) 因子图:**

HMM DAG 的 moral graph（将 evidence X_t 嵌入边因子）:

```
Z₁ —— Z₂ —— Z₃
```

边因子:
```
ψ₁₂(Z₁, Z₂) = P(Z₂|Z₁) × P(X₂|Z₂)  (不含 X₁，X₁ 在 ψ₀₁ 中)
ψ₂₃(Z₂, Z₃) = P(Z₃|Z₂) × P(X₃|Z₃)

根因子 (连 Z₁):
ψ₀₁(Z₁) = P(Z₁) × P(X₁|Z₁) = π_{Z₁} × B_{X₁, Z₁}
```

注意: 这是"把观测嵌入边因子"的视角。另一个等价视角是每个 X_t 作为独立因子节点连接 Z_t。

**(B) BP 消息 m_{Z₁→Z₂} 即 α:**

```
m_{Z₁→Z₂}(Z₂) = Σ_{Z₁} ψ₁₂(Z₁, Z₂) × m_{ϕ→Z₁}(Z₁)

其中 m_{ϕ→Z₁}(Z₁) = ψ₀₁(Z₁) = π_{Z₁} × B_{X₁, Z₁}

所以:
m_{Z₁→Z₂}(Z₂) = Σ_{Z₁} P(Z₂|Z₁) × P(X₂|Z₂) × π_{Z₁} × B_{X₁, Z₁}

注意: P(X₂|Z₂) 不依赖 Z₁，可提到 Σ 外:
= P(X₂|Z₂) × Σ_{Z₁} P(Z₂|Z₁) × π_{Z₁} × B_{X₁, Z₁}
= P(X₂|Z₂) × Σ_{Z₁} P(Z₂|Z₁) × α₁(Z₁)
```

和 Forward α₂ 的关系: α₂(Z₂) = m_{Z₁→Z₂}(Z₂)。Forward 的 α 消息完整包含了 P(X₂|Z₂)（即"收到 α₂ 意味着已经看到 X₂"），而某些 BP 变体可能把 P(X₂|Z₂) 留在 Z₂ 的本地因子中。核心数学本质相同。

**(C) BP 消息 m_{Z₂→Z₁} 即 β:**

```
m_{Z₂→Z₁}(Z₁) = Σ_{Z₂} ψ₁₂(Z₁, Z₂) × m_{Z₃→Z₂}(Z₂)

其中 ψ₁₂(Z₁, Z₂) = P(Z₂|Z₁) × P(X₂|Z₂)

对于 T=3: m_{Z₃→Z₂}(Z₂) = ψ₂₃(Z₂, Z₃) × 1 (Z₃ 是叶子)
                        = P(Z₃|Z₂) × P(X₃|Z₃) × 1
                        = P(Z₃|Z₂) × P(X₃|Z₃)

代入:
m_{Z₂→Z₁}(Z₁) = Σ_{Z₂} [P(Z₂|Z₁) × P(X₂|Z₂)] × [Σ_{Z₃} P(Z₃|Z₂) × P(X₃|Z₃)]
              = Σ_{Z₂} P(Z₂|Z₁) × P(X₂|Z₂) × β₂(Z₂)
              = β₁(Z₁)
```

> 结论: Forward α = 从左→右的 BP 消息。Backward β = 从右→左的 BP 消息。没有新算法，只有新名字！

**(D) BP 节点边际公式:**

在树上，节点 Z_t 的边际 = 所有入边消息的乘积:

```
P(Z_t | X) ∝ m_{Z_{t-1}→Z_t}(Z_t) × m_{Z_{t+1}→Z_t}(Z_t)
           = α_t(Z_t) × β_t(Z_t)          ← 从 (B)(C) 的等价性
           ∝ γ_t(Z_t)
```

这正是 L5 的公式 P(X_i) ∝ Π_{k∈N(i)} m_{k→i}(X_i) 在链上的特殊形式！

</details>

---

### Q6. Viterbi 算法手算

使用 Q1 的 HMM 参数。观测: X₁=Walk, X₂=Shop, X₃=Clean。

**(A)** 写出 Viterbi 算法的 δ 和 ψ 递推公式。

**(B)** 计算 δ₂ = [δ₂(Rainy), δ₂(Sunny)] 和对应的回溯指针 ψ₂。

**(C)** 找出最优状态序列 Z₁*, Z₂*, Z₃*，并给出联合概率值 P(Z*, X)。

**(D)** 对比 Viterbi 序列和"每个位置取边际最大值"的结果。它们相同吗？如果不同，解释原因。

<details>
<summary>点击查看答案</summary>

**(A) 递推公式:**

```
初始化:
δ₁(j) = π_j × B_j(X₁)
ψ₁(j) = 0  (无前驱)

递推:
δ_t(j) = max_i [δ_{t-1}(i) × A_{j,i}] × B_j(X_t)
ψ_t(j) = argmax_i [δ_{t-1}(i) × A_{j,i}]

终止:
Z_T* = argmax_j δ_T(j)

回溯:
Z_t* = ψ_{t+1}(Z_{t+1}*)
```

关键区别: Viterbi 用 **max** 替换了 Forward 的 **Σ**。

**(B) δ₂ 和 ψ₂ 计算:**

先从 Q2 已知: δ₁ = α₁ = [0.06, 0.24]

```
δ₂(Rainy):
  candidate 1: δ₁(Rainy) × A(Rainy→Rainy) = 0.06 × 0.7 = 0.042
  candidate 2: δ₁(Sunny) × A(Sunny→Rainy) = 0.24 × 0.4 = 0.096  ← max
  → × B(Shop|Rainy) = 0.096 × 0.4 = 0.0384
  ψ₂(Rainy) = Sunny

δ₂(Sunny):
  candidate 1: δ₁(Rainy) × 0.3 = 0.06 × 0.3 = 0.018
  candidate 2: δ₁(Sunny) × 0.6 = 0.24 × 0.6 = 0.144  ← max
  → × 0.3 = 0.0432
  ψ₂(Sunny) = Sunny
```

δ₂ = [0.0384, 0.0432], ψ₂ = [Sunny, Sunny]

**(C) 继续 δ₃ 和回溯:**

```
δ₃(Rainy):
  max(0.0384×0.7, 0.0432×0.4) = max(0.02688, 0.01728) = 0.02688
  → × 0.5 = 0.01344
  ψ₃(Rainy) = Rainy

δ₃(Sunny):
  max(0.0384×0.3, 0.0432×0.6) = max(0.01152, 0.02592) = 0.02592
  → × 0.1 = 0.002592
  ψ₃(Sunny) = Sunny
```

终止: Z₃* = argmax(0.01344, 0.002592) = Rainy

回溯:
```
Z₃* = Rainy
Z₂* = ψ₃(Rainy) = Rainy
Z₁* = ψ₂(Rainy) = Sunny
```

最优路径: **Sunny → Rainy → Rainy**

联合概率: δ₃(Rainy) = 0.01344

验证:
```
P(Sunny, Rainy, Rainy, Walk, Shop, Clean)
= P(Sunny)×B(Walk|Sunny) × P(Rainy|Sunny)×B(Shop|Rainy) × P(Rainy|Rainy)×B(Clean|Rainy)
= 0.4×0.6 × 0.3×0.4 × 0.7×0.5
= 0.24 × 0.12 × 0.35
= 0.01008  ← 等一下，这和 0.01344 不一致!

仔细检查: δ₃(Rainy) = 0.02688 × 0.5 = 0.01344
但联合概率:
δ₁(Sunny) = 0.4 × 0.6 = 0.24
δ₂(Rainy) = 0.24 × 0.4 × 0.4 = 0.0384  ← 正确
δ₃(Rainy) = 0.0384 × 0.7 × 0.5 = 0.01344 ✓

验证联合: 0.4×0.6 × 0.4×0.4 × 0.7×0.5 = 0.24 × 0.16 × 0.35 = 0.01344 ✓
```

**(D) 对比边际最大值:**

从 Q4(C) 的 γ 平滑结果:
```
P(Z₁|X) ≈ [0.624, 0.376] → 选 Rainy
P(Z₂|X) ≈ [0.624, 0.376] → 选 Rainy  (实际从 Q4: [0.624, 0.376])
P(Z₃|X) — 需要计算...近似 选 Rainy
```

Viterbi: **Sunny → Rainy → Rainy**

注意 Z₁ 的差异! 边际最大选 Rainy，Viterbi 选 Sunny。

**原因分析**: 边际概率是对所有可能的其他位置取平均 — Z₁ 的边际更高是 Rainy 是因为"考虑所有可能的 Z₂,Z₃ 路径求平均"后 Rainy 更频繁出现。但 Viterbi 找的是"整条路径一起看"的最优 — 局部最优 ≠ 全局最优。

> 经典例子: 就像 NBA 选秀，每个位置选"场均得分最高"的球员不一定组成最强阵容（需要 chemistry）。Viterbi 选的是"最佳阵容"，边际选的是"各位置明星"。

</details>

---

### Q7. HMM 三大推断任务

定义 HMM 的三大推断任务，并说明各自使用什么算法:

**(A)** 滤波 (Filtering) — 给定过去观测，推断当前状态

**(B)** 平滑 (Smoothing) — 给定全部观测，推断过去状态

**(C)** 预测 (Prediction) — 给定过去观测，预测未来状态

**(D)** 哪种任务不需要 Backward 消息？哪种需要完整的 Forward-Backward？

<details>
<summary>点击查看答案</summary>

**(A) 滤波: P(Z_t | X₁, X₂, ..., X_t)**

计算当前状态的后验，只基于"截至目前"的观测。

算法: **Forward 算法** (只需 α 消息)。

```
α_1 → α_2 → ... → α_t → 归一化即可
```

应用: 实时目标追踪、语音识别（逐帧解码）、Kalman 滤波的离散版本。

**(B) 平滑: P(Z_t | X₁, X₂, ..., X_T) 其中 t < T**

根据"全部观测"（未来的也算）推断过去某个时刻的状态。

算法: **Forward-Backward 算法** (α 和 β 都需要)。

```
P(Z_t | X_{1:T}) ∝ α_t(Z_t) × β_t(Z_t)
```

应用: 转录后修正、离线处理（知道整句话后反推每个音素）。

**(C) 预测: P(Z_{t+k} | X₁, ..., X_t) 其中 k > 0**

预测未来状态，不基于未来的观测（未来还没发生）。

算法: **Forward 算法 + 状态转移**。

```
P(Z_{t+k} | X_{1:t}) = Σ_{Z_{t+k-1}} P(Z_{t+k}|Z_{t+k-1}) × ... × P(Z_{t+1}|Z_t) × P(Z_t|X_{1:t})
                                                                                 ↑ 来自滤波
```

即: 在当前滤波结果的基础上，重复应用转移矩阵 A（不乘 B，因为没有未来观测）。

**(D) 算法需求对比:**

| 任务 | 需要 α (Forward) | 需要 β (Backward) |
|------|:---:|:---:|
| 滤波 | ✅ | ❌ |
| 平滑 | ✅ | ✅ |
| 预测 | ✅ | ❌ |
| MAP 解码 (Viterbi) | ❌ (用 δ) | ❌ |

滤波和预测都是"在线"算法（不需要未来信息），平滑是"离线"算法（需要完整序列）。

</details>

---

## 🔴 挑战题

### Q8. Label Bias 问题

**(A)** 考虑一个 HMM，某个状态 S0 只有一条出边指向 S1（即 P(Z_t=S1 | Z_{t-1}=S0) = 1.0）。解释为什么观测信息在 S0 处会被"淹没"，即使观测强烈暗示状态应该是 S0。

**(B)** CRF 如何解决这个问题？从归一化方式的角度解释。

**(C)** 为什么 CRF 的推断算法（Forward/Backward/Viterbi）和 HMM 完全相同？

<details>
<summary>点击查看答案</summary>

**(A) Label Bias 机制:**

HMM 的转移概率是局部归一化的: 对于每个前驱状态 i，Σ_j P(Z_t=j | Z_{t-1}=i) = 1。

如果 P(Z_t=S1 | Z_{t-1}=S0) = 1.0，那么 P(Z_t≠S1 | Z_{t-1}=S0) = 0。

在 Forward 递推中:
```
α_{t+1}(S1) = [Σ_i α_t(i) × A_{S1,i}] × B(X_{t+1}|S1)
```

当 i=S0 时，α_t(S0) × 1.0 这个项无论 α_t(S0) 多大，都被强制流入 S1。即使 X_{t+1} 的观测强烈暗示 S0（即 B(X_{t+1}|S0) 很大），由于 A_{S0,S0}=0（S0 不能自环），这个信息无法在下一时刻被有效表达。

**本质**: 局部归一化强制每个状态的出边概率和为 1，就像一个"配额"—— S0 的所有出边概率必须分完，但只有一条出边，所以 100% 给了 S1，观测信息无法和转移概率"竞争"。

**(B) CRF 的解决方案:**

CRF 用**全局归一化**替代局部归一化。

转移势函数 ψ(Z_t, Z_{t-1}) 不需要列归一化:
```
ψ(S0→S1) = 3.0   ← 只是"偏好"，不是强制概率!
ψ(S0→S0) = 0.01  ← 可以有非零值，和观测势竞争
```

全局归一化常数 Z(X) = Σ_Z Π_t ψ(Z_t, Z_{t-1}) ψ(X_t, Z_t) 在最后一次性计算。

结果: 即使 ψ(S0→S1) 很大（偏好 S1），如果观测势 ψ(X_{t+1}|S0) 更大，S0 仍然可以在竞争中"胜出"。没有局部配额限制，信息不会丢失。

**(C) 推断算法相同的原因:**

因为 Forward/Backward/Viterbi 都是**在链上做动态规划**，它们不关心归一化是局部的还是全局的。算法只依赖:
1. 图的链状拓扑（treewidth = 1）
2. 势函数在边上（pairwise）

HMM 的概率和 CRF 的势函数在计算消息时用法完全一样 — 都是乘到 α/δ 的递推中。区别仅在于:
- HMM: 递推中用概率值（已归一化）→ 最终 P(X) 自然 ≤ 1
- CRF: 递推中用势函数值（未归一化）→ 最终 Z(X) 可能很大，需要显式归一化

> **核心**: 算法结构 = 变量消除（链上的动态规划），和参数语义无关。

</details>

---

### Q9. 从 HMM 到更复杂的序列模型

**(A)** 标准的 HMM 假设"每个观测只依赖于当前状态"（X_t ⟂ Z_{t-1} | Z_t）。如果放松这个假设，允许 X_t 同时依赖于 Z_t 和 Z_{t-1}，图结构会变成什么样？对推断算法有什么影响？

**(B)** 二阶 HMM (second-order HMM) 中，Z_t 依赖于 Z_{t-1} 和 Z_{t-2}。写出联合分布的因子分解，分析树宽度 (treewidth) 的变化，以及对推断复杂度的影响。

<details>
<summary>点击查看答案</summary>

**(A) 放松观测独立性后:**

新图结构: 每条 Z_t→X_t 边之外，再加 Z_{t-1}→X_t。

```
    Z₁ → Z₂ → Z₃
    ↓  ╱↓  ╱↓
    X₁   X₂   X₃
```

X_t 现在有两个父节点: Z_t 和 Z_{t-1}。P(X_t | Z_t, Z_{t-1})。

Moral graph: 需要为每个 X_t 的父节点添加填充边 Z_{t-1}—Z_t（原来就有，所以不需要额外添加）。

推断影响: Forward 递推变为:
```
α_t(Z_t) = [Σ_{Z_{t-1}} α_{t-1}(Z_{t-1}) × P(Z_t|Z_{t-1}) × P(X_t|Z_t, Z_{t-1})]  ← 注意 P(X_t) 现在依赖两个变量
```

P(X_t|Z_t, Z_{t-1}) 无法提出来（因为它也依赖 Z_{t-1}，在 Σ 里面）。

复杂度仍然是 O(T × K²) — treewidth 不变（仍为 1），因为 moral graph 仍然是三角化的链（没有新增无弦环）。

**(B) 二阶 HMM:**

联合分布:
```
P(Z₁,...,Z_T, X₁,...,X_T)
= P(Z₁) × P(Z₂|Z₁) × [Π_{t=3}^T P(Z_t|Z_{t-2}, Z_{t-1})] × Π_{t=1}^T P(X_t|Z_t)
```

Moral graph 变化: 新增边 Z_{t-2}—Z_t（连接同一个子节点的所有父节点 → 但 Z_t 的父节点是 Z_{t-2} 和 Z_{t-1}，而 moral graph 中 Z_{t-2}—Z_{t-1} 已经存在）。

但更关键的是: Z_{t-2} 和 Z_{t-1} 现在在同一个团 {Z_{t-2}, Z_{t-1}, Z_t} 中。

**Treewidth 增加**: 从 1 变为 2。

因为最大的团大小 = 3（包含 Z_{t-2}, Z_{t-1}, Z_t），而 treewidth = 最大团大小 - 1 = 2。

**复杂度变化**: 
- 一阶 HMM: O(T × K²)
- 二阶 HMM: O(T × K³)

每次 Forward 递推需要维护 α_t(Z_{t-1}, Z_t)（两个状态变量的联合），size = K×K，转移需要 Σ_{Z_{t-2}}，计算量 = K³。

推广到 m 阶 HMM: treewidth = m, 复杂度 = O(T × K^{m+1})。

> 这就是为什么实践中很少用高阶 HMM — 指数爆炸。CRF 可以通过设计特征函数间接引入长程依赖，而不必增加 treewidth。

</details>

---

### Q10. 完整推断流水线

给定以下场景: 你有一个语音识别系统，需要将音频特征序列 X₁,...,X_T 映射到音素序列 Z₁,...,Z_T。

**(A)** 在"实时转写"场景（低延迟），你应该用哪种推断？

**(B)** 在"离线字幕生成"场景（可以等整句话说完），你应该用哪种推断？为什么比 (A) 更准确？

**(C)** 如果你想找"最可能的完整音素序列"（考虑音素间的转移是否合法），应该用哪种推断？和"每个位置独立选最可能的音素"有什么区别？

**(D)** 如果音素间转移存在"死胡同"（某个音素后面不可能跟另一个），这对 Viterbi 算法有什么影响？Forward 算法又会如何？

<details>
<summary>点击查看答案</summary>

**(A) 实时转写 → 滤波 (Filtering):**

只使用 Forward pass (α 消息)。每收到一帧音频，立即更新当前状态的信念。

P(Z_t | X₁,...,X_t) ∝ Forward α_t

优点: 低延迟（不需要等未来帧），在线算法。
缺点: 只看到过去，无法利用未来的上下文信息。

**(B) 离线字幕 → 平滑 (Smoothing):**

使用完整的 Forward-Backward (α + β 消息)。

P(Z_t | X₁,...,X_T) ∝ α_t × β_t

为什么比滤波更准确: 平滑可以利用**双向上下文**。
- 一个音素可能被前后的音素共同决定（例如英语中的 "the" 在元音前发 /ði/ 而非 /ðə/）
- 滤波在 t 时刻只知道左半边，平滑知道全局

**(C) 最可能完整序列 → Viterbi (MAP 解码):**

找 argmax_{Z₁,...,Z_T} P(Z₁,...,Z_T | X₁,...,X_T)。

和"每个位置独立取最大"的区别:
```
位置独立: Z_t* = argmax_j P(Z_t=j|X_{1:T})
MAP解码:  Z*  = argmax_{Z} P(Z|X)
```

位置独立解码可能产生**非法路径** — 即使每个 Z_t* 的边际概率最高，Z_{t}* → Z_{t+1}* 之间的转移概率可能是 0！

Viterbi 保证找到的是**合法且最优的**完整路径。

**(D) "死胡同"对算法的影响:**

假设转移矩阵中某些 A_{j,i} = 0（即状态 i 不可能转移到状态 j）。

**对 Viterbi**:
- δ_{t+1}(j) 在求 max 时自动排除前驱 i（因为 δ_t(i) × 0 = 0）
- 回溯指针不会指向死胡同方向
- 最终路径自动合法
- 但如果某时刻**所有**可能的 δ_t(i) × A_{j,i} 都是 0 → δ_{t+1}(j) = 0，该状态"不可达"

**对 Forward**:
- α_{t+1}(j) 中 Σ 的对应项为 0，不影响求和
- 不存在"无路可走"的问题（因为 Σ 允许把概率分配给所有合法路径）

**关键差异**: 如果某个状态在时刻 t 不可达（所有路径的概率都为 0），Viterbi 会绕开它，但 Forward 的 α_t 可能也是 0，导致 γ_t 也是 0 — Forward/Backward 一致性保证最终也会反映这个事实。

> 实践技巧: 在 HMM 参数初始化时，通常给转移矩阵加一个极小的平滑值（如 1e-6），避免真正的 0 概率导致 Viterbi 算法"卡住"。

</details>

---

## 📊 综合自测评分

每题 10 分，共 100 分。

| 得分 | 评价 |
|------|------|
| 90-100 | L6 完全掌握，HMM/CRF 的 VE/BP 视角了然于心 |
| 70-89  | 主干理解扎实，Forward/Backward/Viterbi 三位一体已掌握 |
| 50-69  | 概念框架有了，建议重做 Q2-Q4，彻底弄懂 α/β 的手算 |
| < 50   | 先吃透 Q1-Q3，把 α 消息的递推算到肌肉记忆再往下走 |

---

> 📖 **参考资源**
> - Rabiner (1989), "A Tutorial on Hidden Markov Models and Selected Applications in Speech Recognition" — HMM 经典教程
> - Lafferty, McCallum, Pereira (2001), "Conditional Random Fields: Probabilistic Models for Segmenting and Labeling Sequence Data" — CRF 开山论文
> - Koller & Friedman Ch.19 (Partially Observed Data) & Ch.20 (Undirected Graphical Models)
> - Sutton & McCallum (2012), "An Introduction to Conditional Random Fields" — CRF 综述
> - [Eric Xing 课程讲义 L6](http://www.cs.cmu.edu/~epxing/Class/10708/lecture.html)
