# CMU 10-708 Lecture 7 概念体系梳理 — 变分推断 I (Variational Inference)

> 整理自 Eric Xing 教授 CMU 10-708 Lecture 7: Variational Inference — Mean-Field & CAVI
>
> 核心教材: Bishop Ch.10, Murphy Ch.21, Blei et al. (2017) "Variational Inference: A Review"

---

## 📐 全局定位：L4-L6 → L7 的范式转换

```
L4-L6: 精确推断 (Exact)                L7-L8: 近似推断 (Approximate)
──────────────────────────            ──────────────────────────────
VE: 变量消除 → 单次查询                 变分推断 (VI): 优化 → 近似后验
BP: 消息传递 → 所有边际                 MCMC: 采样 → 近似后验
HMM/Viterbi: 链上特化                  
                                       
复杂度: O(exp(treewidth))              复杂度: O(迭代 × 局部更新)
适用: 树/低树宽图                        适用: 任何图! (近似但有理论保证)
```

**一句话概括 L7**：当精确推断因 treewidth 过大而行不通时，变分推断把"推断问题"转化为"优化问题"——用一个简单的分布 Q(Z) 去逼近复杂的后验 P(Z|X)，通过最大化 ELBO 来找到最好的 Q。

---

## 概念 1：为什么需要近似推断？

### 精确推断的瓶颈

```
VE/BP 复杂度: O(N × K^{treewidth+1})

其中:
  N = 变量数
  K = 每个变量的取值数
  treewidth = 图的最大团大小 - 1

例子:
  N=1000 的网格图: treewidth ~ 30 → K³¹ 不可行!
  N=1000 的树:     treewidth = 1  → K²  完全可行

结论: treewidth 决定一切。高 treewidth 图 → 精确推断不可行 → 需要近似方法。
```

### 两大类近似推断方法

```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  近似推断 (Approximate Inference)                            │
│      │                                                       │
│      ├── 确定性方法 (Deterministic)                          │
│      │     └── 变分推断 (Variational Inference) ← L7         │
│      │           用简单的 Q 逼近 P, 优化 KL 散度              │
│      │                                                       │
│      └── 随机方法 (Stochastic)                               │
│            └── MCMC (Markov Chain Monte Carlo) ← L8          │
│                 通过采样来近似 P                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 概念 2：变分原理 — 把推断变成优化 (🔑🔑🔑)

### 核心思想

我们想计算后验 P(Z | X)，但它太复杂（无法直接计算或采样）。

**变分方法**: 从一个"简单的"分布族 Q 中找一个分布 Q*(Z)，使得 Q*(Z) 和 P(Z|X) 尽可能接近。

### KL 散度 (Kullback-Leibler Divergence)

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  KL(Q || P) = Σ_Z Q(Z) · log [Q(Z) / P(Z|X)]                │
│                                                               │
│  性质:                                                        │
│    - KL(Q || P) ≥ 0  (非负)                                   │
│    - KL(Q || P) = 0  ⟺  Q = P  (几乎处处)                    │
│    - 不对称: KL(Q||P) ≠ KL(P||Q)                             │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 为什么用 KL(Q||P) 而不是 KL(P||Q)?

```
KL(Q||P): "mode-seeking" (寻模)
  → Q 集中在 P 的一个 mode 上, 不会覆盖 P 概率低的区域
  → 适合 VI (因为 Q 的支撑集可以比 P 小)

KL(P||Q): "mass-covering" (覆盖)
  → Q 必须覆盖 P 所有概率非零的区域
  → 适合 Expectation Propagation (EP, L8)
```

---

## 概念 3：ELBO — 证据下界 (Evidence Lower Bound) (🔑🔑🔑)

### ELBO 的推导

```
log P(X) = log Σ_Z P(X, Z)
         = log Σ_Z Q(Z) · [P(X, Z) / Q(Z)]
         ≥ Σ_Z Q(Z) · log [P(X, Z) / Q(Z)]     ← Jensen 不等式
         = E_Q[log P(X, Z)] - E_Q[log Q(Z)]
         ≜ ELBO(Q)

即:  log P(X) = ELBO(Q) + KL(Q(Z) || P(Z|X))

┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  ELBO(Q) = E_Q[log P(X, Z)] - E_Q[log Q(Z)]                 │
│                                                               │
│   第一项: 联合分布在 Q 下的期望对数概率                        │
│           = "Q 和数据的匹配度"                                 │
│                                                               │
│   第二项: -E_Q[log Q(Z)] = H(Q)                               │
│           = Q 的熵 = "Q 的不确定性"                            │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 关键等式

```
log P(X) = ELBO(Q) + KL(Q || P(Z|X))
    ↑           ↑              ↑
  常数      下界(可优化)    ≥ 0 (不可直接算)
  (证据)    (最大化它!)   (最小化它!)

最大化 ELBO ⟺ 最小化 KL(Q||P)
```

**ELBO 的名字由来**: 它是 log evidence P(X) 的下界 (lower bound)。

---

## 概念 4：平均场假设 (Mean-Field Approximation) (🔑🔑)

### 核心假设

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  Q(Z) = ∏_{i=1}^N Q_i(Z_i)                                  │
│                                                               │
│  即: 所有隐变量在 Q 下相互独立!                                │
│       (实际后验 P(Z|X) 中它们通常是相关的)                      │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 为什么叫"平均场"?

物理类比: 在 Ising 模型中，每个自旋受到邻居的"平均"影响，而不是逐个邻居的具体影响。

在 VI 中: 每个 Q_i(Z_i) 把其他变量 Z_{-i} 的影响"平均化"——用期望代替具体值。

### 平均场的表达能力

```
真实后验 P(Z₁, Z₂):           平均场近似 Q(Z₁)Q(Z₂):
    Z₂=0  Z₂=1                   Z₂=0  Z₂=1
Z₁=0 [0.3  0.1]              Z₁=0 [0.32 0.08]
Z₁=1 [0.1  0.5]              Z₁=1 [0.16 0.44]
    ↑ 有相关性!                   ↑ 独立 (外积)!

Q 是 P 的"外积近似" — 失去了相关性, 但抓住了每个变量的边际趋势
```

---

## 概念 5：CAVI 更新公式的推导 (🔑🔑🔑)

### 坐标上升变分推断 (Coordinate Ascent VI)

在平均场假设下，固定所有其他 Q_j (j≠i)，优化 Q_i 的最优解：

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  log Q_i*(Z_i) = E_{Q_{-i}}[log P(Z_i | Z_{-i}, X)] + const │
│                                                               │
│  其中:                                                        │
│    Q_{-i} = ∏_{j ≠ i} Q_j(Z_j)                              │
│    const 是归一化常数 (不依赖于 Z_i)                           │
│                                                               │
│  等价形式:                                                     │
│    log Q_i*(Z_i) = E_{Q_{-i}}[log P(X, Z)] + const           │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 推导的关键步骤

1. 把 ELBO 写成只依赖 Q_i 的形式
2. 固定 Q_{-i}, 对 Q_i 做变分求导 (变分法)
3. 令变分导数 = 0, 解出 Q_i*

```
ELBO = E_Q[log P(X,Z)] - Σ_i E_{Q_i}[log Q_i]
     = E_{Q_i}[E_{Q_{-i}}[log P(X,Z)]] - E_{Q_i}[log Q_i] - Σ_{j≠i} H(Q_j)
     
令 ∂ELBO/∂Q_i = 0 (加上归一化约束):

→ log Q_i*(Z_i) = E_{Q_{-i}}[log P(X,Z)] + const
```

### CAVI 算法流程

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  CAVI 算法:                                                   │
│                                                               │
│  输入: 联合分布 P(X, Z), 平均场族 Q = ∏ Q_i                  │
│                                                               │
│  1. 随机初始化所有 Q_i                                        │
│  2. 重复直到 ELBO 收敛:                                       │
│      对每个 i = 1, 2, ..., N:                                │
│        log Q_i(Z_i) ← E_{Q_{-i}}[log P(X, Z)] + const        │
│        归一化 Q_i                                             │
│      计算 ELBO, 检查收敛                                      │
│  3. 返回 Q* = ∏ Q_i*                                         │
│                                                               │
│  保证: ELBO 每一步不降, 收敛到局部最优                         │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 概念 6：指数族与共轭性 — 为什么 VI 特别"整洁"

### 指数族分布

如果每个条件分布 P(Z_i | Z_{-i}, X) 是指数族:

```
P(Z_i | Z_{-i}, X) = h(Z_i) exp(η_i · T(Z_i) - A(η_i))
```

且 Q_i(Z_i) 也是同一个指数族:

```
Q_i(Z_i) = h(Z_i) exp(ν_i · T(Z_i) - A(ν_i))
```

则 CAVI 更新极其简洁: **只更新自然参数 ν_i**!

### 条件共轭模型中的 CAVI

当模型有"条件共轭"性质时:

```
ν_i* = E_{Q_{-i}}[η_i(Z_{-i}, X)]
```

即: Q_i 的自然参数 = 条件自然参数在其他变量下的期望。

**优势**: 更新公式变成简单的参数更新, 不需要每步都对 Z_i 的每个取值做期望计算。

---

## 概念 7：VI 与 BP 的关系

### 在树结构图上

```
如果原图是树, 且 Q 是"完全分解的"平均场:
  → VI 的定点方程 ≡ BP 的消息更新方程
  → VI (在平均场假设下) 不再等于精确 BP!
  → 但在更丰富的变分族 (如 Bethe 近似) 下, VI = BP

结论: 树的精确 BP 可以被理解为一种特殊的 VI
      (在 Bethe 变分族下优化)
```

### Bethe 近似与 Loopy BP

```
Bethe 变分族: Q(Z) ∝ ∏_{边} ψ_ij / ∏_{节点} ψ_i^{d_i-1}
  (比完全分解的平均场更丰富, 保留了成对相关性)

在这个族上优化 ELBO:
  → 定点方程 = Loopy BP 的消息更新!
  → Loopy BP = Bethe 变分族上的 VI
```

---

## 概念 8：VI 的优缺点

| 维度 | VI (变分推断) | MCMC (采样) |
|------|-------------|------------|
| **方法** | 优化 (最大化 ELBO) | 采样 (构造马尔可夫链) |
| **输出** | 近似分布 Q (解析形式) | 样本集合 |
| **是否精确渐近** | ❌ 永远有近似误差 | ✅ 无限样本时精确 |
| **收敛判断** | ELBO 单调 → 易判断 | 难以判断 (收敛诊断) |
| **速度** | 通常更快 | 通常更慢 |
| **大数据** | 可结合随机梯度 (SVI) | 难以扩展 |
| **适用场景** | 需要快速的近似, 大规模 | 需要精确, 小/中等规模 |

---

## 📋 全部概念一张表

| 概念 | 一句话 |
|------|--------|
| **变分推断 (VI)** | 把推断变成优化: 用简单 Q 逼近复杂 P |
| **KL 散度** | 衡量 Q 和 P 的距离: KL(Q∥P) = E_Q[log Q/P] |
| **ELBO** | 证据下界: ELBO = log P(X) - KL(Q∥P) |
| **Jensen 不等式** | 推导 ELBO 的数学工具: log E[X] ≥ E[log X] |
| **平均场 (Mean-Field)** | Q(Z) = ∏ Q_i(Z_i): 所有变量在 Q 下独立 |
| **CAVI** | 坐标上升 VI: 轮流更新每个 Q_i |
| **CAVI 更新公式** | log Q_i* = E_{Q_{-i}}[log P] + const |
| **指数族** | 当条件分布共轭时, CAVI 只需更新参数 |
| **Bethe 近似** | 比平均场更丰富的变分族, 恰好对应 Loopy BP |
| **SVI (随机 VI)** | 用随机梯度在大数据上做 VI (L8) |

---

## 🔗 概念关系图

```
              精确推断不可行 (高 treewidth)
                        │
                        ▼
              近似推断 (Approximate Inference)
                        │
          ┌─────────────┴─────────────┐
          ▼                           ▼
    确定性方法                     随机方法
    变分推断 (VI)                  MCMC (L8)
          │
    ┌─────┴─────┐
    ▼           ▼
 ELBO        KL(Q||P)
    │           │
    └─────┬─────┘
          │
    最大化 ELBO = 最小化 KL
          │
    ┌─────┴─────┐
    ▼           ▼
 平均场       结构化 VI
 Q=∏Q_i      (Bethe 等)
    │           │
    ▼           ▼
  CAVI       Loopy BP
 (参数更新)   (消息传递)
```

---

## 🎯 核心洞见一句话

| # | 洞见 |
|---|------|
| 1 | **推断 → 优化**: VI 把不可行的求和/积分转化为可行的优化问题 |
| 2 | **ELBO = 桥梁**: 最大化 ELBO ⟺ 最小化 KL(Q∥P) — 优化 ELBO 就得到了最好的 Q |
| 3 | **平均场 = 去相关**: 假设 Q 中所有变量独立, 用"平均效应"替代"逐个邻居效应" |
| 4 | **CAVI = 贪心坐标上升**: 每次只优化一个 Q_i, ELBO 保证单调上升 |
| 5 | **Loopy BP = Bethe VI**: BP 可以被理解为在特定变分族上的 VI |

---

## 🧪 自测清单（看 L7 前带着这些问题）

- [ ] ELBO 为什么叫"证据下界"？它和 log P(X) 的关系是什么？
- [ ] 为什么用 KL(Q∥P) 而不是 KL(P∥Q)？
- [ ] 平均场假设的核心是什么？失去的是什么信息？
- [ ] CAVI 更新公式 log Q_i* = E_{Q_{-i}}[log P] + const 是怎么推导出来的？
- [ ] 为什么 CAVI 每一步 ELBO 一定不降？
- [ ] VI 和 BP 在什么条件下等价？
- [ ] VI 相比 MCMC 的优势和劣势分别是什么？

---

> L7 打开了近似推断的大门。掌握 VI 后，L8 将介绍 MCMC 采样方法 — 另一类近似推断的基石。
