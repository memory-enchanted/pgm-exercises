# CMU 10-708 Lecture 6 概念体系梳理 — 案例研究: HMM & CRF

> 整理自 Eric Xing 教授 CMU 10-708 Lecture 6: Case Studies — Hidden Markov Models & Conditional Random Fields
>
> 核心教材: Koller & Friedman Ch.10-11 (推断) + Ch.19-20 (序列模型)

---

## 📐 全局定位：L4-L5 → L6 案例研究

```
L4: 变量消除 (VE)           L5: 信念传播 (BP)           L6: HMM & CRF
─────────────────────     ─────────────────────     ─────────────────────
通用精确推断算法            通用精确推断算法            特定结构化模型
适用于任意 DAG/树           适用于任意树状图             链状/序列结构
Σ 消元 → 单次查询           消息传递 → 所有边际         Forward/Viterbi = BP特例
                           
L6 = 把 L4-L5 的算法应用到最重要的两个序列模型上
    Forward = VE in left-to-right order
    Viterbi = Max-Product on chain
    Forward-Backward = Sum-Product BP on chain ✨
```

**一句话概括 L6**：HMM 和 CRF 是处理序列数据的概率图模型。HMM 是生成式有向模型，CRF 是判别式无向模型。它们共享同一套推断算法内核 — Forward/Backward/Viterbi 不过是 L4-L5 算法在链状图上的特化。

---

## Part A: 隐马尔可夫模型 (Hidden Markov Model, HMM)

---

## 概念 1：HMM 的定义与结构

### 图结构

```
        Z₁       Z₂       Z₃            ...       Z_T
        ↓        ↓        ↓                       ↓
        X₁       X₂       X₃            ...       X_T
```

- **Z_t** (隐状态, hidden states): 不可直接观测的潜变量序列, 形成一阶马尔可夫链
- **X_t** (观测, observations): 在每个时刻产生的可观测变量, 只依赖于当前时刻的状态

### 三要素 (三个核心参数)

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  HMM = (π, A, B)                                             │
│                                                               │
│  π  = P(Z₁):        初始状态分布, 大小为 K                    │
│  A  = P(Z_t|Z_{t-1}): 转移概率矩阵, 大小为 K×K               │
│  B  = P(X_t|Z_t):    发射(观测)概率矩阵, 大小为 K×|X|        │
│                                                               │
│  K = 隐状态取值数,  |X| = 观测取值数                          │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 两个核心条件独立性假设

```
(1) 一阶马尔可夫性 (Markov property):
    Z_{t+1} ⟂ Z_{1:t-1} | Z_t
    
    "给定当前状态, 未来与过去条件独立"

(2) 观测独立性 (Observation independence):
    X_t ⟂ 所有其他变量 | Z_t
    
    "给定隐状态, 观测独立于其他所有变量"
```

用 d-separation 验证: 在 HMM 的 DAG 中, Z_t 阻断了所有 d-连接路径。

---

## 概念 2：HMM 的三大基本问题 (Rabiner, 1989)

| 问题 | 描述 | 算法 | L4-L5 对应 |
|------|------|------|-----------|
| **1. Evaluation** | 给定模型 λ=(π,A,B) 和观测序列 X, 计算 P(X\|λ) | **Forward 算法** (或 Backward) | VE 从左到右消除 |
| **2. Decoding** | 给定 λ 和 X, 找最可能的隐状态序列 Z* | **Viterbi 算法** | Max-Product on chain |
| **3. Learning** | 给定 X (和结构), 估计参数 π,A,B | **Baum-Welch (EM)** | (L7-L9 学习篇) |

L6 聚焦于问题 1 和 2 — 因为它们就是 L4-L5 算法的直接应用。

---

## 概念 3：Forward 算法 = 链式 VE (🔑🔑🔑)

### 问题

计算观测序列的概率: P(X₁, X₂, ..., X_T)

### 直接暴力

P(X) = Σ_{Z₁,...,Z_T} P(Z₁)Π_{t=2}^T P(Z_t|Z_{t-1}) Π_{t=1}^T P(X_t|Z_t)

K^T 项求和 — 指数级, 不可行!

### Forward 算法 (动态规划)

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  α_t(j) = P(X₁, X₂, ..., X_t, Z_t = j)                      │
│                                                               │
│  α₁(j) = π_j · B_j(X₁)                     (初始化)          │
│                                                               │
│  α_t(j) = [Σ_{i=1}^K α_{t-1}(i) · A_{i→j}] · B_j(X_t)      │
│           └──────────┬──────────┘                             │
│           来自 t-1 的所有路径                                  │
│                                                               │
│  最终: P(X) = Σ_{j=1}^K α_T(j)                               │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 用 L4 VE 的语言重新解释

把 Forward 算法看作按 **消除顺序** π = [Z₁, Z₂, ..., Z_T] 执行 VE:

```
消除 Z₁:
  涉及因子: P(Z₁), P(Z₂|Z₁), P(X₁|Z₁)
  乘积 + 消去 Z₁ → τ₁(Z₂) = α₁ 转发后的信息

消除 Z₂:
  涉及因子: τ₁(Z₂), P(Z₃|Z₂), P(X₂|Z₂)
  乘积 + 消去 Z₂ → τ₂(Z₃) = α₂ 转发后的信息

...

消除 Z_{T-1}:
  涉及因子: τ_{T-2}(Z_{T-1}), P(Z_T|Z_{T-1}), P(X_{T-1}|Z_{T-1})
  消去 Z_{T-1} → τ_{T-1}(Z_T)  

最后:
  涉及因子: τ_{T-1}(Z_T), P(X_T|Z_T)
  P(X) = Σ_{Z_T} τ_{T-1}(Z_T) × P(X_T|Z_T) = Σ α_T(Z_T)
```

**α_t(j) 就是在消除 Z₁,...,Z_{t-1} 后, "截至 t 时刻、以状态 j 结尾的所有路径的概率"**

---

## 概念 4：Backward 算法 = 反向消息

### 动机

Forward 算法可以计算 P(X)，但如果我们想知道 P(Z_t | X) (平滑/smoothing)，单靠 Forward 不够。

### Backward 算法

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  β_t(i) = P(X_{t+1}, X_{t+2}, ..., X_T | Z_t = i)            │
│                                                               │
│  β_T(i) = 1   (对所有 i)                  (初始化)            │
│                                                               │
│  β_t(i) = Σ_{j=1}^K A_{i→j} · B_j(X_{t+1}) · β_{t+1}(j)    │
│                                                               │
│  β_t(i) = "从状态 i 出发, 未来观测的概率"                     │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 用 L5 BP 的语言重新解释

Backward 消息 β_t 就是 BP 中从 **右侧 (未来) 向左侧 (过去)** 传递的消息！

```
      Z₁ → Z₂ → Z₃ → ... → Z_T
      ↑    ↑    ↑            ↑
      X₁   X₂   X₃           X_T

BP 消息:
  m_{Z_{t+1}→Z_t}(Z_t) = Σ_{Z_{t+1}} P(Z_{t+1}|Z_t) × P(X_{t+1}|Z_{t+1}) × m_{Z_{t+2}→Z_{t+1}}(Z_{t+1})

这正是 β_t 的递推公式!
```

**β_t = m_{t+1→t}(Z_t) — 来自"未来子树"的消息**

---

## 概念 5：Forward-Backward = Sum-Product BP (🔑🔑🔑)

### 计算平滑边际 P(Z_t | X₁...X_T)

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  γ_t(i) = P(Z_t = i | X₁, ..., X_T)                         │
│                                                               │
│  γ_t(i) ∝ α_t(i) × β_t(i)                                   │
│           └─┬──┘   └─┬──┘                                    │
│          过去信息   未来信息                                   │
│          (forward)  (backward)                                │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 这就是 L5 的"节点边际 = 入边消息乘积"！

```
在链式图中, 每个 Z_t 有两条入边:
  - 来自左边 (过去) 的消息: α_t  (forward)
  - 来自右边 (未来) 的消息: β_t  (backward)

P(Z_t | X) ∝ α_t(Z_t) × β_t(Z_t)
        = "入边消息乘积"

完全对应 L5 中的: P(Xᵢ) ∝ ∏_{k∈N(i)} m_{k→i}(Xᵢ)
```

### 完整对应表

| L5 BP 概念 | HMM Forward-Backward |
|-----------|---------------------|
| 收集阶段 (叶子→根) | Forward pass (左→右) |
| 分发阶段 (根→叶子) | Backward pass (右→左) |
| 入边消息乘积 | γ_t ∝ α_t × β_t |
| 团树校准 | Forward + Backward 完成后, 所有边际可查 |

---

## 概念 6：Viterbi 算法 = Max-Product on Chain (🔑🔑)

### 问题

找最可能的隐状态序列: Z* = argmax_Z P(Z | X)

等价于: argmax_Z P(Z, X) (分母 P(X) 与 Z 无关)

### Viterbi 算法

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  δ_t(j) = max_{Z₁,...,Z_{t-1}} P(Z₁,...,Z_{t-1}, Z_t=j,     │
│                                    X₁,...,X_t)                │
│                                                               │
│  δ₁(j) = π_j · B_j(X₁)                     (初始化)          │
│                                                               │
│  δ_t(j) = [max_i δ_{t-1}(i) · A_{i→j}] · B_j(X_t)           │
│            └────────┬────────┘                                │
│             最优前驱路径                                       │
│                                                               │
│  ψ_t(j) = argmax_i δ_{t-1}(i) · A_{i→j}   (记录回溯指针)     │
│                                                               │
│  最终: max_Z P(Z,X) = max_j δ_T(j)                           │
│        Z_T* = argmax_j δ_T(j)                                │
│        Z_t* = ψ_{t+1}(Z_{t+1}*)  (回溯)                       │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 与 L5 Max-Product 的对比

```
Sum-Product (Forward):  Σ_i α_{t-1}(i) · A_{i→j}    ← 求和
Max-Product (Viterbi):  max_i δ_{t-1}(i) · A_{i→j}   ← 取最大值

唯一的区别: Σ ↔ max
加上回溯指针 ψ 记录 argmax
```

**Viterbi = 在 HMM 链上运行 Max-Product, 加 traceback**

---

## 概念 7：三种"信念"的物理解释

```
α_t(i) = P(X₁,...,X_t, Z_t=i)     "截至 t 的前向概率"
          过去观测 + 当前状态

β_t(i)  = P(X_{t+1},...,X_T | Z_t=i)   "从 t 出发的未来条件概率"
          未来观测 | 当前状态

γ_t(i)  = P(Z_t=i | X₁,...,X_T)        "t 时刻的后验/平滑概率"
          所有观测 → 当前状态的信念
```

三者的关系: **γ_t(i) ∝ α_t(i) × β_t(i)**

---

## Part B: 条件随机场 (Conditional Random Field, CRF)

---

## 概念 8：CRF — 从生成式到判别式

### HMM 的局限

HMM 是**生成式模型**: 建模联合分布 P(Z, X) = P(Z₁)Π P(Z_t|Z_{t-1}) Π P(X_t|Z_t)

这意味着:
1. 必须对 P(X_t|Z_t) 建模 (观测模型), 而观测可能很复杂
2. 观测 X_t 之间必须条件独立 (给定 Z_t) — 这在很多任务中不成立
3. 不能灵活引入**观测之间**的特征 (如: X_t 和 X_{t-1} 的关系)

### CRF 的核心思想

**直接建模条件分布 P(Z | X)**, 不关心 P(X)。

```
生成式 (HMM):   P(Z, X) → P(Z|X) = P(Z,X)/P(X)
                ↑ 必须建模联合分布

判别式 (CRF):   P(Z|X) ∝ exp(Σ_k λ_k f_k(Z, X, t))
                ↑ 直接建模条件分布
```

### Linear-Chain CRF 的定义

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  P(Z | X) = (1/Z(X)) · ∏_{t=1}^T ψ_t(Z_t, Z_{t-1}, X)      │
│                                                               │
│  其中:                                                        │
│    ψ_t = exp(Σ_k λ_k f_k(Z_t, Z_{t-1}, X, t))               │
│    Z(X) = Σ_Z ∏ ψ_t    (全局归一化常数)                      │
│                                                               │
│    f_k: 特征函数 — 可以是任意定义的 X 和 Z 的函数            │
│    λ_k: 特征权重 — 从数据中学习                              │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 概念 9：HMM vs CRF — 生成式 vs 判别式

| 维度 | HMM | Linear-Chain CRF |
|------|-----|-----------------|
| **图结构** | 有向 DAG (Z_t → Z_{t+1}, Z_t → X_t) | 无向链 (Z_t — Z_{t+1}) + 全部 X 为条件 |
| **建模对象** | 联合分布 P(Z, X) | 条件分布 P(Z \| X) |
| **参数形式** | π, A, B (概率表) | λ_k (特征权重, 对数线性) |
| **观测模型** | P(X_t\|Z_t) — 局部, 简单 | 任意特征函数 f_k(Z, X, t) — 全局, 灵活 |
| **独立性假设** | X_t ⟂ 其他 \| Z_t | 不需要! CRF 不建模 P(X) |
| **推断算法** | Forward/Viterbi (Σ 和 max) | 相同的算法! |
| **参数学习** | MLE (计数) 或 EM | 梯度下降 (CRF 是 log-linear 模型) |
| **Label Bias** | 有 (局部归一化) | 无 (全局归一化) |

---

## 概念 10：Label Bias 问题 (为什么 CRF 优于 HMM?)

### HMM 的局部归一化

HMM 中, 转移概率 A_{i→j} 对每个源状态 i 独立归一化: Σ_j A_{i→j} = 1。

问题: 如果从状态 i 出发只有一条边 (或某条边权重极大), 那么无论观测是什么, 模型都被迫走那条路 — **局部归一化导致观测信息被淹没**。

### CRF 的全局归一化

CRF 中, 只有 Z(X) = Σ_Z ∏ ψ 这一个全局归一化常数。

优势: 势函数 ψ_t 之间可以有不同的量级, 观测可以对转移产生任意强度的影响 — **不会因为局部归一化丢失信息**。

**直观例子**:
```
有一个状态只有一条出边 → HMM 中该出边概率强制为 1 (无论观测!)
                        CRF 中该出边的势函数可以根据观测自由调整
```

---

## 概念 11：从 BP 视角统一 HMM 和 CRF 的推断

```
                  L4-L5: 通用算法
              ┌────────┼────────┐
              ▼        ▼        ▼
          VE/消元   Sum-Product  Max-Product
              │        │           │
     ┌────────┼────────┼───────────┼───────────┐
     ▼        ▼        ▼           ▼           ▼
  Forward  Backward  Forward-    Viterbi    Baum-Welch
  (P(X))   (反向消息) Backward   (MAP解码)   (EM学习)
                     (所有P(Z_t|X))
```

**核心洞见**: HMM 和 CRF 的推断**完全相同** — 都是链状图上的消息传递。唯一区别是:
- HMM 的 ψ_t(Z_t, Z_{t-1}) = P(Z_t|Z_{t-1}) × P(X_t|Z_t) (由模型参数决定)
- CRF 的 ψ_t(Z_t, Z_{t-1}) = exp(Σ λ_k f_k(...)) (由特征权重决定)

消息传递的数学形式一模一样！

---

## 📋 全部概念一张表

| 概念 | 一句话 |
|------|--------|
| **HMM** | 生成式序列模型: Z_t(隐状态) → X_t(观测), Z_t→Z_{t+1} |
| **π** | 初始状态分布 P(Z₁) |
| **A** | 转移概率矩阵 P(Z_t\|Z_{t-1}) |
| **B** | 发射概率矩阵 P(X_t\|Z_t) |
| **Forward 算法** | α_t = 从左到右累积概率, = VE 的左→右消除 |
| **Backward 算法** | β_t = 从右到左的条件概率, = BP 反向消息 |
| **Forward-Backward** | γ_t ∝ α_t × β_t, = BP 节点边际 |
| **Viterbi 算法** | δ_t = max 版本 Forward, + 回溯 = Max-Product on chain |
| **CRF** | 判别式序列模型, 直接建模 P(Z\|X) |
| **特征函数 f_k** | CRF 的核心: 灵活编码观测与标签的任意关系 |
| **全局归一化 Z(X)** | CRF 的关键优势: 避免 label bias |
| **Label Bias** | HMM 局部归一化的缺陷, CRF 通过全局归一化解决 |

---

## 🔗 概念关系图

```
                L4: VE              L5: BP
                  │                   │
          Σ 消元 / max 消元    Sum-Product / Max-Product
                  │                   │
                  └───────┬───────────┘
                          │
                    应用到链状图
                          │
              ┌───────────┼───────────┐
              ▼           ▼           ▼
          Forward     Forward-     Viterbi
          (α 消息)    Backward     (δ 消息)
          P(X)        平滑          MAP 解码
              │           │           │
              └───────────┼───────────┘
                          │
                    ┌─────┴─────┐
                    ▼           ▼
                  HMM         CRF
               (生成式)     (判别式)
               局部归一化   全局归一化
               P(Z,X)      P(Z|X)
```

---

## 🎯 核心洞见一句话

| # | 洞见 |
|---|------|
| 1 | **Forward 算法 = 链上按顺序做 VE** — α_t 就是消去 Z₁...Z_{t-1} 的中间结果 |
| 2 | **Forward-Backward = Sum-Product BP on chain** — α 是左→右消息, β 是右→左消息 |
| 3 | **Viterbi = Max-Product on chain** — 把 Σ 换成 max, 加数组记录 argmax |
| 4 | **HMM vs CRF 的核心差异在建模, 不在推断** — 推断算法完全相同! |
| 5 | **Label Bias 来自局部归一化** — CRF 的全局归一化是解决方案 |

---

## 🧪 自测清单（看 L6 前带着这些问题）

- [ ] Forward 算法中的 α_t(j) 代表什么物理含义？
- [ ] Backward 算法中的 β_t(i) 和 Forward 的 α_t(j) 有什么区别？
- [ ] 为什么 γ_t ∝ α_t × β_t 给出平滑边际？这和 L5 的什么公式对应？
- [ ] Viterbi 和 Forward 唯一的区别是什么？为什么需要额外存储 ψ_t？
- [ ] HMM 作为 BayesianNetwork 时, 图结构是什么样的？
- [ ] CRF 为什么不会有 label bias 问题？
- [ ] 特征函数 f_k 可以涉及哪些变量？HMM 的"特征"可以怎么理解？

---

> L6 是 L4-L5 推断算法在序列数据上的完美应用实例。掌握 HMM 和 CRF 后, 你将能用图模型的统一视角理解几乎所有的序列标注问题。
