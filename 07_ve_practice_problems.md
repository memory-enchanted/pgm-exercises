# CMU 10-708 Lecture 4 课后练习 & 答案

> 配套教材: Koller & Friedman Ch.9 课后习题精选
>
> 题目分为三级: 🟢 基础 | 🟡 进阶 | 🔴 挑战

---

## 🟢 基础题 (必须掌握)

### Q1. 因子乘积手算

给定两个因子：
```
φ₁(A, B):
  A    B    value
  a⁰   b⁰   0.2
  a⁰   b¹   0.8
  a¹   b⁰   0.5
  a¹   b¹   0.5

φ₂(B, C):
  B    C    value
  b⁰   c⁰   0.3
  b⁰   c¹   0.7
  b¹   c⁰   0.6
  b¹   c¹   0.4
```

求 φ₁₂(A, B, C) = φ₁(A, B) × φ₂(B, C)，写出完整的 8 行表格。

**答案**:

```
φ₁₂(A, B, C):
  A    B    C    value
  a⁰   b⁰   c⁰   0.2 × 0.3 = 0.06
  a⁰   b⁰   c¹   0.2 × 0.7 = 0.14
  a⁰   b¹   c⁰   0.8 × 0.6 = 0.48
  a⁰   b¹   c¹   0.8 × 0.4 = 0.32
  a¹   b⁰   c⁰   0.5 × 0.3 = 0.15
  a¹   b⁰   c¹   0.5 × 0.7 = 0.35
  a¹   b¹   c⁰   0.5 × 0.6 = 0.30
  a¹   b¹   c¹   0.5 × 0.4 = 0.20
```

Scope 从 {A,B}+{B,C} 变成 {A,B,C}。表大小从 4+4=8 变成 2×2×2=8。

---

### Q2. 因子边缘化手算

对 Q1 的结果 τ(A, B, C) 消去 B，求 τ_AC(A, C) = Σ_B φ₁₂(A, B, C)。

**答案**:

```
τ_AC(A, C):
  A    C    value
  a⁰   c⁰   0.06 + 0.48 = 0.54
  a⁰   c¹   0.14 + 0.32 = 0.46
  a¹   c⁰   0.15 + 0.30 = 0.45
  a¹   c¹   0.35 + 0.20 = 0.55
```

注意: τ_AC 对 C 不一定 sum to 1，它只是联合分布的未归一化部分。

---

### Q3. 学生网络完整 VE

对以下学生网络：
```
Difficulty (D) → Grade (G) → Letter (L)
                    ↑
Intelligence (I) → SAT (S)
```

参数:
```
P(D=high) = 0.4, P(I=high) = 0.3

P(G|D,I):
          D=low       D=high
        I=low I=high  I=low I=high
  G=g⁰   0.30  0.05   0.90  0.50
  G=g¹   0.40  0.25   0.08  0.30
  G=g²   0.30  0.70   0.02  0.20

P(S=high|I):
  I=low  0.05
  I=high 0.80

P(L=good|G):
  G=g⁰   0.1
  G=g¹   0.4
  G=g²   0.7
```

问题: 用 VE 计算 P(I = high | L = good)，消除顺序 π = [D, S, G]。

写出每一步的因子和中间结果。

**答案**:

**初始因子集**:
```
φ₁(D)     = P(D):           [0.6, 0.4]  scope: {D}
φ₂(I)     = P(I):           [0.7, 0.3]  scope: {I}
φ₃(G,D,I) = P(G|D,I):       3×2×2 表   scope: {G,D,I}
φ₄(S,I)   = P(S|I):         2×2 表     scope: {S,I}
φ₅(L,G)   = P(L=good|G):    [0.1, 0.4, 0.7]  scope: {G}  (证据固定)
```

**Step 1: 消除 D**

涉及 φ₁(D), φ₃(G,D,I):
```
ψ₁(G,D,I) = φ₁ × φ₃

对每个 (G,I) 组合, 计算 ψ₁(G,I,D=low)×0.6 + ψ₁(G,I,D=high)×0.4:

τ₁(G=g⁰, I=low)  = 0.6×0.30 + 0.4×0.90 = 0.18 + 0.36 = 0.54
τ₁(G=g⁰, I=high) = 0.6×0.05 + 0.4×0.50 = 0.03 + 0.20 = 0.23
τ₁(G=g¹, I=low)  = 0.6×0.40 + 0.4×0.08 = 0.24 + 0.032 = 0.272
τ₁(G=g¹, I=high) = 0.6×0.25 + 0.4×0.30 = 0.15 + 0.12 = 0.27
τ₁(G=g², I=low)  = 0.6×0.30 + 0.4×0.02 = 0.18 + 0.008 = 0.188
τ₁(G=g², I=high) = 0.6×0.70 + 0.4×0.20 = 0.42 + 0.08 = 0.50
```

计算量: 2 × 3 × 2 = 12

**Step 2: 消除 S**

涉及 φ₄(S,I):
```
τ₂(I) = Σ_S P(S|I)

τ₂(I=low)  = P(S=low|I=low) + P(S=high|I=low)  = 0.95 + 0.05 = 1.0
τ₂(I=high) = P(S=low|I=high) + P(S=high|I=high) = 0.20 + 0.80 = 1.0
```

这里 τ₂(I) = [1.0, 1.0] — 巧合！因为 P(S|I) 已经对 S 归一化。所以消除 S "无事发生"。在实际计算中我们直接跳过这一步。

计算量: 2 × 2 = 4

**Step 3: 消除 G**

涉及 τ₁(G,I), φ₅(G) (= P(L=good|G)):
```
ψ₃(G,I) = τ₁(G,I) × φ₅(G)

       G=g⁰       G=g¹       G=g²
I=low   0.54×0.1   0.272×0.4  0.188×0.7
I=high  0.23×0.1   0.27×0.4   0.50×0.7

=      G=g⁰     G=g¹     G=g²
I=low   0.054    0.1088   0.1316
I=high  0.023    0.108    0.350
```

消去 G:
```
τ₃(I=low)  = 0.054 + 0.1088 + 0.1316 = 0.2944
τ₃(I=high) = 0.023 + 0.108 + 0.350  = 0.481
```

计算量: 3 × 2 = 6

**Step 4: 归一化**

剩余因子: φ₂(I) = P(I) = [0.7, 0.3], τ₃(I) = [0.2944, 0.481]

```
P(I, L=good) ∝ P(I) × τ₃(I)

P(I=low,  L=good) = 0.7 × 0.2944 = 0.20608
P(I=high, L=good) = 0.3 × 0.481  = 0.1443

归一化: Z = 0.20608 + 0.1443 = 0.35038

P(I=low  | L=good) = 0.20608 / 0.35038 = 0.588
P(I=high | L=good) = 0.1443  / 0.35038 = 0.412
```

**总计算量**: 12 + 4 + 6 = 22 (vs 暴力 48)

---

### Q4. 识别中间因子的 scope

在上题中，如果改变消除顺序为 π = [G, D, S]，中间因子最大的 scope 是多少？

**答案**:

消除 G 时:
- 涉及因子: P(G|D,I), P(L=good|G)
- 乘积后 scope: {G, D, I}
- 消去 G 后 scope: {D, I}

消除 D 时:
- 涉及因子: τ_G(D,I), P(D)
- 乘积后 scope: {D, I}
- 消去 D 后 scope: {I}

消除 S 时:
- 涉及因子: τ_D(I), P(S|I)
- 消去 S 后 scope: {I}

最大中间 scope: {G, D, I} → 大小 = 3×2×2 = 12

原顺序 π=[D, S, G] 的最大中间 scope 也是 12（都是 {G,D,I} 在消除 D 时）。

对于这个小网络，不同顺序差别不大。在大网络中就可能差几个量级。

---

## 🟡 进阶题

### Q5. 填充边与顺序

对以下图，写出两种消除顺序并给出填充边：

```
    A
   / \
  B   C
   \ /
    D
    |
    E
```

(这叫做"钻石图" diamond graph)

(A) 顺序: B, C, A, D, E  — 有几条填充边？

**答案 (A)**:
```
消 B: B 的邻居 = {A, D}, 加边 A—D (填充边 1)
消 C: C 的邻居 = {A, D}, A—D 已存在, 无需加边
消 A: A 的邻居只剩 {D}, 无需加边
消 D: D 的邻居 = {E}, 无需加边
消 E: 没有邻居了

填充边: 1 条 (A—D)
最大团: B 消除时形成 {A, B, D} (大小 3) → treewidth = 2
```

(B) 顺序: A, B, C, D, E  — 有几条填充边？

**答案 (B)**:
```
消 A: A 的邻居 = {B, C, D}, 加边 B—C, B—D, C—D (填充边 3 条!)
        现在 {B, C, D} 成了三角形
消 B: B 的邻居 = {C, D}, 已全连接, 无需加边
消 C: C 的邻居现在只有 {D}, 已连接
消 D: D 的邻居 = {E}, 无需加边
消 E: 无邻居

填充边: 3 条 (B—C, B—D, C—D)
最大团: {A, B, C, D} (大小 4) → treewidth = 3
```

**结论**: 顺序 A 更好! 先消度小的叶子节点。

---

### Q6. 树宽度与复杂度上界

(A) 链状图 (path graph) 的树宽度是多少？

**答案**: tw(path) = 1
- 任何消除顺序，每次只消端点(度 ≤ 1)，中间因子 scope 最多 2 个变量
- VE 复杂度: O(n × k²)，其中 k 是变量基数

(B) 环状图 (cycle graph) 的树宽度是多少？

**答案**: tw(cycle) = 2
- 第一次消除会破坏环，产生 1 条填充边，此后再消除的都是树节点
- 中间因子 scope 最多 3 个变量
- VE 复杂度: O(n × k³)

(C) n×n 网格的树宽度是多少？

**答案**: tw(n×n grid) ≈ n
- 每次消除填一排，中间因子最终会包含一整行 (n 个变量)
- VE 复杂度: O(exp(n)) — 指数级，VE 在此不可行！
- 这就是为什么图像分割 (grid-structured MRF) 需要近似推断

---

### Q7. 为什么 VE 对证据变量的处理是"固定值"？

VE 算法中处理证据 E=e 的方式是：把包含 E 的因子中的非匹配行置 0（或等价地只保留匹配 E=e 的行）。为什么不能先消掉 E 再加证据？

**答案**:

因为条件概率的定义要求证据固定。数学上:
```
P(Q | E=e) = P(Q, E=e) / P(E=e)
```

如果在分子中就消去 E（对所有 e 求和），会丢失"E 必须等于 e"这个条件。保留证据行 = 在因子乘积中嵌入 δ(E=e) 约束因子。

等价地，可以看作在因子集中添加一个证据因子 δ(E=e)，它的 scope 是 {E}，对于 E=e 的行值为 1，其他行值为 0。这样 VE 的乘积操作自动把不匹配的行消成 0。

---

## 🔴 挑战题

### Q8. 从 VE 推导 BP 的消息公式

考虑一个树状 DAG: X₁ → X₂ → X₃ → ... → Xₙ

(A) 用 VE 计算 P(Xₙ)，写出 τ₁ 到 τₙ₋₁ 的递推关系。

**答案**:

```
τ₁(X₂) = Σ_{X₁} P(X₁) × P(X₂|X₁)                      ← 消 X₁
τ₂(X₃) = Σ_{X₂} τ₁(X₂) × P(X₃|X₂)                    ← 消 X₂
...
τ_{n-1}(Xₙ) = Σ_{X_{n-1}} τ_{n-2}(X_{n-1}) × P(Xₙ|X_{n-1})  ← 消 X_{n-1}

最终: P(Xₙ) ∝ τ_{n-1}(Xₙ)  (如果是最后一个节点, 它就是边际概率)
```

(B) 证明 τ_k(X_{k+1}) = m_{k→k+1}(X_{k+1})，其中 m 是 BP 中的消息。

**答案**:

BP 中从 X_k 到 X_{k+1} 的消息公式:
```
m_{k→k+1}(X_{k+1}) = Σ_{X_k} [ P(X_{k+1}|X_k) × (所有进入 X_k 的消息的乘积) ]
```

对于链:
- X₁ 没有入边消息 → m_{1→2}(X₂) = Σ_{X₁} P(X₁)·P(X₂|X₁) = τ₁(X₂) ✓
- X_k 收到 m_{k-1→k} → m_{k→k+1} = Σ_{X_k} m_{k-1→k}(X_k)·P(X_{k+1}|X_k) = τ_k(X_{k+1}) ✓

归纳可证对所有 k 成立。所以 VE 中间结果 τ_k 就是 BP 消息。

---

### Q9. 最大化版本的 VE (Max-Product/Max-Sum)

(A) 将 Q3 中 Sum-Product VE 改成 Max-Product，求 MAP 赋值 argmax_{D,I,G,S} P(D,I,G,S | L=good)。

提示: 把 Σ 换成 max，跟踪 argmax。

**答案**:

**(1) 消去 D (max 版本)**:

ψ₁(G,D,I) = P(D) × P(G|D,I)

求 max_D ψ₁(G,D,I) 并记录 argmax_D:

```
           D=low    D=high   | max  | argmax
G=g⁰ I=low   0.6×0.30=0.18  0.4×0.90=0.36 | 0.36 | D=high
G=g⁰ I=high  0.6×0.05=0.03  0.4×0.50=0.20 | 0.20 | D=high
G=g¹ I=low   0.6×0.40=0.24  0.4×0.08=0.032| 0.24 | D=low
G=g¹ I=high  0.6×0.25=0.15  0.4×0.30=0.12 | 0.15 | D=low
G=g² I=low   0.6×0.30=0.18  0.4×0.02=0.008| 0.18 | D=low
G=g² I=high  0.6×0.70=0.42  0.4×0.20=0.08 | 0.42 | D=low
```

**(2) 消去 S (max 版本)**:

ψ₂(S,I) = P(S|I)

对每个 I，求 max_S P(S|I):
```
I=low:  max(0.95, 0.05) = 0.95, argmax_S = low
I=high: max(0.20, 0.80) = 0.80, argmax_S = high
```

τ₂(I) = [0.95, 0.80]

**(3) 消去 G (max 版本)**:

ψ₃(G,I) = max_D τ₁(G,I) × φ₅(G):

```
           G=g⁰        G=g¹        G=g²       | max   | argmax_G
I=low      0.36×0.1=   0.24×0.4=   0.18×0.7=      |        |
           0.036       0.096       0.126        | 0.126 | G=g²
I=high     0.20×0.1=   0.15×0.4=   0.42×0.7=      |        |
           0.020       0.060       0.294        | 0.294 | G=g²
```

τ₃(I) = [0.126, 0.294]

**(4) 找 max_I**:

max(0.7 × 0.126, 0.3 × 0.294) = max(0.0882, 0.0882) = 0.0882

两个相等！取 I=high（更大的值来源）。

**(5) 回溯 argmax**:

```
I* = high
G* = g²  (从 τ₃(high) 回溯)
S* = high (从 τ₂(high) 回溯)
D* = low  (从 τ₁(G=g², I=high) 回溯)
```

**最终 MAP 赋值**: D=low, I=high, G=g², S=high, L=good

(B) 解释为什么 Max-Sum (在 log 空间中做 max) 比 Max-Product 数值上更稳定。

**答案**:

乘积容易下溢: P(G=g², I=high, D=low) = 0.6 × 0.3 × 0.70 × 0.8 × 0.7 = 0.07056 还好，但更大的网络中可能有很多接近 0 的概率相乘，double 精度不够。

Max-Sum: 取 log 后变成求和:
```
log P(G=g², I=high, D=low) = log(0.6) + log(0.3) + log(0.7) + log(0.8) + log(0.7)
```

max 操作在 log 域等价 — max(log a, log b) = log(max(a, b))，因为是单调函数。求和不会下溢。

---

### Q10. 最小度启发式的反例

最小度 (min-degree) 启发式: 每次消除当前图中度最小的节点。这个启发式是否总是最优？若否，举反例。

**答案**: 不是。

**反例**: 考虑一个图，有一个"关键节点"度很大，但消除它之后整张图变成森林；如果一直避免消除它（因度大），会在其他地方不断产生填充边，最终 treewidth 更大。

更具体的反例来自"最小填充"不一定等价于"最小度"。有一些图构造可以让最小度启发式产生指数级更多的填充边（Bodlaender & Koster, 2010 中有系统分析）。

实践中: min-degree 是一个**快速且通常不错**的启发式，但对硬实例可能很差。更精确的 min-fill (每次消除产生最小填充边的节点) 通常更好，但计算成本更高。

---

## 📊 综合自测评分

每题 10 分，共 100 分。

| 得分 | 评价 |
|------|------|
| 90-100 | L4 完全掌握，可以进 L5 |
| 70-89  | 主干理解了，再多做几个 VE 练习 |
| 50-69  | 回头重看 L4 的 VE 推导部分 (视频 15:00-45:00) |
| < 50   | 建议先巩固 Q1-Q3，彻底弄懂 VE 三步法再推进 |
