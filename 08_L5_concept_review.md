# CMU 10-708 Lecture 5 概念体系梳理 — 和积算法与信念传播

> 整理自 Eric Xing 教授 CMU 10-708 Lecture 5: Sum-Product & Belief Propagation
>
> 核心教材: Koller & Friedman Ch.10-11

---

## 📐 全局定位：L4 → L5 的演进

```
L4: 变量消除 (Variable Elimination)        L5: 信念传播 (Belief Propagation)
─────────────────────────────────────     ────────────────────────────────────
一次回答一个查询 P(Q|E)                    一次计算, 回答所有查询
重复计算 (不同查询重头来过)                 消息缓存 (一次计算, 到处复用)
"按顺序消变量"的视角                       "在图上发消息"的视角
核心操作: Σₓ (乘积)                        核心操作: 节点收集消息 → 发消息
适合: 单次查询                             适合: 多次查询 / 计算所有边际
```

**一句话概括 L5**：把 VE 的消除中间结果重新解释为在**团树 (Clique Tree)** 上流动的**消息 (Messages)**，从而实现"一次计算、回答所有查询"。

---

## 概念 1：为什么 L4 的 VE 不够用？

### VE 的弱点

```
场景: 网络有 100 个节点, 你想知道每个节点的边际概率 P(Xᵢ)

用 VE:
  - 对 X₁: 消 99 个变量 → 一次完整的 VE
  - 对 X₂: 消 99 个变量 → 又一次完整的 VE
  - ...
  - 对 X₁₀₀: 消 99 个变量 → 第 100 次 VE!

大部分中间计算是完全重复的!
```

### 一个启发性例子：链式图

```
X₁ → X₂ → X₃ → X₄

VE 求 P(X₃): 先消 X₁(得 τ₁(X₂)), 再消 X₂(得 τ₂(X₃)), 再消 X₄(得 τ₃(X₃))

VE 求 P(X₂): 先消 X₁(得 τ₁(X₂)), 再消 X₃(需要同时消 X₄)...

注: τ₁(X₂) 在两次查询中完全相同!
```

**核心洞见**: 如果能把所有 τ 都算好存起来，以后任何查询都能直接"拼装"已有结果。

---

## 概念 2：从 VE 到消息传递 — 视角转换

### VE 的消元过程重写

```
VE 消去 X₁ 的步骤:

  ψ(X₁, X₂) = P(X₁) × P(X₂|X₁)              ← 收集所有含 X₁ 的因子
  τ(X₂)     = Σ_{X₁} ψ(X₁, X₂)              ← 对 X₁ 求和

现在把 τ 改名叫 m:

  m_{1→2}(X₂) = Σ_{X₁} [P(X₁) × P(X₂|X₁)]   ← 消息! X₁发给X₂的"信息"
```

### 消息的物理直觉

```
         P(X₁)         P(X₂|X₁)        P(X₃|X₂)
       ┌──────┐       ┌────────┐       ┌────────┐
       │  X₁  │──m₁→₂─→│   X₂   │──m₂→₃─→│   X₃   │
       └──────┘       └────────┘       └────────┘
                            │
                         消息 m₁→₂(X₂):
                         "X₁ 把所有关于 X₂ 的信息
                          汇总成了这个消息"
                           
                         消息 = "整合好的局部知识"
```

---

## 概念 3：Sum-Product 算法的核心公式 (🔑🔑🔑)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   从节点 i 发给节点 j 的消息:                             │
│                                                           │
│   m_{i→j}(Xⱼ) = Σ_{Xᵢ} [ ψᵢⱼ(Xᵢ, Xⱼ)                   │
│                           × ∏_{k ∈ N(i)\{j}} m_{k→i}(Xᵢ) ]│
│                                                           │
│   其中:                                                   │
│     ψᵢⱼ(Xᵢ, Xⱼ) : i 和 j 之间的"边因子"                 │
│     N(i)\{j}     : i 的所有邻居, 除去 j (接收方)          │
│     m_{k→i}(Xᵢ) : i 从其他邻居收到的消息                  │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### 逐词拆解公式

```
m_{i→j}(Xⱼ) = Σ_{Xᵢ} [ 边因子  ×  (所有入边消息的乘积) ]
       ↑          ↑        ↑              ↑
    发给j的    对i求和   i和j之间     i从其他邻居收的消息
    关于Xⱼ     (消去i)   的局部关系    (已整合的信息)
```

**直觉**: 
1. 节点 i 先收集所有邻居（除了 j）发给它的消息
2. 乘上 i 和 j 之间的局部因子（边上的 CPD/势函数）
3. 对 i 求和（因为 j 不关心 i 的具体值）
4. 结果 = i 给 j 的消息 — "根据我所知，关于你的信息是..."

### 链式图的实例

```
X₁ → X₂ → X₃ → X₄

消息从叶子开始向内部传播:

m_{1→2}(X₂) = Σ_{X₁} P(X₁) × P(X₂|X₁)
              ↑ 没有其他邻居, N(1)\{2} = ∅, 乘积为空=1

m_{3→2}(X₂) = Σ_{X₃} P(X₃|X₂) × m_{4→3}(X₃)
              ↑ N(3)\{2} = {4}, 需要先有 m_{4→3}

m_{4→3}(X₃) = Σ_{X₄} P(X₄|X₃)
              ↑ 叶子节点, 没有其他邻居
```

**消息从叶子向内传播** — 这是所有 BP 算法的基本调度策略。

---

## 概念 4：节点边际 = 所有入边消息的乘积 (🔑🔑)

一旦所有消息算好，任意节点的边际概率只需一步：

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   P(Xᵢ) ∝ ∏_{k ∈ N(i)} m_{k→i}(Xᵢ)                     │
│                                                           │
│   节点的边际 = 它从所有邻居收到的消息的逐元素乘积          │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### 为什么这是对的？

回顾 VE 计算 P(Xᵢ) 时，最终剩下的因子 = 所有被消变量的"信息"汇总在 Xᵢ 上。每个邻居方向来的消息正好对应 VE 中消除那个子树的结果。

```
          m_{k→i}(Xᵢ) ← 消除"k 所在子树"后, 汇总到 i 的信息

          P(Xᵢ) ∝ 所有方向汇总的信息
```

### 链式图验证

```
X₁ → X₂ → X₃ → X₄

P(X₂) ∝ m_{1→2}(X₂) × m_{3→2}(X₂)
         ↑ 来自左边子树   ↑ 来自右边子树
         (消X₁的结果)      (消X₃,X₄的结果)

P(X₃) ∝ m_{2→3}(X₃) × m_{4→3}(X₃)
```

---

## 概念 5：Sum-Product 算法流程

### 步骤

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│  Step 1: 选根节点 r                                       │
│          (任意节点, 通常选中间或度大的)                     │
│                                                           │
│  Step 2: Collect (收集阶段 / 向上传递)                     │
│          从叶子开始, 逐层向根节点发消息                    │
│          顺序: 后序遍历 (post-order)                       │
│                                                           │
│  Step 3: Distribute (分发阶段 / 向下传递)                  │
│          从根节点开始, 逐层向叶子发消息                    │
│          顺序: 前序遍历 (pre-order)                        │
│                                                           │
│  Step 4: 计算边际                                         │
│          P(Xᵢ) ∝ ∏_{k∈N(i)} m_{k→i}(Xᵢ)                │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### 收集 + 分发图示

```
以 X₃ 为根的链: X₁ → X₂ → X₃ → X₄ → X₅

收集 (Collect) 阶段 ──→                  ←── 分发 (Distribute) 阶段

X₁ → X₂ → X₃ ← X₄ ← X₅                X₁ ← X₂ ← X₃ → X₄ → X₅
    ↗    ↗                                   ↙    ↘
  消息  消息                                消息   消息
  向根  向根                                离根   离根

完成后: 每条边的两个方向都有消息!
```

**收集 + 分发 = 每条边上双向都有消息，所有节点的边际可同时算出。**

---

## 概念 6：团树 (Clique Tree / Junction Tree) 

### 从原图到团树

BP 在普通树上可以直接跑。但**如果原图不是树怎么办？**

```
原图 (可能有环):                    团树 (Clique Tree):
    A — B                            ┌──────────┐
    |   |          → 编译成 →         │  {A,B,C} │
    C — D                            └────┬─────┘
                                    ┌─────┴─────┐
                                    │  {B,C,D}  │
                                    └───────────┘
```

**团树** = 节点的集合 = 团 (cliques)，边 = 团之间的交集 (sepsets)。

### 团树必须满足的三条性质

| 性质 | 含义 | 
|------|------|
| **团覆盖** | 每个因子 ψ_C 必须被某个团包含 |
| **运行交集 (Running Intersection)** | 如果变量 X 出现在团 Cᵢ 和 Cⱼ 中，则 X 必须出现在连接 Cᵢ 和 Cⱼ 的路径上**所有**团中 |
| **树结构** | 团节点之间形成一棵树（无环） |

运行交集性质保证了**信息的全局一致性** — 消息传递的结果与消除顺序无关。

---

## 概念 7：BP 与 VE 的严格对应

```
VE (单次查询)                         BP (所有边际)
─────────────                        ────────────
消除变量 X                            节点发消息
    ↓                                    ↓
找出所有含X的因子                     收集所有邻居消息(除了接收方)
    ↓                                    ↓
全部乘起来                            全部乘起来
    ↓                                    ↓
对X求和 → 传给下一个变量              对发方求和 → 发给接收方
    ↓                                    ↓
τ(剩余变量)                           m(接收方变量)

完全相同! 只是组织方式不同。
```

### 为什么 BP 更高效？

```
VE 回答所有边际: O(n × n × exp(w))   ← n 次独立 VE, 重复劳动
BP 回答所有边际: O(n × exp(w))       ← 一次收集+分发, 2n 条消息

其中 w = clique tree 的最大团大小
```

---

## 概念 8：Max-Product / Max-Sum — MAP 推断

### 把 Σ 换成 max

```
Sum-Product (边际):      Max-Product (MAP):
───────────────────      ─────────────────
m_{i→j} = Σ_{Xᵢ} [...]    m_{i→j} = max_{Xᵢ} [...]
                          ↑ 同时记录 argmax!
```

### Max-Sum = 数值稳定的 Max-Product

在 log 空间中操作:
```
m_{i→j} = max_{Xᵢ} [log ψᵢⱼ + Σ log(入边消息)]
```

乘积变加法 → 不会出现数值下溢。

---

## 概念 9：Loopy BP — 图中有环怎么办？

### 朴素方案：直接在有环图上跑 BP

```
初始化所有消息为 1
重复:
  对每条边, 用当前入边消息计算新消息
  更新消息
直到"收敛"或达到最大迭代次数
```

### Loopy BP 的困境

| 特性 | 树上的 BP | Loopy BP |
|------|----------|----------|
| 收敛保证 | ✅ 一定收敛 | ❌ 可能不收敛/震荡 |
| 精确性 | ✅ 精确边际 | ❌ 近似边际 |
| 迭代次数 | 2 轮 (收集+分发) | 可能很多轮 |
| 实际表现 | 完美 | 很多情况下还不错! |

**Eric Xing 的经典评价**: "Loopy BP 理论上没有保证，但在实践中经常工作得惊人的好 — 这就是为什么它被广泛使用。"

**为什么能工作？**
- 环足够长时，消息绕环一周的"回声"衰减
- 对 Turbo codes (译码)、图像去噪等，Loopy BP 是 SOTA 方法

---

## 📋 全部概念一张表

| 概念 | 位置 | 一句话 |
|------|------|--------|
| **信念传播 (BP)** | L5 | 在图上通过消息传递计算所有边际的算法 |
| **消息 m_{i→j}** | L5 | 节点 i 汇总子树信息后发给 j 的因子 |
| **Sum-Product** | L5 | Σ 版本: 计算边际概率 |
| **Max-Product** | L5 | max 版本: 计算 MAP 赋值 |
| **Max-Sum** | L5 | log 空间的 Max-Product, 数值稳定 |
| **收集 (Collect)** | L5 | 从叶子→根的向上消息传递 |
| **分发 (Distribute)** | L5 | 从根→叶子的向下消息传递 |
| **边因子 ψᵢⱼ** | L5 | 边两端节点的联合局部因子 (CPD × 证据) |
| **节点边际** | L5 | P(Xᵢ) ∝ 所有入边消息的乘积 |
| **团树 (Clique Tree)** | L5 | 将原图编译为一棵树, 节点=团, 边=交集 |
| **运行交集性质** | L5 | 保证团树消息传递的全局一致性 |
| **Calibration (校准)** | L5 | 相邻团在交集上边际一致的状态 |
| **Loopy BP** | L5 | 在有环图上直接跑 BP (近似, 不保证收敛) |

---

## 🔗 概念关系图

```
                  L4: VE (消元视角)
                        │
                   ∑_{X} (∏ 因子)
                        │
              重新解释消元结果
                        │
                        ▼
                  L5: BP (消息视角)
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
   Sum-Product      Max-Product     团树 (Junction Tree)
   算边际概率        算 MAP 赋值       编译非树图为树
        │               │               │
        ▼               ▼               ▼
   收集+分发        argmax追踪       运行交集性质
        │               │               │
        ▼               ▼               ▼
   所有 P(Xᵢ)      MAP 赋值图        保证一致性
        │
        ├── 树结构 → 精确, O(n·exp(w)), 2轮收敛
        │
        └── 有环结构 → Loopy BP, 近似, 可能不收敛
```

---

## 🎯 核心洞见一句话

| # | 洞见 |
|---|------|
| 1 | **消息 = 子树信息的充分统计量** — 消息汇总了消除一个子树得到的全部信息 |
| 2 | **BP = VE 的缓存复用** — L4 每次查询单独消元, L5 一次传递服务所有查询 |
| 3 | **收集 + 分发 = 每条边双向有消息** — 之后任何边际 = 本地消息乘积 |
| 4 | **团树是"编译器"** — 把有环原图编译成无环团树, BP 就能在上面精确运行 |
| 5 | **Loopy BP 是"明知不可为而为之"** — 实践中经常有效, 但永远记得它是近似的 |

---

## 🧪 自测清单（看 L5 前带着这些问题）

- [ ] L4 VE 的中间结果 τ 和 L5 的消息 m 是什么关系？
- [ ] 消息公式中的 N(i)\{j} 为什么要排除接收方？
- [ ] 为什么收集阶段要从叶子开始？如果从中间开始会怎样？
- [ ] 分发阶段的目的是什么？不执行分发能算所有的边际吗？
- [ ] 为什么团树要求"运行交集"性质？违反它会怎样？
- [ ] Sum-Product 和 Max-Product 唯一的区别是什么？
- [ ] Loopy BP 和精确 BP 在实现上有什么区别？

---

> L4→L5 桥梁已搭好。L5 看完后，L6-L9 会进入 PGM 的第三大支柱: **学习 (Learning)** — 参数和结构如何从数据中习得。
