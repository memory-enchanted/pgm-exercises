# CMU 10-708 Lecture 5 概念体系梳理 — 参数估计

> 整理自 Eric Xing 教授 CMU 10-708 Lecture 5: Parameter Estimation
>
> 核心教材: Koller & Friedman Ch.17-19; Bishop PRML Ch.2-4, 9

---

## 📐 全局定位：参数估计在 PGM 中的位置

```
                        概率图模型 (PGM)
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
    表示 (Representation)   推理 (Inference)      学习 (Learning)
    L1-L3                  L4                     L5-L9
    图怎么画                怎么算P(Q|E)?          参数和图怎么从数据学
                                                    │
                                                    ▼
                                              L5: 参数估计
                                        给定图结构，学最优参数 θ
```

**L5 的核心问题**：图结构已知（L1-L3），数据已观测到（即没有隐变量或部分观测），如何从数据中估计 CPD 的参数 θ？

---

## 概念 1：MLE — 极大似然估计 (🔑🔑🔑)

### 问题形式化

```
已知: N 个 i.i.d. 样本 D = {x¹, x², ..., xᴺ}
      参数化分布族 P(x | θ)

目标: 找 θ 使得观察到的数据"最可能发生"
      θ* = argmax_θ P(D | θ)
         = argmax_θ ∏_{n=1}^N P(xⁿ | θ)
```

### 对数似然

乘积 → 求和（数值稳定 + 求导方便）:

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   ℓ(θ; D) = log P(D | θ) = Σ_{n=1}^N log P(xⁿ | θ)     │
│                                                           │
│   θ* = argmax_θ ℓ(θ; D)                                  │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### MLE 三步法

```
Step 1: 写出对数似然 ℓ(θ) = Σ log P(xⁿ|θ)
Step 2: 求导 ∂ℓ/∂θ = 0
Step 3: 解出 θ*
```

### 例 1: 抛硬币 (Bernoulli MLE)

```
数据: H, T, H, H, T  (3次正面, 2次反面)
参数: θ = P(H)

ℓ(θ) = 3 log θ + 2 log(1-θ)
∂ℓ/∂θ = 3/θ - 2/(1-θ) = 0
→ θ* = 3/5 = 0.6  ← "正面次数 / 总次数", 即经验频率
```

### 例 2: 一元高斯 MLE

```
数据: x¹, x², ..., xᴺ ∈ ℝ
参数: θ = (μ, σ²)

ℓ(μ, σ²) = -N/2 log(2πσ²) - 1/(2σ²) Σ (xⁿ - μ)²

∂ℓ/∂μ = 0 → μ* = (1/N) Σ xⁿ        ← 样本均值
∂ℓ/∂σ² = 0 → σ²* = (1/N) Σ (xⁿ - μ*)²  ← 样本方差 (有偏)
```

> ⚠️ σ²* 的分母是 N 而非 N-1 — MLE 方差是有偏的（小样本时偏低），但大样本时一致。

---

## 概念 2：多元高斯分布的 MLE (🔑)

### 密度函数

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   P(x | μ, Σ) = (2π)^(-d/2) |Σ|^(-1/2)                   │
│                 × exp[-½ (x-μ)ᵀ Σ⁻¹ (x-μ)]               │
│                                                           │
│   参数: μ ∈ ℝᵈ (均值向量), Σ ∈ ℝᵈˣᵈ (协方差矩阵)        │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### MLE 解

```
μ*  = (1/N) Σ_{n=1}^N xⁿ                    ← 样本均值向量

Σ*  = (1/N) Σ_{n=1}^N (xⁿ - μ*)(xⁿ - μ*)ᵀ   ← 样本协方差矩阵
     = (1/N) Xᵀ X   (数据中心化后, X 是 N×d 矩阵)
```

### 关键性质

| 性质 | 说明 |
|------|------|
| 充分统计量 | 对于高斯分布, **样本均值** 和 **样本协方差** 是充分统计量 |
| Σ 的正定性 | Σ 必须是正定的 → 要求 N ≥ d (否则不满秩, 不可逆) |
| 高维问题 | d 很大时 Σ 有 d(d+1)/2 个参数 → 需要大量数据或正则化 |

### 高斯分布的几何直觉

```
一维: 钟形曲线, μ=中心, σ=宽度
二维: 椭圆等高线, μ=椭圆中心, Σ 的特征向量=椭圆轴方向, 特征值=轴长度²
三维+: 超椭球
```

---

## 概念 3：GLM — 广义线性模型 (🔑)

### 为什么需要 GLM？

普通线性回归: y = wᵀx + ε, ε ~ N(0,σ²) — 只适用于连续、无界、正态的响应变量。

GLM 扩展到了**任意指数族分布**的响应变量。

### GLM 三要素

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│  1. 随机分量 (Random Component):                          │
│     Y | X ~ ExponentialFamily(η)                          │
│     η = 自然参数 (natural parameter)                      │
│                                                           │
│  2. 系统分量 (Systematic Component):                      │
│     η = θᵀ x    ← 线性预测器                              │
│                                                           │
│  3. 连接函数 (Link Function):                             │
│     g(μ) = η = θᵀ x                                      │
│     μ = E[Y|X] = g⁻¹(θᵀ x)                               │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### 常见 GLM 实例

| 模型 | 响应分布 | 连接函数 g(μ) | 用途 |
|------|----------|--------------|------|
| 线性回归 | Gaussian(μ, σ²) | μ (恒等) | 连续值预测 |
| 逻辑回归 | Bernoulli(μ) | log(μ/(1-μ)) (logit) | 二分类 |
| 泊松回归 | Poisson(μ) | log(μ) | 计数数据 |
| 多项回归 | Multinomial | log(μⱼ/μₖ) | 多分类 |

**核心洞见**: GLM 统一了这些看起来不一样的模型 — 它们只是换了不同的指数族分布 + 连接函数。

### 指数族 (Exponential Family) 标准形式

```
P(y | η) = h(y) exp(ηᵀ T(y) - A(η))

其中:
  η    = 自然参数 (natural/canonical parameter)
  T(y) = 充分统计量 (sufficient statistic)
  A(η) = log-partition function (对数配分函数), 保证归一化
  h(y) = 底层测度 (base measure)
```

**关键性质**（指数族的魔力）:
- E[T(Y)] = ∇_η A(η) — 充分统计量的期望 = A 的梯度
- Var[T(Y)] = ∇²_η A(η) — 充分统计量的方差 = A 的 Hessian
- A(η) 是凸函数 → 对数似然是凹的 → **MLE 有唯一全局最优**！

---

## 概念 4：IRLS — 迭代重加权最小二乘 (🔑)

### 问题

对 GLM 做 MLE，似然方程**一般没有闭式解**（除高斯线性回归外）。

怎么办？用数值优化。

### IRLS = 牛顿法的一种巧妙实现

GLM 的 MLE 目标:

```
ℓ(θ) = Σ_{n=1}^N [ yⁿ ηⁿ - A(ηⁿ) ]   (忽略常数 h(y))
      其中 ηⁿ = θᵀ xⁿ
```

牛顿更新: θ^(t+1) = θ^(t) - H⁻¹ g

其中:
- 梯度: g = ∇ℓ = Xᵀ (y - μ) （μ = 预测均值向量）
- Hessian: H = ∇²ℓ = -Xᵀ W X

  W = diag(Var[Y₁], ..., Var[Yₙ]) — **对角权重矩阵**，随迭代变化！

### IRLS 迭代公式

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   θ^(t+1) = (Xᵀ W^(t) X)⁻¹ Xᵀ W^(t) z^(t)                │
│                                                           │
│   其中:                                                    │
│     W^(t) = diag( Var[Yⁿ | θ^(t)] )    ← 权重矩阵          │
│     z^(t) = Xθ^(t) + (W^(t))⁻¹ (y - μ^(t))  ← 工作响应     │
│                                                           │
│   这恰好是加权最小二乘 (Weighted Least Squares) 的解!       │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### IRLS 的名字由来

```
迭代 (Iteratively)  : 多次迭代直到收敛
重加权 (Reweighted) : 每步用新的 W 矩阵重新加权每个样本
最小二乘 (Least Squares) : 每步求解一个加权最小二乘问题
```

### 直觉

```
IRLS 做的事:
  当前猜测 θ → 预测 μ → 算每个样本的"可信度"W
  → 更可信的样本权重更高 → 用加权LS更新θ
  → 重复...

这就是一种 Fisher Scoring (用期望Hessian替代真实Hessian的牛顿法)
```

### 对逻辑回归的具体形式

```
逻辑回归: Y ~ Bernoulli(μ), μ = σ(θᵀx), σ(z) = 1/(1+e^(-z))

Wⁿⁿ = μⁿ(1 - μⁿ)     ← 预测越不确定 (μ≈0.5), 权重越大!
zⁿ  = θᵀ xⁿ + (yⁿ - μⁿ) / (μⁿ(1 - μⁿ))

每步 = 加权线性回归, 权重随预测变化
```

---

## 概念 5：EM 算法 — 期望最大化 (🔑🔑🔑)

### 动机：隐变量问题

MLE 在完全观测数据上很好用。但实际中常有**隐变量**（latent variables）：

```
例 1: 高斯混合模型 (GMM)
  观测 xⁿ，隐变量 zⁿ ∈ {1,2,...,K} 表示属于哪个分量
  → 如果知道 zⁿ, MLE 很简单 (就是 K 个高斯各自的 MLE)
  → 但 zⁿ 未知!

例 2: 贝叶斯网络中部分节点未观测
  观测到部分变量，其余是隐藏的
```

### EM 的两个步骤

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   E-Step (Expectation):                                   │
│     计算隐变量的后验分布, 给定当前参数:                    │
│     Q(zⁿ) = P(zⁿ | xⁿ, θ^(t))                            │
│                                                           │
│     然后构造"完整数据对数似然"的期望:                      │
│     Q(θ | θ^(t)) = E_{Z|X,θ^(t)} [log P(X,Z | θ)]       │
│                                                           │
│   M-Step (Maximization):                                  │
│     最大化 Q 函数, 得到新的参数:                           │
│     θ^(t+1) = argmax_θ Q(θ | θ^(t))                      │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### EM 不是魔法 — 它保证什么？

```
关键定理: ℓ(θ^(t+1)) ≥ ℓ(θ^(t))

每次迭代后, (不完整的) 对数似然单调不减!

但不保证找到全局最优 — 可能陷入局部极值。
→ 实践中需要多次随机初始化
```

### 为什么 EM 有效？— Jensen 不等式视角

```
log P(X | θ) = log Σ_Z P(X, Z | θ)
             = log Σ_Z q(Z) × P(X,Z|θ)/q(Z)       ← 任意 q
             ≥ Σ_Z q(Z) × log P(X,Z|θ)/q(Z)       ← Jensen (log是凹函数)
             = L(q, θ)  ← ELBO (Evidence Lower BOund)

E-Step:  固定 θ, 令 q(Z) = P(Z|X,θ) → 不等式取等号 (ELBO = log似然)
M-Step:  固定 q, 最大化 L → 提升 log似然的下界

本质: EM = 坐标上升法, 交替优化 q 和 θ
```

### EM 的图示

```
    ℓ(θ)
     ↑
     │    ╭──────────────╮
     │   ╱  M-step  ↑    ╲
     │  ╱           │     ╲
     │ ╱  ┌─────┐   │  ┌──╲────
     │╱   │E-step│  │  │   ╲
     │    └─────┘  │  │    ╲
     └─────────────┴──┴──────→ θ
```

---

## 概念 6：K-Means = EM 的特例 (硬分配)

### K-Means 算法回顾

```
输入: 数据 X = {x¹,...,xᴺ}, 聚类数 K

重复:
  1. 分配 (Assignment): 每个点分配给最近的中心
     zⁿ = argmin_k ||xⁿ - μ_k||²

  2. 更新 (Update): 重新计算每个类的中心
     μ_k = (1/N_k) Σ_{n: zⁿ=k} xⁿ

直到分配不再改变
```

### K-Means ⟺ EM 的对应关系

| 概念 | K-Means | GMM + EM |
|------|---------|----------|
| 隐变量 zⁿ | 哪个类 (硬分配) | 哪个类 (软分配/概率) |
| E-Step | zⁿ = argmin_k ‖xⁿ - μ_k‖² | γ_{nk} = P(zⁿ=k \| xⁿ) (后验概率) |
| M-Step | μ_k = 类内均值 | μ_k, Σ_k, π_k 的加权 MLE |
| 协方差 | 隐含 Σ = I (球形, 等大小) | 学习完整的 Σ_k |
| 收敛 | 局部最优 | 局部最优 |

### K-Means 作为极限情况

```
GMM with EM, 取极限 Σ_k = σ² I, σ² → 0:

E-Step: γ_{nk} → {0, 1}   ← 后验变成"硬"的 (0或1)
M-Step: 加权平均 → 普通平均  ← 因为权重是0/1

→ 退化成了 K-Means!
```

> 🎯 **核心洞见**: K-Means = GMM EM 在**球形协方差 + σ²→0** 极限下的退化。理解了这个，就知道为什么 K-Means 倾向于产生球形、等大的类 — 这是它隐含的模型假设。

---

## 📋 全部概念一张表

| 概念 | 一句话 | 核心公式 |
|------|--------|----------|
| **MLE** | 选使观测数据最可能发生的参数 | θ* = argmax Σ log P(xⁿ\|θ) |
| **充分统计量** | 总结数据所有信息的最简形式 | T(x) 使 P(x\|θ) ∝ exp(η·T(x)) |
| **多元高斯** | 最常用的连续分布, μ 定位置, Σ 定形状 | N(x\|μ,Σ) |
| **指数族** | MLE 有唯一最优的分布族 | P(x\|η) = h(x)exp(ηᵀT(x)-A(η)) |
| **GLM** | 把线性模型推广到任意指数族响应 | g(μ) = θᵀx |
| **连接函数** | 把线性预测器 η 映射到均值 μ | μ = g⁻¹(η) |
| **正则连接** | 使 η=θᵀx 直接成为自然参数 | g = (A')⁻¹ (canonical link) |
| **IRLS** | 用加权LS迭代求解GLM的MLE | θ^(t+1) = (XᵀWX)⁻¹XᵀWz |
| **工作响应 z** | IRLS 每步的"伪目标变量" | z = Xθ + W⁻¹(y-μ) |
| **EM 算法** | 有隐变量时的 MLE（交替E步M步） | Q(θ\|θᵗ) = E[log P(X,Z\|θ)] |
| **E-Step** | 推断隐变量的后验分布 | q(z) = P(z\|x, θᵗ) |
| **M-Step** | 用推断的"软标签"做 MLE | θᵗ⁺¹ = argmax Q |
| **ELBO** | ln似然的下界, EM 在最大化它 | L(q,θ) ≤ log P(X\|θ) |
| **K-Means** | 硬分配 + 球形等协方差的极限 EM | zⁿ = argmin‖xⁿ-μ_k‖² |

---

## 🔗 概念关系图

```
                        L5: 参数估计
                    给定图结构 + 数据 → θ*
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
   完全观测数据           部分观测数据          无标签数据
   (无隐变量)            (有隐变量)            (聚类)
        │                   │                   │
        ▼                   ▼                   ▼
      MLE               EM 算法              K-Means
   Σ log P(x|θ)       E步: 推断z           硬分配
        │              M步: 用软标签做MLE   + 球形假设
        │                   │                   │
        ├── 闭式解? ────────┼── 一般无闭式    │
        │   (高斯:有)       │   解, 需迭代      │
        │                   │                   │
        ▼                   ▼                   ▼
   指数族 MLE            IRLS              EM 的特例
   凹似然 →              (GLM拟合用)        (σ²→0 极限)
   唯一全局最优
        │
        ▼
   多元高斯 MLE:
   μ*=样本均值, Σ*=样本协方差
```

---

## 🎯 核心洞见

| # | 洞见 |
|---|------|
| 1 | **MLE = 统计推断的基础砖块** — 几乎每个学习算法底层都是某种 MLE (或 MAP) |
| 2 | **指数族 = MLE 的"甜区"** — 似然是凹的, 唯一全局最优, 充分统计量低维 |
| 3 | **GLM = 同一模板, 不同分布** — 换响应分布 + 连接函数 = 换一个模型, 但推导/推断框架完全复用 |
| 4 | **IRLS = 每步加权最小二乘** — 把非线性 MLE 变成一连串加权 LS, 实现简单且收敛快 |
| 5 | **EM = 交替填补 + 重估** — E步推断隐变量, M步用"完整数据"重估参数 |
| 6 | **K-Means = EM 的退化版** — 硬分配 + 球形协方差 → 简单但有限制 |

---

## 📊 方法速查：怎么解 MLE？

| 情况 | 方法 | 例子 |
|------|------|------|
| 完全观测, 指数族, 正则连接 | **闭式解** | 高斯 μ,Σ; 多项分布 |
| 完全观测, GLM, 非正则连接 | **IRLS** (牛顿法) | 逻辑回归, 泊松回归 |
| 有隐变量 | **EM** | GMM, HMM, 含缺失数据的贝叶斯网络 |
| 无标签 + 球形假设 | **K-Means** | 硬聚类 |
| 无标签 + 全协方差 | **EM for GMM** | 软聚类 |

---

## 🧪 自测清单（看 L5 前带着这些问题）

- [ ] MLE 的基本步骤是什么？什么情况下有闭式解？
- [ ] 多元高斯分布的 MLE 解是什么？为什么需要 N ≥ d？
- [ ] 指数族的三个组件 (h, T, A) 分别起什么作用？
- [ ] GLM 的三个组件 (随机分量, 系统分量, 连接函数) 分别是什么？
- [ ] 正则连接函数 (canonical link) 有什么特殊性质？
- [ ] IRLS 的名字中每个词各对应什么含义？
- [ ] EM 算法中 E 步在做什么？M 步在做什么？
- [ ] 为什么 EM 保证似然单调不减？（Jensen 不等式怎么用？）
- [ ] K-Means 和 EM for GMM 的对应关系是什么？
- [ ] 什么时候该用 EM，什么时候该直接用 MLE？

---

> L6 预告: L5 是"频域"视角 (MLE, 点估计)。L6 将进入**贝叶斯参数估计** — 给参数加先验, 从 MAP 到全贝叶斯推断, 以及共轭先验的便利性。
