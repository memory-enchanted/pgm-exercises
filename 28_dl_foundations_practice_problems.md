# CMU 10-708 Lecture 11 课后练习 & 答案

> 配套教材: Goodfellow et al. (2016) Deep Learning Ch.6-8, Bishop Ch.5, Hastie et al. ESL Ch.11
>
> 题目分为三级: 🟢 基础 | 🟡 进阶 | 🔴 挑战

---

## 🟢 基础题 (必须掌握)

### Q1. 损失函数的统计对应

**(A)** MSE Loss: L = (1/N) Σ (y_i - f(x_i))² 对应什么统计假设?

**(B)** Cross-Entropy Loss: L = -Σ y_i log p_i 对应什么统计假设?

**(C)** 给 MSE Loss 加 L2 正则: L + λ||w||²。从 MAP 角度解释这等价于什么先验?

<details>
<summary>点击查看答案</summary>

**(A) MSE = MLE with Gaussian noise:**

假设 y = f(x) + ε, ε ~ N(0, σ²)

```
P(y|x) = N(y | f(x), σ²)
       ∝ exp(-(y - f(x))² / (2σ²))

log P(y_1,...,y_N | x_1,...,x_N)
  = -N log σ - N/2 log(2π) - (1/2σ²) Σ (y_i - f(x_i))²

最大化 log likelihood ⟺ 最小化 Σ (y_i - f(x_i))² = MSE
```

**(B) Cross-Entropy = MLE with Categorical output:**

假设 y|x ~ Categorical(p_1, ..., p_K)

```
P(y=k | x) = p_k

log P(y_1,...,y_N | x_1,...,x_N)
  = Σ_i log p_{y_i}
  = Σ_i Σ_k 1[y_i=k] log p_k

最大化 ⟺ 最小化 -Σ_i Σ_k y_{ik} log p_{ik} = Cross-Entropy
```

**(C) L2 Regularization = Gaussian Prior (MAP):**

```
w ~ N(0, 1/(2λ) · I)  (prior)

P(w) ∝ exp(-λ ||w||²)

MAP: max_w log P(y|X,w) + log P(w)
    = max_w -MSE - λ||w||²
    = min_w MSE + λ||w||²
```

L2 正则化 = 给权重加 Gaussian 先验 → MAP 估计。

</details>

---

### Q2. 反向传播的手算

对简单的计算图: L = (w·x + b - y)², 其中 w=2, x=3, b=1, y=10。

**(A)** 画计算图, 标注中间变量。

**(B)** 从 L 反向计算 ∂L/∂w, ∂L/∂b, ∂L/∂x。验证 ∂L/∂w + ∂L/∂b + ∂L/∂x 的意义。

<details>
<summary>点击查看答案</summary>

**(A) 计算图:**

```
x=3 ─┐
      ├→ [×] → z₁=6 ─┐
w=2 ─┘                ├→ [+] → z₂=7 ─→ [-] → r=-3 → [²] → L=9
b=1 ──────────────────┘             y=10 ─┘
```

**(B) 反向计算:**

前向: z₁ = w·x = 6,  z₂ = z₁ + b = 7,  r = z₂ - y = -3,  L = r² = 9

反向:
```
∂L/∂r = 2r = -6
∂L/∂z₂ = ∂L/∂r · ∂r/∂z₂ = -6 · 1 = -6
∂L/∂y  = ∂L/∂r · ∂r/∂y  = -6 · (-1) = 6
∂L/∂z₁ = ∂L/∂z₂ · ∂z₂/∂z₁ = -6 · 1 = -6
∂L/∂b  = ∂L/∂z₂ · ∂z₂/∂b  = -6 · 1 = -6
∂L/∂w  = ∂L/∂z₁ · ∂z₁/∂w  = -6 · x = -6 · 3 = -18
∂L/∂x  = ∂L/∂z₁ · ∂z₁/∂x  = -6 · w = -6 · 2 = -12
```

验证: 直接计算 L = (2·3 + 1 - 10)² = (-3)² = 9

dL/dw = 2(wx+b-y)·x = 2·(-3)·3 = -18 ✓
dL/db = 2(wx+b-y)·1 = 2·(-3)·1 = -6 ✓
dL/dx = 2(wx+b-y)·w = 2·(-3)·2 = -12 ✓

</details>

---

### Q3. Momentum 的指数移动平均

Momentum: v_t = β v_{t-1} + (1-β) g_t  (有时写 v_t = β v_{t-1} + g_t)

**(A)** 展开 v_t, 展示它是过去梯度的指数加权平均。

**(B)** β=0.9 时, 过去第 k 步的梯度贡献衰减到什么程度? (有效窗口 ≈ ?)

<details>
<summary>点击查看答案</summary>

**(A) 展开:**

```
v_t = β v_{t-1} + g_t
    = g_t + β g_{t-1} + β² g_{t-2} + ... + β^t g_0

(假设 v₀ = 0, 采用 v_t = β v_{t-1} + g_t 版本)
```

这是过去所有梯度的**指数衰减加权和** → 最新的梯度权重最大。

**(B) 有效窗口:**

```
β^k < 1/e → k > -1/ln(β) ≈ 1/(1-β)  (for β ≈ 1)

β=0.9: 有效窗口 ≈ 1/(1-0.9) = 10 步
β=0.99: 有效窗口 ≈ 100 步
```

β 越大 → 窗口越长 → 方向更平滑但响应慢。

</details>

---

### Q4. Dropout 的训练/测试差异

**(A)** Dropout 在训练时以概率 p 随机丢弃神经元。在测试时怎么处理? 为什么?

**(B)** 如果训练时用了 dropout(p=0.5), 测试时忘记乘 (1-p), 会发生什么?

<details>
<summary>点击查看答案</summary>

**(A) 测试时的处理:**

训练时: 每个神经元以概率 p 被 mask → 输出期望值缩小了 (1-p) 倍。

测试时: **不** dropout, 但所有权重乘以 (1-p)。

原因: 测试时使用"平均模型" (所有 2^N 个子网络的集成):
```
E[h] = (1-p)·E_{full}[h] + p·0 = (1-p)·E_{full}[h]

为匹配训练时的期望输出 → 测试时乘 (1-p)
```

等价实现 (inverted dropout): 训练时除以 (1-p), 测试时不变。

**(B) 忘记乘 (1-p) 的后果:**

测试时所有神经元都激活 → 每层输出是训练时的 1/(1-p) 倍。

对于 p=0.5: 输出是训练时的 2 倍 → 各层激活值暴涨 → 分类器输出极端概率 (过于自信) → 准确率可能暴跌。

例如: 一个原本输出 0.6 的 sigmoid, 由于输入放大 2 倍, 可能变成 0.9 → 校准 (calibration) 严重失效。

</details>

---

## 🟡 进阶题

### Q5. Adam 的三个状态变量

**(A)** Adam 维护了哪两个指数移动平均? 各自的 β 通常取多少?

**(B)** 为什么 Adam 需要**偏差校正** (bias correction)? 写出校正公式。

**(C)** Adam 的更新规则 θ ← θ - η·m̂/(√ŝ + ε) 中, m̂/√ŝ 的单位是什么? 为什么这个"自适应"步长有效?

<details>
<summary>点击查看答案</summary>

**(A) 两个 EMA:**

```
m_t = β₁ m_{t-1} + (1-β₁) g_t     ← 一阶矩 (momentum)
s_t = β₂ s_{t-1} + (1-β₂) g_t²    ← 二阶矩 (RMSprop)

典型值: β₁=0.9, β₂=0.999
```

**(B) 偏差校正:**

在 t 很小时, m_t 和 s_t 偏向 0 (因为初始化为 0, 且 β 接近 1):

```
E[m_t] = (1 - β₁^t) · E[g_t]  → 需要除以 (1 - β₁^t)

m̂_t = m_t / (1 - β₁^t)
ŝ_t = s_t / (1 - β₂^t)
```

当 t 大时: β₁^t → 0, 校正因子 → 1 → 几乎无影响。

**(C) m̂/√ŝ 的单位:**

g 的单位是 ∂L/∂θ [loss/param]。
m 的单位也是 [loss/param]。
s 的单位是 [loss²/param²] → √s 的单位也是 [loss/param]。

m̂/√ŝ 是**无量纲**的! → 更新的数量级与参数尺度无关!

这意味着 Adam 自动做 per-dimension 学习率调整 → 各维度尺度差异大时特别有效 (如稀疏梯度、不同层)。

</details>

---

### Q6. 偏差-方差分解的推导

**(A)** 推导: E[(y - f̂)²] = σ² + Bias[f̂]² + Var[f̂], 其中 f̂ 在随机训练集上变化。

**(B)** 在深度学习中, 什么操作主要降低 Bias? 什么操作主要降低 Variance?

<details>
<summary>点击查看答案</summary>

**(A) 推导:**

令 f̂ = f̂(x₀; D) 依赖随机训练集 D, f* = E[y|x₀] (真实期望)。

```
E_D[(f̂ - f*)²]
  = E_D[(f̂ - E[f̂] + E[f̂] - f*)²]
  = E_D[(f̂ - E[f̂])²] + (E[f̂] - f*)² + 2 E_D[(f̂ - E[f̂])(E[f̂] - f*)]
                                                    └── = 0 ──┘
  = Var[f̂] + Bias[f̂]²
```

加上不可约噪声:
```
E[(y - f̂)²] = E[(y - f* + f* - f̂)²]
            = E[(y - f*)²] + E[(f̂ - f*)²]
            = σ² + Var[f̂] + Bias[f̂]²
```

**(B) 降低 Bias vs Variance:**

| 降低 Bias (欠拟合→更好) | 降低 Variance (过拟合→更好) |
|------------------------|---------------------------|
| 更大模型 (更深/更宽) | 更多数据 |
| 更长训练 | L1/L2 正则化 (weight decay) |
| 更好的架构 | Dropout |
| 降低正则化强度 | Early Stopping |
| 更好的优化器 | Data Augmentation |
| | Batch Normalization |
| | Ensembling |

</details>

---

### Q7. 反向传播与 PGM 消息传递的对应

**(A)** L5 Belief Propagation 的消息公式和反向传播的梯度传递有什么结构上的相似性?

**(B)** 计算图 (computation graph) 和因子图 (factor graph) 有什么对应关系?

<details>
<summary>点击查看答案</summary>

**(A) 结构相似性:**

BP 消息: m_{i→j}(X_j) = Σ_{X_i} ψ_{ij} × ∏_{k∈N(i)\j} m_{k→i}(X_i)

反向传播: ∂L/∂a_i = Σ_{j∈children(i)} ∂L/∂a_j × ∂a_j/∂a_i

共同结构:
```
节点收集来自"其他邻居/子节点"的信息 → 聚合 → 发给下一个邻居/父节点

BP:    "消息" = 边际概率信息, 聚合 = 乘积+求和
BProp: "消息" = 梯度,        聚合 = 求和+Jacobian乘积
```

两者都是 **collect → aggregate → propagate** 的图算法!

**(B) 因子图 ↔ 计算图:**

| 因子图 | 计算图 |
|--------|--------|
| 变量节点 (circle) | 数据/参数节点 |
| 因子节点 (square) | 操作节点 (+, ×, ReLU) |
| 边: 变量参与因子 | 边: 数据流入/流出操作 |
| 消息: 概率因子 (向量) | 消息: 梯度 (同维度向量) |
| Sum-Product / Max-Product | Chain Rule (梯度反向) |

核心区别: BP 做 Σ/Π (离散), BP 做 Σ ∂·/∂· (连续, 自动微分)。

</details>

---

## 🔴 挑战题

### Q8. SGD 的收敛理论

**(A)** 对于凸函数 f(θ), SGD 满足 E[||θ_t - θ*||²] ≤ ? 写出依赖 η_t 的收敛率。

**(B)** 对于**非凸**函数 (深度学习), SGD 的收敛保证是什么? 为什么"鞍点"比"局部最优"更关键?

<details>
<summary>点击查看答案</summary>

**(A) 凸函数 SGD 收敛:**

对 μ-强凸, L-光滑的 f, SGD 以递减步长 η_t = 1/(μt):

```
E[f(θ̄_T) - f(θ*)] ≤ O(σ²/(μT))
```

其中 σ² 是梯度噪声方差, T 是迭代数。

收敛率: O(1/T) — 与 batch GD 相同 (但有更大的常数)。

**(B) 非凸 SGD:**

SGD 保证: E[||∇f(θ)||²] → 0 (梯度趋于 0) — 找到驻点 (stationary point)。

**为什么鞍点 > 局部最优:**

高维中 (D > 100):
- 局部最优的数量随 D 指数增长? 不 — 大多数驻点是**鞍点**!
- 在高维随机 Gaussian 场中, 局部最优的概率 ~ e^{-cD} → 极小
- 鞍点: 某些方向是极大, 某些方向是极小 → 占了绝大多数驻点

SGD 的噪声**帮助逃离鞍点**: 确定性 GD 可能卡在鞍点 (梯度=0 但 Hessian 有负特征值), SGD 的随机噪声可以"震出来"。

这就是深度学习能工作的原因之一 — SGD 自动找到"好的"解。

</details>

---

### Q9. Batch Normalization 的统计作用

**(A)** BatchNorm 对每个 mini-batch: x̂ = (x - μ_B)/√(σ²_B + ε), y = γ x̂ + β。μ_B 和 σ²_B 是什么?

**(B)** 为什么 BatchNorm 允许使用更大的学习率? 从"内部协变量偏移"(ICS) 和"损失面平滑"两个角度解释。

<details>
<summary>点击查看答案</summary>

**(A) μ_B 和 σ²_B:**

```
μ_B = (1/B) Σ_{i=1}^B x_i        ← mini-batch 均值
σ²_B = (1/B) Σ_{i=1}^B (x_i - μ_B)²  ← mini-batch 方差

γ, β 是可学习参数 (恢复表达能力)
```

**(B) 为什么允许更大 LR:**

**ICS 角度 (原始论文):**
每层输入分布随训练持续变化 → 后面层需要不断适应 → 需要小 LR。
BatchNorm 固定每层输入的均值和方差 → 减少分布漂移 → 允许更大 LR。

**损失面平滑 (Santurkar et al. 2018):**
BatchNorm 使得损失面对参数的变化更平滑:
- ||∇f|| 更小的 Lipschitz 常数
- ∇²f 的 β-smoothness 改善
→ 更大的可行学习率, 更快的收敛

实际效果: BatchNorm 使 loss 曲面"更球形" → 梯度方向更指向最优 → SGD 更高效。

</details>

---

### Q10. 深度学习中的"Benign Overfitting"

**(A)** 经典偏差-方差理论预言: 模型参数 >> 训练样本 → 过拟合 → 测试误差大。但深度学习中常观察到: 参数远超样本数时, 测试误差反而下降! 这是什么现象?

**(B)** 解释 "double descent" 曲线: 为什么测试误差在 interpolation threshold 处先升后降?

<details>
<summary>点击查看答案</summary>

**(A) Benign Overfitting (良性过拟合):**

经典理论基于"模型容量 ↑ → 方差 ↑" 的单调关系。

深度学习反例: 过度参数化 (over-parameterized) 模型可以完美拟合训练数据 (训练误差=0), 同时仍有很好的测试性能。

原因:
- **隐式正则化**: SGD 的动力学在过参数化模型中隐式地偏好"简单"解 (最小范数解)
- **Margin maximization**: 在分类中, SGD 倾向于找最大间隔分类器 → 泛化好
- **Interpolation**: 过参数化时, 存在无穷多个完美拟合的解, SGD 选的是"最平滑"那个

**(B) Double Descent:**

```
测试误差
  │  \                  /
  │   \                /
  │    \              /
  │     \    ┌──┐    /
  │      \   │峰│   /
  │       ───┘  └───
  └──────────────────────→ 模型容量/参数量
       ↗      ↗      ↗
   欠拟合  临界点  过参数化
          (interpolation
           threshold)
```

在 interpolation threshold (刚好能拟合全部训练数据): 只有一个解 → 这个解可能是"坏"的 (被噪声扭曲) → 测试误差尖峰。

超过临界点: 无穷多个解 → SGD 隐式正则化选出最优 → 测试误差下降。

这是对传统偏差-方差理论的修正 — 在过参数化区域, "更多参数"反而降低方差!

</details>

---

## 📊 综合自测评分

每题 10 分，共 100 分。

| 得分 | 评价 |
|------|------|
| 90-100 | L11 完全掌握, 已理解 DL 的统计基础和算法核心 |
| 70-89  | 主干扎实, 建议动手实现反向传播验证理解 |
| 50-69  | 概念清晰, 回去推一遍 MSE↔MLE 和偏差-方差推导 |
| < 50   | 先吃透 Q1-Q4, 确保理解损失函数、反向传播、正则化 |

---

> L11 完成了 PGM → DL 的桥梁。理解这些统计和算法基础后, 你将能以统一视角看待概率图模型和深度神经网络。
