# CMU 10-708 Lecture 16 课后练习 & 答案 — 结构学习

> 配套教材: Koller & Friedman Ch.18, Friedman et al. (2008), Meinshausen & Bühlmann (2006), Kolar et al. (2010)
>
> 题目覆盖 L16 五大主题, 三级: 🟢 基础 | 🟡 进阶 | 🔴 挑战

---

# 第一部分: 高斯图模型 (GGM)

---

## 🟢 Q1. 精度矩阵与条件独立

给定 3 维高斯随机向量 X = (X₁, X₂, X₃) ~ N(0, Σ), 精度矩阵 Ω = Σ⁻¹:

```
Ω = [[ 2, -1,  0],
     [-1,  2, -1],
     [ 0, -1,  2]]
```

**(A)** 画出对应的无向图结构。哪些变量对是条件独立的?

**(B)** 计算协方差矩阵 Σ = Ω⁻¹。验证 Σ₁₃ ≠ 0, 尽管 Ω₁₃ = 0。

**(C)** 解释为什么 Σ₁₃ ≠ 0 而 Ω₁₃ = 0。这说明了什么?

<details>
<summary>点击查看答案</summary>

**(A) 图结构:**

```
Ω 的非对角非零元: Ω₁₂ = -1, Ω₂₃ = -1
                             Ω₁₃ = 0 → 无边!

图: X₁ — X₂ — X₃  (链式)

条件独立性:
  X₁ ⟂ X₃ | X₂  (给定中间变量, 首尾独立)
  X₁ ⟂̸ X₃ 无条件
```

**(B) 协方差矩阵:**

```
Σ = Ω⁻¹

det(Ω) = 2(4-1) + 1(-2) = 2·3 - 2 = 4

用伴随矩阵法:
  Σ₁₁ = (Ω₂₂Ω₃₃ - Ω₂₃²)/det = (4-1)/4 = 3/4 = 0.750
  Σ₂₂ = (Ω₁₁Ω₃₃ - Ω₁₃²)/det = (4-0)/4 = 4/4 = 1.000
  Σ₃₃ = (Ω₁₁Ω₂₂ - Ω₁₂²)/det = (4-1)/4 = 3/4 = 0.750
  Σ₁₂ = -(Ω₁₂Ω₃₃ - Ω₁₃Ω₂₃)/det = -((-1)·2 - 0)/4 = 2/4 = 0.500
  Σ₂₃ = -(Ω₁₁Ω₂₃ - Ω₁₂Ω₁₃)/det = -(2·(-1) - 0)/4 = 2/4 = 0.500
  Σ₁₃ = (Ω₁₂Ω₂₃ - Ω₁₁Ω₁₃Ω₂₂?)/det

  更直接: Σ₁₃ = (Ω₁₂·Ω₂₃ - Ω₁₃·Ω₂₂的补?)/det

  用公式: Σ₁₃ = (Ω₁₂Ω₂₃?)/|Ω| 的余子式

  实际计算:
  Σ = [[ 3/4, 1/2, 1/4],
       [ 1/2,   1, 1/2],
       [ 1/4, 1/2, 3/4]]

验证 Ω @ Σ = I:
  行1: 2·3/4 + (-1)·1/2 + 0·1/4 = 1.5 - 0.5 + 0 = 1 ✓
  行1·列2: 2·1/2 + (-1)·1 + 0·1/2 = 1 - 1 + 0 = 0 ✓
  行1·列3: 2·1/4 + (-1)·1/2 + 0·3/4 = 0.5 - 0.5 + 0 = 0 ✓

所以: Σ₁₃ = 1/4 = 0.250

确认: Σ₁₃ ≠ 0, 而 Ω₁₃ = 0!
```

**(C) 解释:**

```
Ω₁₃ = 0 意味着 X₁ ⟂ X₃ | X₂ (给定 X₂, X₁ 和 X₃ 独立)
        这是"净"关联: 扣除 X₂ 的影响后, X₁ 和 X₃ 无直接关联

Σ₁₃ = 0.25 ≠ 0 因为:
  X₁ 和 X₃ 通过 X₂ 间接关联 (X₁↔X₂↔X₃)
  Σ 捕获了这种间接效应!

直觉:
  如果 X₁ 和 X₂ 相关, X₂ 和 X₃ 相关
  → 即使 X₁ 和 X₃ 没有直接关联, 它们也会因为 X₂ 而"看起来"相关
  → Σ₁₃ = Cov(X₁,X₃) 包含了这条间接路径的贡献

核心教训: Ω 定义图结构 (条件独立), Σ 定义边缘关联
          两者不同! 结构学习 = 估计 Ω 的零模式
```

</details>

---

## 🟢 Q2. 偏相关系数

沿用 Q1 的 Ω 和 Σ。

**(A)** 计算偏相关系数 ρ_{12|3} 和 ρ_{13|2}。

**(B)** ρ_{13|2} 是多少? 为什么? 与 Ω₁₃ 的关系是什么?

<details>
<summary>点击查看答案</summary>

**(A) 偏相关系数:**

公式: ρ_{ij|rest} = -Ω_ij / √(Ω_ii · Ω_jj)

```
ρ_{12|3} = -Ω₁₂ / √(Ω₁₁ · Ω₂₂)
         = -(-1) / √(2 · 2)
         = 1/2 = 0.500

(给定 X₃ 后, X₁ 和 X₂ 仍有正偏相关)

ρ_{13|2} = -Ω₁₃ / √(Ω₁₁ · Ω₃₃)
         = -0 / √(2 · 2)
         = 0

ρ_{23|1} = -Ω₂₃ / √(Ω₂₂ · Ω₃₃)
         = -(-1) / √(2 · 2)
         = 1/2 = 0.500
```

**(B) ρ_{13|2} = 0:**

```
因为 Ω₁₃ = 0!

一般地: ρ_{ij|rest} = 0 ⇔ Ω_ij = 0 ⇔ X_i ⟂ X_j | rest

这是 GGM 的核心定理:
  偏相关系数为 0 ⇔ 精度矩阵非对角元为 0 ⇔ 无边

所以结构学习可以简化为: 寻找偏相关 = 0 的变量对!
(实践中用正则化估计, 因为样本偏相关几乎永远不会精确为 0)
```

</details>

---

# 第二部分: Graphical Lasso

---

## 🟡 Q3. Graphical Lasso 目标函数

**(A)** 写出 Graphical Lasso 的优化目标, 解释三项各自的作用。

**(B)** λ=0 和 λ→∞ 时, 分别得到什么解?

**(C)** 为什么用 ℓ1 而不是 ℓ2 惩罚? ℓ2 会产生稀疏解吗?

<details>
<summary>点击查看答案</summary>

**(A) 目标函数:**

```
min_{Ω≻0}  -log det(Ω) + tr(SΩ) + λ · Σ_{i≠j} |Ω_ij|

① -log det(Ω): 来自高斯密度的 log 行列式项
   作用: 防止 Ω 退化 (det→0 = 方差无限大)
   凸函数! (在正定锥上)

② tr(SΩ): 数据拟合项
   作用: 让 Ω 拟合经验协方差 S
   tr(SΩ) = Σ_{i,j} S_ij·Ω_ij — 类似"内积"
   
   如果 Ω = S⁻¹: tr(S·S⁻¹) = p (完美拟合)
   但 S⁻¹ 通常稠密!

③ λ·Σ|Ω_ij|: ℓ1 正则化 (只罚非对角元)
   作用: 鼓励 Ω_ij → 0 → 稀疏图
   λ 控制稀疏程度

三项都是凸函数 → 整体是凸优化 → 全局最优解唯一!
```

**(B) 极限行为:**

```
λ = 0 (无惩罚):
  目标: -log det(Ω) + tr(SΩ)
  解: Ω̂ = S⁻¹ (当 S 可逆时)
  
  问题: S⁻¹ 通常稠密 (所有 Ω̂_ij ≠ 0)
       → 完全图! 所有节点都相连
       当 p > n: S 奇异, 无法求逆

λ → ∞ (极强惩罚):
  所有 Ω_ij (i≠j) 被压缩为 0
  Ω̂ → 对角阵
  → 空图! 所有节点独立

实际有用的 λ 在两者之间:
  通过 CV / BIC / StARS 选择
```

**(C) ℓ1 vs ℓ2:**

```
ℓ1 (Lasso):    Σ|Ω_ij|        → 角点解 (很多正好为 0)
ℓ2 (Ridge):    Σ Ω_ij²       → 缩小但不为 0 → 非稀疏!

几何直觉 (考虑二维: Ω_12 和 Ω_13):
  ℓ1 约束: |Ω_12| + |Ω_13| ≤ t   → 菱形 (有角点!)
  ℓ2 约束: Ω_12² + Ω_13² ≤ t    → 圆形 (光滑)

  目标函数的等高线与菱形在角点相切
  → 某个坐标正好为 0 的概率 > 0!

ℓ1 的角点 → 稀疏解 → 自动选边
ℓ2 的光滑 → 非零但小 → 所有边都保留 (弱)

这就是为什么 Graphical LASSO 叫 "LASSO" —
它在精度矩阵的估计中使用了 ℓ1 (Lasso) 惩罚!
```

</details>

---

## 🟡 Q4. 坐标下降一步手算

简化的 Graphical Lasso (p=2): Ω = [[ω₁₁, ω₁₂], [ω₂₁, ω₂₂]]。

给定 S = [[1.0, 0.8], [0.8, 1.0]], λ = 0.5, 初始 W = S + λI = [[1.5, 0.8], [0.8, 1.5]]。

**(A)** 写出对 ω₁₂ (第二行第一列, 分块后) 的 Lasso 子问题。

**(B)** 手算一步更新后的 Ω。

<details>
<summary>点击查看答案</summary>

**(A) 分块 & Lasso 子问题:**

```
对 j=1 (更新 Ω 的第 1 行/列):

W = [[1.5, 0.8],    S = [[1.0, 0.8],
     [0.8, 1.5]]         [0.8, 1.0]]

把 j=0 移到最后 (即 j=1 移到最后, 索引: [0,1] → [0,1]):
  W_11 = 1.5 (标量, 因为 p=2, 去掉第2行/列后剩 1×1)
  s_12 = 0.8 (S[0,1] = S[1,0])

Lasso 子问题:
  min_β  ½W_11·β² - s_12·β + λ|β|

  = min_β 0.75·β² - 0.8·β + 0.5|β|

解析解 (软阈值):
  β* = soft(s_12, λ) / W_11
     = soft(0.8, 0.5) / 1.5
     = 0.3 / 1.5
     = 0.2

(soft(0.8, 0.5) = sign(0.8)·max(|0.8|-0.5, 0) = 0.3)
```

**(B) 更新 W 和 Ω:**

```
w_12 = W_11 · β = 1.5 · 0.2 = 0.3

更新 W:
  W[0,1] = 0.3, W[1,0] = 0.3
  (ω₂₂ 也需更新, 但 p=2 时 ω = s₂₂ + λ - β^T W_11 β
        ω₂₂ = 1.0 + 0.5 - 0.3·0.2 = 1.44? 不对, 对角元不变
   
   实际上, W 的对角元不变, 只更新非对角元)

  W_new = [[1.5, 0.3],
           [0.3, 1.5]]

Ω = W_new⁻¹:
  det = 1.5·1.5 - 0.3·0.3 = 2.25 - 0.09 = 2.16
  Ω = (1/2.16) · [[1.5, -0.3],
                   [-0.3, 1.5]]

    Ω₁₁ = Ω₂₂ = 1.5/2.16 = 0.694
    Ω₁₂ = Ω₂₁ = -0.3/2.16 = -0.139

初始 Ω (W_old⁻¹):
  det_old = 1.5·1.5 - 0.8·0.8 = 2.25 - 0.64 = 1.61
  Ω_old = (1/1.61)·[[1.5, -0.8], [-0.8, 1.5]]
        = [[0.932, -0.497], [-0.497, 0.932]]

更新后: |Ω₁₂| 从 0.497 降到 0.139 → 向 0 压缩!
这正是 ℓ1 惩罚的作用 — 将非对角元推向 0。
```

</details>

---

# 第三部分: Neighbor Selection

---

## 🟡 Q5. Neighbor Selection 回归等价性

**(A)** 对节点 1, Neighbor Selection 用 Lasso 回归 X₁ ~ X₂ + X₃ + ... + X_p。
回归系数 β̂ 和精度矩阵 Ω 有什么关系?

**(B)** 证明: 在高斯假设下, 最优回归系数 β*_j = -Ω_{1j} / Ω_{11}。

**(C)** Neighbor Selection 在什么条件下能正确恢复图结构?

<details>
<summary>点击查看答案</summary>

**(A) 回归系数 ↔ 精度矩阵:**

```
Neighbor Selection 做:
  X₁ = Σ_{k≠1} β_{1k}·X_k + ε₁

在高斯假设下:
  β₁* = -Ω_{1,-1} / Ω_{11}

即 β_{1k}* ≠ 0 ⇔ Ω_{1k} ≠ 0 ⇔ (1,k) 有边!

所以:
  Node 1 的 Lasso 回归选出的非零系数 → Node 1 的邻居
  φ(1) = {k: β̂_{1k} ≠ 0}

对所有节点重复 → 组合所有 φ(j) → 完整图结构
```

**(B) 证明:**

```
多元高斯的条件分布:
  X₁ | X_{-1} ~ N(μ_{1|-1}, σ²_{1|-1})

其中:
  μ_{1|-1} = Σ_{1,-1}·(Σ_{-1,-1})⁻¹·x_{-1}
  σ²_{1|-1} = Σ_{11} - Σ_{1,-1}·(Σ_{-1,-1})⁻¹·Σ_{-1,1}

用分块矩阵求逆公式:
  Ω = Σ⁻¹, 分块:
  Σ = [[Σ_{11}, Σ_{1,-1}],
       [Σ_{-1,1}, Σ_{-1,-1}]]

  Ω_{11} = 1/σ²_{1|-1}
  Ω_{1,-1} = -Ω_{11}·Σ_{1,-1}·(Σ_{-1,-1})⁻¹

所以:
  Σ_{1,-1}·(Σ_{-1,-1})⁻¹ = -Ω_{1,-1} / Ω_{11}

这正是回归系数!
  β₁* = Σ_{1,-1}·(Σ_{-1,-1})⁻¹ = -Ω_{1,-1} / Ω_{11}

∎
```

**(C) 正确恢复的条件:**

```
Meinshausen & Bühlmann (2006) 的条件:

1. 稀疏性 (Sparsity):
   每个节点的真实邻居数 deg(j) << p (稀疏图)

2. Incoherence (不相干条件):
   非邻居变量不能和邻居变量"太相关"
   数学: ||Σ_{non-neighbor, neighbor}·Σ_{neighbor}^{-1}||_∞ < 1
   
   直觉: 如果一个非邻居和某个邻居高度相关,
         Lasso 可能会选它 → 假阳性

3. λ 选择:
   λ ∝ √(log p / n)   (理论上最优率)
   实践中用交叉验证

4. 样本量:
   n ≥ C·deg_max·log p  (理论下界)
   其中 deg_max = 最大节点度

在这些条件下: P(N̂(j) = N(j) for all j) → 1 as n→∞
```

</details>

---

## 🟡 Q6. AND vs OR 规则

一个 4 节点图 (p=4), Neighbor Selection 的结果:

| 节点 j | N̂(j) (估计的邻居) |
|--------|---------------------|
| 1 | {2, 4} |
| 2 | {1, 3} |
| 3 | {4} |
| 4 | {1, 2, 3} |

**(A)** 分别用 AND rule 和 OR rule 构建图的边集。

**(B)** 如果真实边是 {(1,2), (2,3), (3,4), (1,4)} (环), 计算两种规则的 TP/FP/FN。

**(C)** 什么场景更适合 AND rule? 什么场景更适合 OR rule?

<details>
<summary>点击查看答案</summary>

**(A) 边集:**

```
OR rule: (i,j) 有边 ⇔ i ∈ N̂(j) OR j ∈ N̂(i)

从估计的邻居:
  1: {2,4} → 候选边 (1,2), (1,4)
  2: {1,3} → 候选边 (1,2), (2,3)
  3: {4}   → 候选边 (3,4)
  4: {1,2,3} → 候选边 (1,4), (2,4), (3,4)

OR 边集 = {(1,2), (1,4), (2,3), (2,4), (3,4)}


AND rule: (i,j) 有边 ⇔ i ∈ N̂(j) AND j ∈ N̂(i)

逐一检查:
  (1,2): 2∈N̂(1) ✓ AND 1∈N̂(2) ✓ → 有边 ✓
  (1,4): 4∈N̂(1) ✓ AND 1∈N̂(4) ✓ → 有边 ✓
  (2,3): 3∈N̂(2) ✓ AND 2∈N̂(3) ✗ → 无边! (不对称)
  (2,4): 4∈N̂(2) ✗ AND 2∈N̂(4) ✓ → 无边!
  (3,4): 4∈N̂(3) ✓ AND 3∈N̂(4) ✓ → 有边 ✓

AND 边集 = {(1,2), (1,4), (3,4)}
```

**(B) TP/FP/FN:**

```
真实边: {(1,2), (2,3), (3,4), (1,4)} = 4 条

OR rule (5 条边):
  TP: (1,2), (2,3), (3,4), (1,4) → 4
  FP: (2,4) → 1
  FN: 无 → 0
  Precision = 4/5 = 0.800
  Recall = 4/4 = 1.000

AND rule (3 条边):
  TP: (1,2), (3,4), (1,4) → 3
  FP: 无 → 0
  FN: (2,3) → 1
  Precision = 3/3 = 1.000
  Recall = 3/4 = 0.750
```

**(C) 场景选择:**

```
AND rule (更保守):
  适用: 需要高 Precision (低假阳性)
        例: 医学基因网络 — 错误边可能导致错误的实验!
        代价: 可能丢失弱边 (低 Recall)

OR rule (更灵敏):
  适用: 需要高 Recall (不想漏掉真实边)
        例: 探索性分析 — 希望找到所有可能的关联
        代价: 可能有假阳性边 (低 Precision)

实践中:
  - 报告两种结果, 让读者了解不确定性
  - 或使用 AND + 对不对称的边做进一步检查
  - 超参数 (λ) 的选择对两种规则都至关重要
```

</details>

---

# 第四部分: 时变结构学习

---

## 🔴 Q7. 时变 Graphical Lasso 的核平滑

时间点 t=1,...,10, 样本在每个时间点独立 (n_t = 50, p=5)。

**(A)** 写出核平滑经验协方差 S^(t) 的公式。用 Epanechnikov 核 K(u) = ¾(1-u²)·1(|u|≤1), 带宽 h=2。

**(B)** 计算 t=3 时 S^(3) 涉及的权重 (假设均匀样本量)。

**(C)** 带宽 h 太小或太大各有什么问题?

<details>
<summary>点击查看答案</summary>

**(A) 核平滑协方差:**

```
S^(t) = Σ_{τ=1}^{T} w_{t,τ} · S_τ

其中 S_τ 是时间 τ 的经验协方差矩阵

权重:
  w_{t,τ} = K_h(|t-τ|) / Σ_s K_h(|t-s|)

  K_h(u) = K(u/h)
  Epanechnikov: K(u) = 0.75(1-u²) for |u|≤1, else 0

  → K_h(|t-τ|) = 0.75(1 - (|t-τ|/h)²) if |t-τ| ≤ h, else 0

h=2: 时间差 > 2 的时间点权重为 0
```

**(B) t=3 时的权重:**

```
h=2 → 只有 |3-τ| ≤ 2 的时间点有非零权重 → τ ∈ {1,2,3,4,5}

时间差和核权重:
  τ=1: |3-1| = 2, K(2/2) = K(1) = 0.75(1-1) = 0
  τ=2: |3-2| = 1, K(1/2) = 0.75(1-0.25) = 0.75×0.75 = 0.5625
  τ=3: |3-3| = 0, K(0) = 0.75(1-0) = 0.75
  τ=4: |3-4| = 1, K(1/2) = 0.5625
  τ=5: |3-5| = 2, K(1) = 0

非零权重: τ=2,3,4
未归一化: w' = [0, 0.5625, 0.75, 0.5625, 0, 0, 0, 0, 0, 0]
归一化: Σ w' = 0.5625+0.75+0.5625 = 1.875

归一化权重:
  w_{3,2} = 0.5625/1.875 = 0.300
  w_{3,3} = 0.75/1.875 = 0.400
  w_{3,4} = 0.5625/1.875 = 0.300

S^(3) = 0.300·S₂ + 0.400·S₃ + 0.300·S₄
```

**(C) 带宽的影响:**

```
h 太小 (如 h=1):
  S^(t) 只用 t-1, t, t+1 三个时间点
  → 估计方差大 (有效样本量 ≈ 3n_t = 150)
  → 图结构随时间波动剧烈 (可能过拟合噪声)
  → 敏感: 能检测快速变化, 但也容易"看到"假变化

h 太大 (如 h=5):
  S^(t) 用几乎所有时间点 (权重分散)
  → 过度平滑 (有效样本量大但损失时间分辨率)
  → 图结构几乎不随时间变化
  → 迟钝: 可能完全错过真实的结构变化!

选择 h:
  - 交叉验证: 留出时间点, 评估预测误差
  - 先验知识: 预期变化的速度 (秒 vs 天 vs 年)
  - 自适应带宽: 在变化点附近用更小的 h

实际应用 (fMRI): h ≈ 30-60 秒的窗口是常见选择
```

</details>

---

## 🔴 Q8. Fused Lasso 的断点检测

3 个时间点的精度矩阵估计 (p=3):

```
Ω̂^(1) = [[ 1.5, -0.5,  0  ],    Ω̂^(2) = [[ 1.5, -0.1,  0  ],
          [-0.5,  1.5, -0.5],              [-0.1,  1.5, -0.1],
          [ 0,   -0.5,  1.5]]              [ 0,   -0.1,  1.5]]

Ω̂^(3) = [[ 1.5, -0.5, -0.4],
          [-0.5,  1.5, -0.5],
          [-0.4, -0.5,  1.5]]
```

**(A)** 画出每个时间点的图结构 (节点 1-2-3)。

**(B)** 用 Fused Lasso 惩罚 Σ_t ||Ω̂^(t) - Ω̂^(t-1)||₁ 计算相邻图的结构差异。

**(C)** 最大的结构差异在哪里? 这可能意味着什么?

<details>
<summary>点击查看答案</summary>

**(A) 图结构:**

```
t=1:  1——2——3  (链式)
      边: (1,2) [强, -0.5], (2,3) [强, -0.5]

t=2:  1··2··3  (非常弱)
      边: (1,2) [弱, -0.1], (2,3) [弱, -0.1]

t=3:  1——2    (团状)
       \ /
        3
      边: (1,2) [强, -0.5], (2,3) [强, -0.5], (1,3) [新, -0.4]
```

**(B) 结构差异:**

```
Δ(t=1→2) = ||Ω̂^(2) - Ω̂^(1)||₁
          = |(-0.1)-(-0.5)| + |(-0.1)-(-0.5)| + 3×|1.5-1.5| ...
          (非对角元)
          = |0.4| + |0.4| + |0|  (只计 i≠j)
          = 0.4 + 0.4 = 0.8
          (注意上下三角都算: ×2)
          = 0.4×2 + 0.4×2 = 1.6  (?) 

让我们更仔细:
  非对角元之差 (i<j): 上三角
  t=1: Ω_12=-0.5, Ω_13=0, Ω_23=-0.5
  t=2: Ω_12=-0.1, Ω_13=0, Ω_23=-0.1
  t=3: Ω_12=-0.5, Ω_13=-0.4, Ω_23=-0.5

  Δ_12(t=1→2) = |(-0.1)-(-0.5)| = 0.4
  Δ_13(t=1→2) = |0-0| = 0
  Δ_23(t=1→2) = |(-0.1)-(-0.5)| = 0.4
  总和 × 2 (对称) = (0.4+0+0.4)×2 = 1.6

  Δ(t=2→3):
  Δ_12 = |(-0.5)-(-0.1)| = 0.4
  Δ_13 = |(-0.4)-0| = 0.4
  Δ_23 = |(-0.5)-(-0.1)| = 0.4
  总和 × 2 = (0.4+0.4+0.4)×2 = 2.4

差异:
  Δ(1→2) = 1.6  (边强→边弱, 无结构变化)
  Δ(2→3) = 2.4  (边弱→边强 + 新边出现!)
```

**(C) 最大差异:**

```
t=2→t=3 的变化最大 (2.4 > 1.6)

包含两类变化:
  1. 已有边变强: (1,2) 和 (2,3) 从 -0.1 恢复到 -0.5
  2. 新边出现: (1,3) 从 0 变为 -0.4

这可能意味着:
  - t=3 附近存在"结构性断点" (structural breakpoint)
  - t=2 可能是"过渡期"或"异常期"
  - 如果这是 fMRI 数据: t=3 可能对应任务状态切换

Fused Lasso 通过 λ₂ Σ||Ω^(t)-Ω^(t-1)||₁ 惩罚:
  λ₂ 大 → 平滑掉这种差异 (认为它是噪声)
  λ₂ 小 → 保留差异 (识别为结构变化)

选择 λ₂ 的关键: 区分"真正的结构变化"和"随机波动"!
```

</details>

---

# 第五部分: 综合与理论

---

## 🔴 Q9. 高维一致性

**(A)** 对于 Graphical Lasso, 在什么条件下 (n, p, 稀疏度) 估计的 Ω̂ 在 Frobenius norm 下收敛到真实的 Ω?

**(B)** 为什么需要"incoherence condition"? 如果违反会怎样?

**(C)** Meinshausen-Bühlmann vs Graphical Lasso: 两者的渐近方差有什么差异?

<details>
<summary>点击查看答案</summary>

**(A) 收敛条件 (Ravikumar et al., 2011):**

```
设 s = 每个节点的最大邻居数 (图稀疏度)
    d = Ω 的最大非零非对角元数 / 行

在以下条件下:
  1. 稀疏性: s = o(n / log p)
     即 s 不能太大 (图不能太密)
  
  2. 样本量: n ≥ C·s²·log p
     (Graphical Lasso) 或 n ≥ C·s·log p (Neighbor Selection)
  
  3. Incoherence: 非邻居与邻居的"最大相关性" < 1
     数学: |||Σ_{NS}·Σ_{SS}^{-1}|||_∞ < 1 - α (α>0)

  4. λ 选择: λ ≈ c·√(log p / n)

则:
  ||Ω̂ - Ω||_F = O_P(√(s·log p / n)) → 0
  ||Ω̂ - Ω||_∞,∞ = O_P(√(log p / n))
  
  即: 收敛速度接近 √(log p / n) — 几乎和 p 已知的低维情形一样快!

关键: n 不需要 > p! 只需要 n > C·s²·log p
     如果 s=5 (很稀疏), p=10⁴, n≈500 就足够了!
```

**(B) Incoherence condition:**

```
Incoherence 衡量: 真实邻居对"看起来像邻居"的非邻居的干扰程度

数学定义 (简化版):
  对每个节点 j, 分变量为:
    S = N(j) (邻居集)
    C = others (非邻居集)
  
  Incoherence: max_{k∈C} ||Σ_{k,S}·Σ_{SS}^{-1}||₁ < 1
  
  直觉: 非邻居 k 不能用邻居的线性组合来"冒充"

违反意味着:
  存在一个非邻居 k, 它和真实邻居的线性组合高度相关
  → Lasso 可能会选 k 而不是真实邻居
  → 假阳性边! (False Positive)

例: 在一个近似全连接的组件中:
  X₁ 和 X₂ 高度相关, X₂ 和 X₃ 高度相关
  即使 (X₁,X₃) 无边, Lasso 可能选 X₃ 当 X₁ 的邻居
  (因为 X₃ 通过 X₂ 可以很好地预测 X₁!)

这就是为什么高度相关的变量集 (如股票价格) 
对结构学习是挑战 — Incoherence 容易违反。
```

**(C) 渐近方差对比:**

```
Graphical Lasso:
  估计的是全矩阵 Ω → 同时利用了所有变量间的约束
  → 渐近方差更低 (统计高效)
  → 但计算复杂度高 O(p³)

Neighbor Selection:
  逐节点独立回归 → 每次只用了"本地"信息
  → 渐近方差比 GL 高 (统计效率损失)
  → 但计算简单, 可并行 → 可处理巨大 p

量化:
  Var(Ω̂_ij^{GL}) / Var(Ω̂_ij^{NS}) ≈ (1 + c)/(1 + 2c)  (近似)
  其中 c 依赖于图结构
  通常 GL 的方差是 NS 的 0.7~0.9 倍

实践中:
  p < 1000: GL 更准确
  p > 5000: NS 是唯一可行的选择 (GL 太慢)
  两者一起使用: NS 初筛 → GL 精修
```

</details>

---

## 🔴 Q10. 从 GGM 到非高斯 — 结构学习的扩展

**(A)** 如果数据不是高斯的 (如计数数据、类别数据), GGM 框架还适用吗?

**(B)** 简述以下扩展的核心思想:
  - Ising Model 结构学习
  - Nonparanormal (非参数正态变换)
  - Poisson Graphical Model

**(C)** 为什么"非高斯"是现实数据中的常态而非例外?

<details>
<summary>点击查看答案</summary>

**(A) 高斯假设的局限性:**

```
GGM 框架的核心: Ω_ij = 0 ⇔ X_i ⟂ X_j | rest
这个等价性只在多元高斯下成立!

非高斯数据:
  - 计数数据 (如 RNA-seq counts): 非负、离散、过离散
  - 二元数据 (如问卷 Yes/No): 只能取 0/1
  - 混合类型: 连续 + 离散混合

直接用 GGM:
  → Ω_ij 不再能干净地表示条件独立
  → ℓ1 正则化仍可用, 但统计解释变了
  → 可能出现"假"边 (因为非高斯性)
```

**(B) 三种扩展:**

```
1. Ising Model (二元数据):
   X_j ∈ {0,1}
   P(X) ∝ exp(Σ_j θ_j·X_j + Σ_{i<j} θ_{ij}·X_i·X_j)
   
   参数 θ_{ij}: 成对交互项
   θ_{ij} = 0 ⇔ X_i ⟂ X_j | rest (对二元数据!)
   
   结构学习 = 估计稀疏的 θ_{ij} 矩阵
   方法: ℓ1 正则化的逻辑回归 → 伪似然估计 (pseudo-likelihood)
   
   实际上就是: 对每个 X_j, 做 ℓ1-惩罚的逻辑回归!
   (和 Neighbor Selection 同样的思路, 用 logit 替换线性回归)

2. Nonparanormal (非参数正态变换):
   假设: 存在单调变换 f_j 使得 (f₁(X₁), ..., f_p(X_p)) ~ N(0, Σ)
   
   方法:
     Step 1: 对每个变量, 用边际分布估计 f̂_j
             (如用 rank-based 的 normal score 变换)
     Step 2: 在变换后的数据上运行 Graphical Lasso
   
   优势: 保留了 GGM 的理论优雅性, 同时允许任意边际分布
   劣势: 只允许单调变换 → 不适用于真正的离散数据

3. Poisson Graphical Model (计数数据):
   X_j | X_{-j} ~ Poisson(exp(θ_j + Σ_{k≠j} θ_{jk}·X_k))
   
   不能有负的交互! (因为 rate 必须 > 0)
   → θ_{jk} 限制为非负或需特殊参数化
   
   方法: ℓ1-惩罚的 Poisson 伪似然
   应用: RNA-seq 基因共表达网络, 社交网络计数
```

**(C) 非高斯的普遍性:**

```
现实数据中, 真正的高斯数据极为稀少:

  领域         变量类型           分布特征
  ─────────────────────────────────────────
  基因组学     基因表达计数       非负, 偏态, 过离散
  神经科学     fMRI BOLD 信号     近似高斯 (经预处理)
  金融        股票收益率          重尾, 时变波动率
  社交网络    好友数/互动数       计数, 极度偏斜
  医学        诊断指标            混合类型 (连续+二元)
  文本        TF-IDF / BoW       稀疏, 非负, 计数

为什么高斯这么常用?
  ✅ CLT: 大样本下均值趋向高斯
  ✅ 数学上最易处理: 条件独立 = Ω_ij=0
  ✅ 作为"一阶近似"通常不差
  ❌ 但边际分布的偏态/离散性不能被忽视

实践中:
  1. 尝试变换 (log, sqrt, rank-based) → 更接近高斯
  2. 用 Nonparanormal 作为稳健的"中间地带"
  3. 对真正的离散数据, 使用专门的离散图模型 (Ising/Poisson)
  4. GGM 的结果即使不完美, 也常提供有用的"近似网络"
```

</details>

---

## 📊 综合自测评分

每题 10 分, 共 100 分。

| 得分 | 评价 |
|------|------|
| 90-100 | L16 完全掌握, 结构学习的理论和算法已贯通 |
| 75-89  | 主干扎实, 建议跑 42 的代码加深直觉 |
| 60-74  | 概念基本清晰, 重点回顾 Q1 (Σ vs Ω) 和 Q3 (目标函数) |
| < 60   | 先把 Q1-Q4 (GGM + Graphical Lasso 基础) 手算一遍 |

---

> L16 完成了 PGM 体系从表示到学习的完整闭环:
> 如果图已知 → 做推断 (L4, L7-L10)
> 如果参数未知 → 做参数估计 (L5)
> 如果图也未知 → 做结构学习 (L16)!
>
> 对于高斯数据: 图 = 精度矩阵的非零模式 = 用 ℓ1 正则化估计的稀疏 Ω̂。
> 这一洞察连接了概率图模型、高维统计和凸优化三个领域,
> 是现代网络科学 (脑连接组学、基因调控网络、金融网络) 的核心工具。
