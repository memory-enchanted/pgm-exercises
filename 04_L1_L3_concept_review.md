# CMU 10-708 Lecture 1-3 概念体系梳理

> 整理自 Eric Xing 教授 CMU 10-708 概率图模型课程 (BV1tX4y1371G)
>
> 核心教材: Koller & Friedman Ch.1-4

---

## 📐 先看全局：PGM 在做什么

```
                        概率图模型 (PGM)
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
    表示 (Representation)   推理 (Inference)      学习 (Learning)
    如何用图编码P(X)?      给定部分变量求其他的   如何从数据学图和参数?
                           条件概率?
        │                                          │
   L1-L3 讲这个 ──────────┘                        │
        │                    L4-L5, L13-L18        L6-L9
        ▼
   图结构 + 因子分解 = 联合分布
```

**一句话**: PGM = 图 (graph) + 概率 (probability)，两者通过**条件独立性**桥接。

---

## Lecture 1: 导论 — 为什么需要图？

### 1.1 核心问题

联合分布 P(X₁, ..., Xₙ) 用表格存需要 **2ⁿ - 1** 个参数（指数爆炸）。

**图的救场**: 用图结构编码变量之间的独立性，将联合分布拆成**小块局部函数**的乘积 → 参数从指数降到线性/多项式级别。

### 1.2 三个基本概念

| 概念 | 一句话 |
|------|--------|
| **图 (Graph)** | 节点 = 变量，边 = 概率依赖关系 |
| **概率分布 (Distribution)** | 定义在变量上的联合概率 P(X₁,...,Xₙ) |
| **条件独立性 (CI)** | X ⟂ Y \| Z ：给定Z后，X和Y的概率分布独立 |

### 1.3 两种图模型

```
有向图模型 (DAG)                     无向图模型 (MRF)
┌─────────────────┐                 ┌─────────────────┐
│  A → B → C      │                 │  A — B — C     │
│  ↓              │                 │  │   │         │
│  D              │                 │  D — E         │
│                 │                 │                 │
│  贝叶斯网络      │                 │  马尔可夫随机场   │
│  边 = 因果关系   │                 │  边 = 相关关系   │
│  非循环 (no cycle)│                │  可以有环       │
│  Collider 效应   │                 │  无 collider 效应│
└─────────────────┘                 └─────────────────┘
```

### 1.4 三大支柱

| 支柱 | 问题 | L1-L3 阶段解法 |
|------|------|----------------|
| **表示** | 图长什么样？如何从图得到 P？ | 因子分解：P = ∏ 局部因子 |
| **推理** | P(X_evidence) = ? | 变量消除、BP（L4-L5） |
| **学习** | 图结构和参数从哪来？ | MLE、EM（L6-L9） |

---

## Lecture 2: 有向图模型（贝叶斯网络）

这是整个 PGM 的基础，概念最密集的一讲。

### 2.1 贝叶斯网络的定义

一个贝叶斯网络 = **DAG (有向无环图) G** + **局部条件概率表 (CPD)**

```
            结构 G               +              参数 θ
    ┌──────────┐                            ┌──────────────┐
    │ Difficulty│                            │ P(D=high)=0.4│
    │     ↓     │                            │ P(D=low) =0.6│
    │   Grade   │                            │ P(G|D,I)=... │
    │   ↑     ↓ │                            │ P(S|I)=...   │
    │ Intell. Letter                        │ ...          │
    │   ↓       │                            └──────────────┘
    │   SAT     │
    └──────────┘
```

---

### 2.2 因子分解 (Factorization) — 🔑 最核心公式

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   P(X₁, X₂, ..., Xₙ) = ∏ᵢ₌₁ⁿ  P(Xᵢ | Pa(Xᵢ))           │
│                                                           │
│   Pa(Xᵢ) = Xᵢ 的父节点（箭头指向 Xᵢ 的节点）             │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**例子 (学生网络)**:

```
P(D, I, G, S, L) = P(D) × P(I) × P(G|D,I) × P(S|I) × P(L|G)
                     ↑       ↑         ↑          ↑        ↑
                   无父节点  无父节点  父:{D,I}  父:{I}   父:{G}
```

**为什么这是对的？**

贝叶斯网络的定义隐含了：**每个节点在给定父节点后，与所有非后代节点独立** → 这就引出了局部马尔可夫性质。

---

### 2.3 局部马尔可夫性质 (Local Markov Property)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│            Xᵢ ⟂ NonDescendants(Xᵢ) | Pa(Xᵢ)             │
│                                                           │
│     给定"直接原因"（父节点），节点与所有"非结果"          │
│     （非后代节点）条件独立                                  │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**逐词拆解**:
- **NonDescendants(Xᵢ)**: 图中所有不是 Xᵢ 后代的节点（不包括 Xᵢ 自己）
  - 后代 = 从 Xᵢ 出发，沿箭头方向能到达的所有节点
- **Pa(Xᵢ)**: Xᵢ 的父节点（直接指向 Xᵢ 的节点）
- **⟂ ... | ...** : 条件独立

**在学生网络中验证 Grade**:
- Pa(Grade) = {Difficulty, Intelligence}
- Descendants(Grade) = {Letter}
- NonDescendants(Grade) = {SAT} ← 除了 Grade 和它的后代以外的所有节点
- 局部马尔可夫：**Grade ⟂ SAT | {Difficulty, Intelligence}**

> 💡 **直觉**: 知道了课程难度和学生智商，SAT 分数不能告诉我关于成绩的更多信息。因为成绩的"直接原因"就是难度+智商，SAT 只是智商的另一个"结果"而已。

**为什么叫"防火墙"性质？**

```
      所有非后代节点
           ↓
    ┌──────Pa(X)──────┐   ← 父节点 = 防火墙
    │        ↓        │
    │        X        │
    │        ↓        │
    └───Descendants───┘   ← 后代节点
```

给定 Pa(X) 后，X 和非后代之间的所有信息通道都被切断。父节点完全"屏蔽"了外界。

---

### 2.4 d-分离 (d-separation) — 🔑🔑 最难但最重要的概念

**问题**: 给定图 G，判断 X ⟂ Y | Z 是否在图中成立？

**答案**: 用 d-分离准则 (d = directional)

#### Step 1: 找到所有无向路径

先把 DAG 的所有箭头当无向边，找出 X 到 Y 的所有路径。

#### Step 2: 对每条路径判断"活跃/阻塞"

每条路径由若干基本结构拼接而成，按以下规则判断：

```
三种基本结构：

① 链式 (Chain)                     ② 分叉 (Fork)
   X → M → Y                           X ← M → Y
                                     
   M 未观测 → 活跃 🔴                  M 未观测 → 活跃 🔴
   M 被观测 → 阻塞 🟢                  M 被观测 → 阻塞 🟢

③ 汇聚 (Collider / V-structure) ← 这是最容易搞反的!
   X → M ← Y

   M 未观测 → 阻塞 🟢 (!!)
   M 被观测 → 活跃 🔴 (!!)
   M的后代被观测 → 活跃 🔴 (!!!)
```

**记忆口诀**:

> 链式和分叉: 观测就堵，不管就通 (符合直觉)
> Collider: 不管就堵，观测就通 (反直觉！)

**Collider 的直觉 — "解释消除" (Explaining Away)**:

```
天才(X) → 成功(M) ← 运气(Y)

- 不知道某人是否成功 → 天才和运气独立 (collider 未观测 → 阻塞)
- 知道某人成功了 → 如果他很天才，那他可能运气不好；如果他很幸运，那他可能不天才
  → 天才和运气变得负相关! (collider 被观测 → 激活!)
```

#### Step 3: 最终判定

```
X 和 Y 被 Z d-分离  ⟺  所有路径都是阻塞的
X 和 Y 是 d-连通的  ⟺  至少有一条路径活跃
```

#### ⚠️ 经典陷阱: 一个节点在不同路径中扮演不同角色

```
    A → B → C
        ↑
        D → E

判断 A ⟂ E | {B} ?

路径1: A → B, B ← D → E
在路径1中，B 是 collider (A→B←D)! 被观测 → 激活! → A和E d-连通

路径2: A → B → C (不到E了，不用看)

→ d-连通! 虽然 B 在链式结构中阻塞了 A→B→C 方向，但 B 在 A→B←D 中是 collider!
```

> 🎯 d-分离是整个 PGM 大厦的地基。消息传播 (BP) 算法就是 d-分离的算法化。

---

### 2.5 全局马尔可夫性质 (Global Markov Property)

如果你理解了 d-分离，全局马尔可夫性质就很简单：

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│     d-sep_G(X, Y | Z)   ⇒   X ⟂ Y | Z (在 P 中)        │
│                                                           │
│     "图中 d-分离"  ⇒  "分布中条件独立"                    │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**这就是图结构 和 概率分布之间的桥梁。**

一旦你验证了 P 按照 G 做因子分解，全局马尔可夫性质就自动保证：G 中 d-分离的所有变量对，在 P 中确实是条件独立的。

**三个性质的关系 (DAG中)**:

```
        因子分解
           ⇕
      局部马尔可夫性质
           ⇕
      全局马尔可夫性质
           ⇓
      成对马尔可夫性质 (最弱版本)
```

在 DAG 中，前三个是**互相等价**的！你可以从任何一个出发推导出另外两个。

---

### 2.6 I-Map (独立性映射)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   I(G)  = 图G中所有d-分离声明的集合                        │
│   I(P)  = 分布P中所有实际成立的CI声明的集合                 │
│                                                           │
│   G 是 P 的 I-map  ⟺  I(G) ⊆ I(P)                        │
│                                                           │
│   图编码的独立性都是真的 (sound)，但可能遗漏一些 (不完全)    │
│                                                           │
│   G 是 P 的 P-map  ⟺  I(G) = I(P)                        │
│                                                           │
│   图完美捕获分布的所有独立性 (sound + complete)             │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**用画圈理解**:

```
   I-map:              P-map:              Not I-map:
   ┌─────────┐        ┌─────────┐          ┌─────────┐
   │ I(P)    │        │ I(G)    │          │ I(G)    │
   │ ┌─────┐ │        │ = I(P)  │          │  ┌─────┐│
   │ │I(G) │ │        │         │          │  │I(P) ││
   │ └─────┘ │        └─────────┘          │  └─────┘│
   └─────────┘                              └─────────┘
   图在分布里                                图说错了!
```

**关键认知**: 不是所有分布都有 DAG P-map。有些独立结构 DAG 无法完美表达（如对称独立性）。

---

### 2.7 马尔可夫毯 (Markov Blanket)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   MB(X) = Pa(X) ∪ Children(X) ∪ Spouses(X)               │
│                                                           │
│   其中 Spouses(X) = X 的各个子节点的其他父节点             │
│                                                           │
│   性质: X ⟂ (所有不在 MB(X) 中的节点) | MB(X)             │
│                                                           │
│   → 要预测 X，只需要知道它的马尔可夫毯中的变量              │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**图示**:

```
        配偶
         ↓
    ┌───Spouse───┐
    │     ↓      │
    Pa → X → Child
    │            │
    └───MB(X)────┘

图例：MB(Grade) = {Difficulty, Intelligence, Letter}
```

在学生网络中:
- Pa(Grade) = {Difficulty, Intelligence}
- Children(Grade) = {Letter}
- Spouses(Grade) = Letter 的其他父节点 = ∅

练习验证: 给定 MB(Grade)，Grade 和 SAT **是** d-分离的 ✓

---

## Lecture 3: 无向图模型（马尔可夫随机场 / MRF）

### 3.1 为什么需要无向图？

DAG 有个限制：**无法表示某些对称的/循环的依赖结构**。

```
例子：四个人互相影响对方的观点

DAG 表达:        无向图表达:
A → B            A — B
↓   ↓            │   │        ← 更自然!
D ← C            D — C
(有 collider      (纯双向关联)
问题)
```

**无向图的优势**: 不强制方向性，更自然地表示"相互作用"而非"因果"。

**无向图的劣势**: 无 collider 结构 → 无法表示 explaining away。

---

### 3.2 MRF 的参数化 — 团和因子

DAG 用 CPD (条件概率表) 参数化，MRF 用"因子 (factor)"参数化。

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   P(X₁,...,Xₙ) = (1/Z) × ∏_{C∈cliques} ψ_C(X_C)         │
│                                                           │
│   ψ_C: 定义在团 C 上的非负"势函数" (potential)            │
│   Z:   归一化常数 (partition function)                     │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**关键区别 DAG vs MRF**:
- DAG: 因子 = CPD，自动归一化（每项加和为1）
- MRF: 因子 = 任意非负函数，需全局归一化常数 Z（计算 Z 非常困难！）

**定义**:
- **团 (Clique)**: 完全连接的节点子集（每对节点之间都有边）
- **极大团 (Maximal Clique)**: 不能再加节点的团

```
    A — B
    |   |
    C — D

团: {A,B}, {B,D}, {A,C}, {C,D}, {A,B,C}, {B,C,D}... 
极大团: {A,B,C}, {B,C,D}
```

---

### 3.3 因子分解 vs 吉布斯分布

```
因子分解                  吉布斯分布 (Gibbs Distribution)
─────────────────────     ─────────────────────────────
P(X) ∝ ∏ ψ_C(X_C)        P(X) = (1/Z) exp(-Σ_C E_C(X_C))

                          其中 E_C = -log ψ_C (能量函数)

势函数 ψ ≥ 0             能量 E 可以取任意实数值
乘积形式的因子            指数形式，来自统计物理
```

两者等价：取 log 就从乘积变成求和。吉布斯形式在变分推断（L13-L15）和统计物理中更方便。

---

### 3.4 MRF 的三个马尔可夫性质

这是 L3 的核心内容，需要理清层次：

```
       全局马尔可夫 (Global Markov)
       d-sep(X,Y|Z) in UG ⇒ X⟂Y|Z in P
              ⇕ (正分布下)
       局部马尔可夫 (Local Markov)
       X ⟂ 非闭包节点 | 邻居
              ⇕ (正分布下)
       成对马尔可夫 (Pairwise Markov)
       X ⟂ Y | 所有其他节点  (对于不相邻的X,Y)
```

| 性质 | DAG 版本 | MRF 版本 | "防火墙" |
|------|----------|----------|----------|
| 局部 | X ⟂ NonDesc \| Pa(X) | X ⟂ 非闭包 \| 邻居(X) | 父节点 vs 邻居 |
| 全局 | d-sep ⇒ CI | d-sep(UG) ⇒ CI | 相同逻辑 |
| 成对 | 非邻接父子 ⇒ CI | 非邻接 ⇒ CI \| 其余 | DAG不常用 |

**无向图的"防火墙"**:
- DAG: Pa(X) 屏蔽了非后代 → 单向的"因果屏蔽"
- MRF: 邻居 bd(X) 屏蔽了其他所有节点 → 双向的"关联屏蔽"

```
DAG 的 X:                  MRF 的 X:
                           
   ○──→ Pa ──→ X            ○ ── 邻居 ── X ── 邻居 ── ○
        防火墙                  ↑         ↑
                           这些都不是邻居  防火墙 = 邻居
```

**闭包 (Closure)**: cl(X) = {X} ∪ bd(X) = X 和它的所有邻居
**非闭包节点**: 图中除了 cl(X) 以外的所有节点

---

### 3.5 Hammersley-Clifford 定理 ⭐⭐⭐

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   Hammersley-Clifford 定理:                               │
│                                                           │
│   对于严格正的分布 P (所有 P(x) > 0):                      │
│                                                           │
│   成对马尔可夫 ⟺ 局部马尔可夫 ⟺ 全局马尔可夫               │
│       ⇕                                                 │
│   吉布斯分布 (因子分解在极大团上)                           │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**为什么这个定理重要？**

它完成了 MRF 版本的"因子分解 ⟺ 马尔可夫性质"等价闭环：
- 如果你知道 P 在 MRF 的极大团上因子分解 → 所有马尔可夫性质成立
- 如果 P 满足任何一个马尔可夫性质 → P 可以在极大团上因子分解

**非正分布的反例为什么能破？**
- 考虑一个包含 0 概率的分布，可以精心构造使成对独立成立但局部/全局不成立
- 实际应用中几乎总是处理正分布，所以这个定理通常实用

---

### 3.6 I-Map 对 MRF 的意义

和 DAG 完全类似：
- MRF H 是 P 的 **I-map** ⟺ I(H) ⊆ I(P)
- MRF H 是 P 的 **P-map** ⟺ I(H) = I(P)

---

### 3.7 DAG vs MRF: 表达能力的对比

这是 L3 最精彩的总结部分：

| 独立性模式 | DAG 能表示？ | MRF 能表示？ |
|-----------|:-----------:|:-----------:|
| X ⟂ Y \| Z (链式) | ✅ | ✅ |
| X ⟂ Y (边缘独立) | ✅ | ✅ |
| X ⟂ Y, 但 X和Y在给定Z时相关 (explaining away) | ✅ collider | ❌ **不能!** |
| X ⟂ Y \| Z, 但 X ⟂ Y \| {Z,W} 也成立 (对称性) | ❌ 困难 | ✅ |
| A-B-C-D 循环依赖 | ❌ 必须是无环的 | ✅ |

**核心结论**: 两类图模型各有能/不能表达的独立性模式，**没有一方绝对"更好"**。

```
能编码的 CI 集合:

    ┌──────────┐     ┌──────────┐
    │          │     │          │
    │  DAG可   │     │  MRF可   │
    │  编码的   │     │  编码的   │
    │          │     │          │
    └────┬─────┘     └────┬─────┘
         │                │
         └───────┬────────┘
                 │
         ┌───────┴───────┐
         │  两者都能编码   │
         │  (有交集!)     │
         └───────────────┘
```

---

## 📋 全部概念一张表

| 概念 | 所属 | 定义 | 一句话 |
|------|------|------|--------|
| **因子分解** | L1-L2 | P = ∏ P(Xᵢ\|Pa(Xᵢ)) | 联合分布拆成局部条件概率的乘积 |
| **CPD** | L2 | P(X \| Pa(X)) | 每个节点的条件概率表 |
| **局部马尔可夫** | L2 | X ⟂ NonDesc \| Pa(X) | 给定父节点，和非后代独立 |
| **d-分离** | L2 | 图中路径全阻塞 | 看图判断条件独立性的算法 |
| **Collider** | L2 | X→M←Y | 汇聚结构，未观测阻塞/观测激活 |
| **全局马尔可夫** | L2 | d-sep(G) ⇒ CI(P) | 图分离 → 分布独立 |
| **I-map** | L2 | I(G) ⊆ I(P) | 图是分布的"安全上界" |
| **P-map** | L2 | I(G) = I(P) | 图完美捕获分布的所有独立性 |
| **马尔可夫毯** | L2 | Pa + Child + Spouses | 预测X所需的最小变量集 |
| **成对马尔可夫** | L2/L3 | 非邻接 ⇒ CI\|其他 | 最弱的独立性性质 |
| **极大团** | L3 | 最大的完全连接子图 | MRF 因子分解的基本单位 |
| **势函数 ψ** | L3 | 定义在团上的非负函数 | MRF 的"因子" |
| **配分函数 Z** | L3 | Σ∏ψ | MRF 的归一化常数，计算困难 |
| **吉布斯分布** | L3 | P ∝ exp(-ΣE) | MRF 的能量函数形式 |
| **闭包 & 边界** | L3 | cl(X), bd(X) | MRF 局部马尔可夫的定义工具 |
| **Hammersley-Clifford** | L3 | 正分布下三者等价 | MRF 版本的核心定理 |
| **无向图分离** | L3 | 所有路径经过Z | MRF 的 d-分离（更简单） |

---

## 🔗 概念之间的关系图

```
                         PGM 的核心问题:
                用图结构紧凑地表示概率分布
                            │
            ┌───────────────┴───────────────┐
            ▼                               ▼
      有向图 (DAG)                     无向图 (MRF)
      L2 重点                          L3 重点
            │                               │
            ▼                               ▼
      因子分解 (CPD)                因子分解 (团势函数)
      P = ∏ P(Xᵢ|Pa)               P ∝ ∏ ψ_C(X_C)
            │                               │
            ├───────────等价────────────────┤ (正分布下)
            ▼                               ▼
      局部马尔可夫性质                局部马尔可夫性质
      X ⟂ NonDesc | Pa            X ⟂ 非闭包 | 邻居
            │                               │
            ├───────────等价────────────────┤ (正分布下)
            ▼                               ▼
      全局马尔可夫性质                全局马尔可夫性质
      d-sep(G) ⇒ CI(P)              UG-sep(G) ⇒ CI(P)
            │                               │
            └───────────桥接───────────────┘
                        │
                        ▼
                 I-map / P-map
             (图对分布独立性的刻画精度)
                        │
                        ▼
                  马尔可夫毯
            (预测X的最小充分统计量)

═══════════════════════════════════════════════════════

L1:  "为什么要用图？"         → 指数 → 多项式的参数压缩
L2:  "有向图怎么表示分布？"    → 因果结构 + d-分离判断独立
L3:  "无向图怎么表示分布？"    → 对称关联 + 三个性质的等价条件
```

---

## 🧪 自测清单

在进入 L4 (变量消除) 之前，确认以下都能回答：

- [ ] 给定一个 DAG，能写出因子分解
- [ ] 给定一个 DAG 和两个节点，能找出所有无向路径
- [ ] 能正确判断每条路径在给定 Z 时是活跃还是阻塞
- [ ] 能判断两个变量给定 Z 时是否 d-分离
- [ ] 知道 Collider 被观测（或后代被观测）后的"惊喜"效应
- [ ] 能为任意节点写出局部马尔可夫性质声明
- [ ] 能从 d-分离推导出全局马尔可夫性质
- [ ] 能解释 I-map、P-map、minimal I-map 的区别
- [ ] 能为一个节点找出它的马尔可夫毯
- [ ] 能在 DAG 和 MRF 之间转换（知道何时可以/不可以）
- [ ] 理解 Hammersley-Clifford 定理的条件和结论
- [ ] 知道 DAG 和 MRF 表达能力上的互补性（谁能表示 explaining away）

---

> L4 预告: L1-L3 讲了"图长什么样" → L4 开始讲"用图做计算": **变量消除算法**。
> 所有 L1-L3 的 d-分离概念，在 L4 中会被算法化 — d-分离将变成实际的计算步骤。
