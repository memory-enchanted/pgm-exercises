# CMU 10-708 Lecture 9 概念体系梳理 — 蒙特卡洛方法 (Monte Carlo)

> 整理自 Eric Xing 教授 CMU 10-708 Lecture 9: Monte Carlo Methods
>
> 核心教材: Murphy Ch.23-24, Bishop Ch.11, Robert & Casella (2004)

---

## 📐 全局定位：VI → MCMC — 两条近似推断路线

```
L7-L8: 变分推断 (VI / Optimization)      L9: 蒙特卡洛 (MCMC / Sampling)
──────────────────────────────────      ──────────────────────────────
把推断变成优化问题                        把推断变成采样问题
输出: 近似分布 Q (解析形式)               输出: 样本集合 {z_s}
永远有近似误差 (变分族限制)               渐近精确 (样本→∞ 时收敛)
收敛判断: ELBO 单调 → 易                  收敛判断: 困难 (需要诊断)
适合: 大规模, 需要快速答案                适合: 中小规模, 需要精确答案
```

**一句话概括 L9**：当 VI 的近似误差不可接受时，蒙特卡洛方法通过**采样**来近似后验分布。MCMC 构造一条马尔可夫链，使其稳态分布恰好是目标分布 P(Z|X) — 大样本极限下**精确**。

---

## 概念 1：蒙特卡洛基本原理

### 核心思想

```
我们想计算: E_{P(Z)}[f(Z)] = ∫ f(z) P(z) dz  (难以解析计算)

蒙特卡洛近似:
  1. 从 P(Z) 中采样 z_1, z_2, ..., z_S
  2. E[f(Z)] ≈ (1/S) Σ_{s=1}^S f(z_s)

由大数定律: 当 S → ∞, 近似 → 真实值
由中心极限定理: 误差 ~ O(1/√S)
```

### 为什么有效？

```
误差 scale: O(1/√S) — 与维度 D 无关!

对比:
  网格积分: 误差 ~ O(h^2), 但需要 O(1/h^D) 个点 → 维度灾难
  蒙特卡洛: 误差 ~ O(1/√S), 只需要 S 个样本 → 不受维度灾难影响!

这就是 MC 的魔力: 在高维空间中唯一可行的方法。
```

---

## 概念 2：拒绝采样 (Rejection Sampling)

### 核心思想

当无法直接从 P(Z) 采样时，从一个容易采样的**建议分布** Q(Z) 中采样，然后以概率 P/Q 接受。

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  Rejection Sampling 算法:                                     │
│                                                               │
│  要求: 存在 M 使得 M·Q(z) ≥ P(z) 对所有 z 成立               │
│        (M·Q 必须"包住" P)                                     │
│                                                               │
│  For s = 1, 2, ...:                                          │
│    1. 从 Q 采样 z ~ Q(z)                                     │
│    2. 从 Uniform(0, M·Q(z)) 采样 u                           │
│    3. If u ≤ P(z): accept z                                  │
│       Else:        reject z, goto 1                           │
│                                                               │
│  接受率 = 1/M  (M 越小 → 接受率越高 → 越高效)                 │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 优缺点

| 优点 | 缺点 |
|------|------|
| 简单直观, 容易实现 | 需要找到包络 M·Q ≥ P — 高维中几乎不可能 |
| 接受的样本是 exact i.i.d. 的 | 接受率 1/M 随维度指数衰减 → 维度灾难! |
| 无需 burn-in (不像 MCMC) | 只适用于低维 (D < 10) |

---

## 概念 3：重要性采样 (Importance Sampling)

### 核心思想

不拒绝样本，而是给每个样本**加权**。

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  E_P[f(Z)] = ∫ f(z) P(z) dz                                  │
│            = ∫ f(z) [P(z)/Q(z)] Q(z) dz                      │
│            ≈ (1/S) Σ_s f(z_s) · w_s                          │
│                                                               │
│  其中:                                                        │
│    z_s ~ Q(z)       (proposal distribution)                   │
│    w_s = P(z_s)/Q(z_s)  (importance weight)                  │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 归一化的重要性采样

当 P(z) 只知到归一化常数 (P̃ = Z·P):

```
E_P[f] ≈ Σ_s w_s · f(z_s) / Σ_s w_s

其中 w_s = P̃(z_s) / Q(z_s)  (未归一化权重)
```

### 有效样本量 (Effective Sample Size)

```
ESS = (Σ w_s)² / Σ w_s²  ∈ [1, S]

ESS 衡量"权重退化"程度:
  - 所有权重均匀: ESS = S  (完美)
  - 一个权重占主导: ESS ≈ 1  (无用!)
```

---

## 概念 4：马尔可夫链蒙特卡洛 (MCMC) — 核心思想 (🔑🔑🔑)

### 为什么需要 MCMC？

拒绝采样和重要性采样在高维中都面临严重问题:
- Rejection: 找不到包络 → 接受率 → 0
- Importance: 权重方差爆炸 → ESS → 1

**MCMC 的解决方案**: 不试图产生 i.i.d. 样本，而是构造一条**马尔可夫链**，使其**稳态分布**恰好是目标分布 P(Z)。

### 马尔可夫链基础

```
马尔可夫链: 序列 Z_1, Z_2, Z_3, ... 
满足: P(Z_{t+1} | Z_1, ..., Z_t) = P(Z_{t+1} | Z_t)  (一阶马尔可夫性)

转移核 (Transition Kernel): T(z' | z) = P(Z_{t+1}=z' | Z_t=z)

稳态分布 π: 如果 ∫ π(z) T(z'|z) dz = π(z')  (不动点)
           则 π 是链的稳态分布 (stationary distribution)
```

### Detailed Balance (细致平衡)

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  Detailed Balance:                                            │
│    π(z) · T(z' | z) = π(z') · T(z | z')                     │
│                                                               │
│  含义: 从 z 到 z' 的概率流 = 从 z' 到 z 的概率流              │
│                                                               │
│  Detailed Balance ⇒ π 是稳态分布 (充分条件)                   │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

**MCMC 的核心策略**: 设计 T(z'|z) 满足 detailed balance w.r.t. P(Z) → 链的稳态 = P → 跑链足够长 → 样本 ≈ 从 P 中采样。

---

## 概念 5：Metropolis-Hastings (MH) 算法 (🔑🔑🔑)

### 算法

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  Metropolis-Hastings 算法:                                    │
│                                                               │
│  1. 初始化 z^(0)                                              │
│  2. For t = 1, 2, ...:                                       │
│     a. 从建议分布采样: z* ~ Q(z* | z^{(t-1)})               │
│     b. 计算接受概率:                                         │
│        α = min(1, [P(z*) · Q(z^{(t-1)} | z*)] /             │
│                     [P(z^{(t-1)}) · Q(z* | z^{(t-1)})])      │
│     c. 以概率 α 接受: z^{(t)} = z*                           │
│        以概率 1-α 拒绝: z^{(t)} = z^{(t-1)}                  │
│                                                               │
│  Q 的常见选择:                                                │
│    - Random Walk: z* = z + ε, ε ~ N(0, σ²)                  │
│    - Independent: z* ~ Q(z) (不依赖当前状态)                  │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 为什么 MH 满足 Detailed Balance?

```
P(z) · Q(z'|z) · α(z→z')
  = P(z) · Q(z'|z) · min(1, P(z')·Q(z|z') / (P(z)·Q(z'|z)))
  = min(P(z)·Q(z'|z), P(z')·Q(z|z'))
  = P(z') · Q(z|z') · min(P(z)·Q(z'|z)/(P(z')·Q(z|z')), 1)
  = P(z') · Q(z|z') · α(z'→z)

✓ Detailed Balance 成立!
```

### Proposal 调参

```
σ 太小 → 接受率高 (~80-90%) 但移动慢 → 需要很多步才探索整个空间
σ 太大 → 接受率低 (~10%) → 链卡住不动
最优接受率: ~23.4% (高维 Random Walk MH, 理论结果)
             ~44%   (1D Random Walk MH)
```

---

## 概念 6：Gibbs 采样 (🔑🔑)

### MH 的特例

Gibbs 采样 = Metropolis-Hastings 的**接受率始终为 1** 的特例。

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  Gibbs 采样:                                                  │
│                                                               │
│  For t = 1, 2, ...:                                          │
│    For each variable i:                                       │
│      z_i^{(t)} ~ P(Z_i | z_{-i}^{(t)})                       │
│      (从 i 的 full conditional 中采样)                        │
│                                                               │
│  Proposal Q = P(Z_i | z_{-i}) — 恰好是条件分布!               │
│  代入 MH 接受率公式: α = 1 (总是接受)                         │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 什么时候用 Gibbs?

**优点**:
- 不需要调 proposal (不像 MH)
- 接受率 = 100% — 无拒绝
- 当 full conditionals 容易采样时非常高效

**缺点**:
- 需要知道所有 full conditionals P(Z_i | Z_{-i})
- 在强相关变量之间移动慢 (zig-zag behavior)
- 只适用于离散/条件共轭模型

### Gibbs vs MH

```
Gibbs = MH with proposal Q(z*|z) = P(z_i* | z_{-i})
       → α = 1 always

Gibbs 采样 = "逐坐标采样"
MH 采样   = "沿任意方向采样 + 接受/拒绝"
```

---

## 概念 7：MCMC 实践 — Burn-in, Thinning, 诊断

### Burn-in (预热)

```
链从随机起点出发, 需要一定步数才能"忘记"初始值 → 到达稳态

Burn-in: 丢弃前 B 个样本
  z_1, ..., z_B → 扔掉!
  z_{B+1}, ..., z_T → 用于估计
```

**问题**: 没有通用方法确定 B 应该是多少。

### Thinning (稀疏化)

```
链中相邻样本是相关的 (自相关)

Thinning: 每 K 步保留一个样本
  → 减少存储, 减少自相关
  → 但丢弃了信息! (通常不建议, 除非存储受限)
```

### 收敛诊断

| 方法 | 原理 |
|------|------|
| **Trace Plot** | 画 z_t vs t — 直观检查是否稳定, 有无趋势 |
| **Running Mean** | 累积均值是否稳定 |
| **Gelman-Rubin (R-hat)** | 跑多条链, 比较链内方差 vs 链间方差; R̂ ≈ 1 → 收敛 |
| **Effective Sample Size** | 考虑自相关后的"等效独立样本数" |

---

## 概念 8：MCMC vs VI 的综合对比

| 维度 | VI (L7-L8) | MCMC (L9) |
|------|-----------|-----------|
| **范式** | 优化 | 采样 |
| **输出** | 近似分布 Q | 样本集合 |
| **渐近精确** | ❌ (受变分族限制) | ✅ (S → ∞) |
| **速度** | 通常更快 | 通常更慢 (需要很多步) |
| **收敛判断** | ELBO 单调 → 容易 | 需要诊断 → 困难 |
| **高维可扩展** | 好 (SVI + 随机梯度) | 一般 (链混合慢) |
| **调参** | 初始化, 学习率 | Proposal 宽度, burn-in |
| **何时选用** | 需要快速答案, 大数据 | 需要精确, 中小规模 |

---

## 📋 全部概念一张表

| 概念 | 一句话 |
|------|--------|
| **Monte Carlo** | 用样本均值近似期望: E[f] ≈ (1/S)Σ f(z_s) |
| **Rejection Sampling** | 从 Q 采样, 以 P/(M·Q) 概率接受 — 高维失效 |
| **Importance Sampling** | 不拒绝, 用权重 w=P/Q 重新加权 — 权重退化 |
| **MCMC** | 构造马尔可夫链 → 稳态 = P → 采样 ≈ P |
| **Detailed Balance** | π(z)·T(z'\|z) = π(z')·T(z\|z') → 保证稳态 |
| **Metropolis-Hastings** | proposal Q + accept/reject → 满足 detailed balance |
| **Gibbs Sampling** | proposal = full conditional → 接受率 100% |
| **Burn-in** | 丢弃前 B 步 — 等链到达稳态 |
| **ESS** | 有效样本量 — 衡量自相关的等价独立样本数 |

---

## 🔗 概念关系图

```
              精确推断不可行 (高 treewidth)
                        │
              近似推断 (Approximate Inference)
                        │
          ┌─────────────┴─────────────┐
          ▼                           ▼
    确定性 (VI, L7-L8)          随机 (Monte Carlo, L9)
    Q ≈ P via 优化               样本 {z_s} ≈ P via 采样
          │                           │
          │              ┌────────────┼────────────┐
          │              ▼            ▼            ▼
          │        Rejection    Importance      MCMC
          │         (i.i.d.)    (weighted)   (correlated)
          │              │            │            │
          │              └── 高维失效 ─┘           │
          │                           ┌───────────┴───────────┐
          │                           ▼                       ▼
          │                     Metropolis-Hastings      Gibbs Sampling
          │                      (proposal + A/R)     (full conditional)
          │                           │                       │
          │                           └───────┬───────────────┘
          │                                   ▼
          │                           MCMC 实践:
          │                           burn-in, thinning, R-hat
          │
          ▼
      VI → 快速, 近似          MCMC → 慢, 渐近精确
```

---

## 🎯 核心洞见一句话

| # | 洞见 |
|---|------|
| 1 | **MC 不受维度灾难影响** — 误差 O(1/√S) 与维度 D 无关 |
| 2 | **Rejection/Importance 在高维失效** — 找到好的 proposal Q 极难 |
| 3 | **MCMC 用相关性换可行性** — 样本不独立, 但稳态恰好是 P |
| 4 | **Detailed Balance = MCMC 的数学保证** — MH/Gibbs 都靠它 |
| 5 | **Gibbs = MH with α=1** — 逐坐标采样, 无需调 proposal |

---

## 🧪 自测清单

- [ ] 为什么 Monte Carlo 不受维度灾难影响? (对比网格积分)
- [ ] Rejection Sampling 的接受率为什么在高维中指数衰减?
- [ ] Importance Sampling 的 ESS 衡量什么? ESS=1 意味着什么?
- [ ] Metropolis-Hastings 为什么满足 detailed balance?
- [ ] Gibbs Sampling 的 proposal 分布是什么? 接受率是多少?
- [ ] Random Walk MH 的 proposal 宽度 σ 如何影响接受率? 最优值是多少?
- [ ] Burn-in 的目的是什么? 如何诊断链是否已收敛?

---

> L9 打开了 Monte Carlo 的大门。掌握 MCMC 后, 你将拥有 VI (快但近似) 和 MCMC (慢但精确) 两套互补的推断工具。
