# CMU 10-708 Lecture 4 概念体系梳理 — 精确推理 (Variable Elimination)

> 整理自 Eric Xing 教授 CMU 10-708 Lecture 4: Exact Inference (BV1tX4y1371G)
>
> 核心教材: Koller & Friedman Ch.9-10

---

## 📐 全局定位：推理在 PGM 中的位置

```
                        概率图模型 (PGM)
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
    表示 (Representation)   推理 (Inference)      学习 (Learning)
    L1-L3                  L4-L5, L13-L18        L6-L9
    图怎么画                怎么算P(Q|E)?         参数和图怎么学
                              │
                              │
            ┌─────────────────┴─────────────────┐
            ▼                                   ▼
      精确推理 (L4-L5)                   近似推理 (L13-L18)
      变量消除 + 信念传播                MCMC + 变分推断
      答案精确，但可能指数慢              答案近似，但可处理大规模
```

**L4 的核心问题**：给定一个已经建好的图模型（L1-L3 的成果），如何高效地计算条件概率 P(Query | Evidence)？

---

## 🔑 先建立直觉：推理到底在算什么？

### 场景：学生网络

```
    Difficulty ──→ Grade ──→ Letter
                     ↑
    Intelligence ──→ SAT
```

**问题**: 已知 Letter = Good（推荐信好），求 Intelligence = High 的概率？

即计算：**P(I = high | L = good)**

### 朴素方法（暴力枚举）— 为什么不行

```
P(I=high | L=good) = P(I=high, L=good) / P(L=good)

需要:
  1. 对 D, G, S 所有取值求和(共 2×3×2 = 12 项)
  2. 再做归一化

变量少时还行，20个二值变量 → 2^20 ≈ 百万项，完全不可行
```

**推理的核心挑战**：如何用"聪明的方法"求和，避免枚举整个联合分布的指数级状态空间。

---

## 概念 1：因子 (Factor)

### 什么是因子？

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   因子 φ(X₁, X₂, ..., Xₖ)  =  从变量取值 → 非负实数的映射  │
│                                                           │
│   本质上是一个多维表格（张量）                              │
│                                                           │
│   作用域 Scope[φ] = {X₁, X₂, ..., Xₖ}  (因子涉及的变量)    │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### 因子的三种来源

| 来源 | 符号 | 例子 | 性质 |
|------|------|------|------|
| CPD | P(Xᵢ \| Pa(Xᵢ)) | P(G\|D,I) | 每行 sum = 1（条件概率） |
| 证据 | δ(E=e) | δ(L=good) | 只保留匹配行，其余为 0 |
| 中间结果 | τ(...) | 消除 D 后的因子 | sum 不一定 = 1 |

**关键认识**: VE 算法中，无论是原始 CPD 还是证据还是中间计算结果，**统统当作因子处理** — 这是统一抽象的关键。

### 因子的"形状"

```
因子 φ(Grade, Difficulty, Intelligence):

           D=low            D=high
           I=low  I=high    I=low  I=high
  G=A      0.3    0.05      0.9    0.5
  G=B      0.4    0.25      0.08   0.3
  G=C      0.3    0.70      0.02   0.2

→ 一个 3 × 2 × 2 的三维数值表
→ Scope = {G, D, I}
```

---

## 概念 2：两个基本操作 — 乘积 和 边缘化

整个 VE 算法只做两件事，反复做：

### 操作 A：因子乘积 (Factor Product)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   φ₁₂(x,y,z) = φ₁(x,y) × φ₂(y,z)                        │
│                                                           │
│   逐元素相乘（对应赋值行匹配）                              │
│   Scope[φ₁₂] = Scope[φ₁] ∪ Scope[φ₂]                     │
│                                                           │
│   计算量: |Val(X)| × |Val(Y)| × |Val(Z)|                  │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**逐元素乘积示例**:

```
φ₁(G, I):              φ₂(I, S):              φ₁₂(G, I, S):
     I=low  I=high        I=low  I=high        G     I     S     value
G=A  0.3    0.05     S=0   0.95   0.2     →   A    low    0    0.3×0.95=0.285
G=B  0.4    0.25     S=1   0.05   0.8          A    low    1    0.3×0.05=0.015
G=C  0.3    0.70                               A   high    0    0.05×0.2=0.01
                                               ... (继续填满 3×2×2=12 行)
```

> 💡 **直觉**: 因子乘积 = 把两个"局部知识"合并成一个"更大的局部知识"

### 操作 B：因子边缘化 (Factor Marginalization)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│   τ(y,z) = Σₓ φ(x,y,z)                                   │
│                                                           │
│   对变量 X 的所有取值求和，把 X "消掉"                     │
│   Scope[τ] = Scope[φ] \ {X}                              │
│                                                           │
│   计算量: |Val(X)| × |Val(Y)| × |Val(Z)|                  │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

**边缘化示例** (消去 I，保留 G 和 S):

```
φ(G, I, S):                               τ(G, S) = Σ_I φ(G, I, S):
G     I     S     value                   G     S     value
A    low    0     a₁           →         A     0     a₁ + a₂
A    low    1     a₂                     A     1     a₃ + a₄
A   high    0     a₃         对 I 求和    B     0     b₁ + b₂
A   high    1     a₄         ========>   B     1     b₃ + b₄
B    low    0     b₁                     C     0     c₁ + c₂
B    low    1     b₂                     C     1     c₃ + c₄
...                                      (维度从 3×2×2 降为 3×2)
```

> 💡 **直觉**: 边缘化 = 整合所有可能情况，把不关心的维度"压缩掉"

---

## 概念 3：变量消除算法 (Variable Elimination) — 🔑🔑🔑 核心

### 问题形式化

```
输入:
  - 一组因子 Φ = {φ₁, φ₂, ..., φₖ}  (来自 CPD + 证据)
  - 查询变量 Q (要保留的变量)
  - 消除顺序 π (要消掉的变量，按什么顺序消)

输出:
  - P(Q | E=e)  (归一化后的条件概率)
```

### 算法流程（三步法）

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│  Step 1: 选择消除顺序 π                                   │
│          例: 要消 [D, S, G, I]，顺序 π = [S, D, G, I]    │
│                                                           │
│  Step 2: 按顺序逐个消除变量                                │
│          FOR each X in π:                                 │
│            a. 找出所有 scope 含 X 的因子                   │
│            b. 把它们全乘起来 → ψ                           │
│            c. 对 X 求和消去 → τ = Σₓ ψ                    │
│            d. 把 τ 加入因子集合（替代原来的因子）            │
│                                                           │
│  Step 3: 对剩余因子（只含 Q）做乘积 + 归一化               │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### 完整示例（学生网络，用手推一遍！）

**问题**: P(I | L=good)，消除顺序 π = [D, S, G]

**初始因子集**（CPD + 证据）:
```
φ₁(D)     = P(D)              scope: {D}
φ₂(I)     = P(I)              scope: {I}
φ₃(G,D,I) = P(G|D,I)         scope: {G,D,I}
φ₄(S,I)   = P(S|I)           scope: {S,I}
φ₅(L,G)   = P(L=good|G)      scope: {G}   ← L 固定为 good!
```

**消除 D** (π[1] = D):
```
找出含 D 的因子: {φ₁(D), φ₃(G,D,I)}
乘积: ψ₁(G,D,I) = φ₁(D) × φ₃(G,D,I)
求和: τ₁(G,I) = Σ_D ψ₁(G,D,I)

因子集合更新: {φ₂(I), τ₁(G,I), φ₄(S,I), φ₅(G)}

总计算量: |D|×|G|×|I| = 2×3×2 = 12
```

**消除 S** (π[2] = S):
```
找出含 S 的因子: {φ₄(S,I)}
乘积: ψ₂(S,I) = φ₄(S,I)          ← 只有一个因子时直接消
求和: τ₂(I) = Σ_S ψ₂(S,I) = [1, 1]  ← 对 S 求和后恒为 1!

因子集合更新: {φ₂(I), τ₁(G,I), τ₂(I), φ₅(G)}

总计算量: 2×2 = 4
```

**消除 G** (π[3] = G):
```
找出含 G 的因子: {τ₁(G,I), φ₅(G)}
乘积: ψ₃(G,I) = τ₁(G,I) × φ₅(G)
求和: τ₃(I) = Σ_G ψ₃(G,I)

因子集合更新: {φ₂(I), τ₂(I), τ₃(I)}

总计算量: 3×2 = 6
```

**归一化**:
```
剩余因子乘积: φ₂(I) × τ₂(I) × τ₃(I) = P(I) × 1 × τ₃(I) ∝ P(I|L=good)

归一化: P(I|L=good) = τ₃(I) × P(I) / Σ_I (τ₃(I) × P(I))
```

**总计算量对比**:
- 暴力法: 2×2×3×2×2 = 48 (所有变量组合)
- VE: 12 + 4 + 6 = 22 ← 更少！

大规模问题中差距是指数级的。

---

## 概念 4：计算复杂度 — 为什么顺序很重要？

### 核心概念：诱导图 (Induced Graph)

```
原图 G  +  消除顺序 π  →  诱导图 G_π

构造规则:
  按 π 顺序，每消除一个节点 X:
    1. 把 X 的所有邻居两两相连 (fill-in edges)
    2. 然后删掉 X
```

### 示例: 不同顺序产生不同填充

```
原图:  A—B—C—D  (链状)

顺序1: 消 A → 消 B → 消 C → 消 D
  - 消 A: A 的邻居只有 B, 无需加边 → 删 A
  - 消 B: B 的邻居是 A(已删)和 C, 无需加边 → 删 B
  - 消 C: 类似 → 删 C
  填充边总数: 0  ← 完美！

顺序2: 消 B → 消 A → 消 C → 消 D
  - 消 B: B 的邻居是 A 和 C, 连 A—C! → 删 B
  - 消 A: A 的邻居只剩 C(已连), 无需加边 → 删 A
  填充边: 1 条 (A—C)
```

**哪个顺序更好？**

```
填充边越少 → 每个中间因子的 scope 越小 → 计算量越小
```

### k-树宽度 (Treewidth)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│  图 G 的树宽度 tw(G):                                     │
│                                                           │
│    所有可能消除顺序中，最小化"最大团大小 - 1"的那个值      │
│                                                           │
│    VE 复杂度 ∝ exp(tw(G))                                 │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

| 图类型 | 树宽度 | VE 复杂度 |
|--------|--------|-----------|
| 链 (chain) | 1 | O(n·k²) 线性! |
| 树 (tree) | 1 | O(n·k²) 线性! |
| 环 (cycle) | 2 | O(n·k³) |
| 网格 (grid) | ~√n | 指数! |
| 完全图 (clique) | n-1 | 全指数! = 暴力 |

**实践意义**: 
- 树/链 → VE 非常快，顺序选好就行
- 环 → 还是能搞的
- 密集网格 → VE 不可行，必须用近似推断（L13-L18）

---

## 概念 5：变量消除 = 消息传递

### 从"消元"视角转为"消息"视角

L4 的 VE 算法和后续课程的和积算法 (Sum-Product / Belief Propagation) 本质是同一回事，只是视角不同：

```
消除视角（L4）:                       消息视角（BP）:

"把因子乘起来再求和消掉 X"            "节点 X 收到所有邻居的消息后，
                                       计算发给下一节点的消息"

φ₁ × φ₂ × φ₃ → Σ_X → τ               m_{X→Y}(y) = Σ_x [φ_X(x) × ∏ m_{Z→X}(x)]
                                           ↑             ↑          ↑
                                      X发给Y的消息   X的局部因子  X收到的所有输入消息
```

### 消息 = 消除的中间结果

```
VE 中的 τ₁(G,I) ← 消除 D 后的因子
  = Σ_D [ P(D) × P(G|D,I) ]
  = 消息 m_D→G_{给定I}(G,I)  ← D 通过 G 传给下游的信息

本质: τ = 已经"整合好"的局部信息，准备好传给下一个变量
```

### 为什么 BP 比 VE 更好？

```
VE:  每次查询 P(Q|E) 都要重头跑一遍，即使只是换个查询变量
BP:  预先计算所有边上的消息，存起来。不同查询可以复用消息
     → 所有查询一次性完成!
```

**类比**:
- VE = 每次做饭都去买菜 → 灵活但重复劳动
- BP = 先备好所有食材 → 一次投入，后续随便搭配

> 🎯 这个视角转换是 L4→L5 的关键桥梁。L4 让你理解"消元 = 重复乘加"，L5 让你看到"消元的结果 = 在图上流动的消息"。

---

## 概念 6：Sum-Product VE vs Max-Product VE

| 变体 | 求和/取max | 作用 | 
|------|-----------|------|
| Sum-Product | Σ (求和) | 计算边际概率 P(Q\|E) |
| Max-Product | max (取最大) | 计算 MAP 赋值 argmax P(Q\|E) |
| Max-Sum | log + max | Max-Product 的数值稳定版 |

---

## 📋 全部概念一张表

| 概念 | 所属 | 一句话 |
|------|------|--------|
| **推理 (Inference)** | L4 | 给定证据E，计算查询变量Q的条件概率 |
| **因子 (Factor)** | L4 | 变量取值到非负实数的映射，多维表格 |
| **因子作用域 (Scope)** | L4 | 因子涉及的变量集合 |
| **因子乘积** | L4 | 两个因子的逐元素相乘，scope = 并集 |
| **因子边缘化** | L4 | 对某个变量求和，把该变量"消掉" |
| **变量消除 (VE)** | L4 | 按顺序逐个消去非查询/非证据变量 |
| **消除顺序 π** | L4 | 消除的先后次序，决定中间因子大小 |
| **诱导图 (Induced Graph)** | L4 | 原始图在特定消除顺序下产生的填充图 |
| **填充边 (Fill-in Edge)** | L4 | 消除X时，把X的邻居两两相连 |
| **团 (Clique)** | L4 | 填充过程中出现的最大完全子图 |
| **树宽度 (Treewidth)** | L4 | 最优消除顺序下，最大团-1；决定VE复杂度 |
| **证据因子** | L4 | 固定观测变量值的 δ-型因子 |
| **消息 (Message)** | L4→L5 | 消除操作的结果 = 沿边传递的信息 |
| **Sum-Product** | L4→L5 | 用求和做边缘化的消息传递 |
| **Max-Product** | L4→L5 | 用取最大值做MAP推断的消息传递 |
| **变量消去图 (Bucket Elimination)** | L4 | VE的另一种视角：把因子按消除顺序分桶 |

---

## 🔗 概念关系图

```
                         推理问题 P(Q|E=e)
                              │
                              ▼
                    ┌──────────────────┐
                    │   联合分布因子化    │  ← L1-L3 的成果
                    │   P = ∏ φᵢ        │
                    └────────┬─────────┘
                             │
              ┌──────────────┴──────────────┐
              ▼                             ▼
        暴力枚举                      变量消除 (VE)
        (指数级)                      (复杂度 = f(treewidth))
                                          │
                          ┌───────────────┼───────────────┐
                          ▼               ▼               ▼
                     因子乘积        因子边缘化        消除顺序
                     φ₁ × φ₂          Σ_X φ            π 的选择
                          │               │               │
                          └───────┬───────┘               │
                                  ▼                       ▼
                          ┌──────────────┐        ┌──────────────┐
                          │  中间因子 τ   │        │  诱导图 G_π   │
                          └──────┬───────┘        └──────┬───────┘
                                 │                       │
                                 ▼                       ▼
                          ┌──────────────┐        ┌──────────────┐
                          │  消息 (L5)    │        │ 树宽度 tw(G)  │
                          │  在图上流动    │        │ 复杂度上界     │
                          └──────────────┘        └──────────────┘
```

---

## 🎯 理解 L4 的三个层次

| 层次 | 要求 | 标志 |
|------|------|------|
| **会用** | 给定 DAG 和消除顺序，能手推一遍 VE | 能在纸上求出正确结果 |
| **会分析** | 能判断一个消除顺序的好坏，能计算复杂度 | 能画出诱导图并对比不同顺序 |
| **会贯通** | 理解 VE = 消息传递，知悉和 BP (Sum-Product) 的联系 | 能解释为什么 BP 比 VE 效率更高 |

---

## 🧪 自测清单（看完 L4 后）

- [ ] 理解因子是什么（多维表格），能在纸上写出一个 CPD 作为因子
- [ ] 能手算两个因子的乘积
- [ ] 能手算因子的边缘化（消去某个变量）
- [ ] 记住 VE 三步法：排序 → 逐个消 → 归一化
- [ ] 能在学生网络上，用手推一遍 P(I \| L=good) 的 VE 过程
- [ ] 能解释：为什么消除顺序会影响计算量？
- [ ] 能对链状图画出诱导图，识别填充边
- [ ] 理解树宽度的定义和为什么 VE 复杂度是 O(exp(tw))
- [ ] 知道 Sum-Product 和 Max-Product 的区别
- [ ] 能用自己的话解释 VE 和消息传递的关系

---

> L5 预告: L4 学了"怎么算" (推理) — L5 转向"怎么学" (参数估计)：MLE、GLM、EM 算法、K-Means。推理和参数学习是 PGM 的两大核心能力。
